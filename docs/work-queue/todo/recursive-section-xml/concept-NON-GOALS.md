# Non-Goals: Recursive Section XML Model

## Implementation algorithms

Detailed pseudo-code for tree-reconstruction parsers (sync-edits heading-stack, finalize recursive build, merge-mode tree matching) belongs in phase plans, not the concept spec. The concept specifies the approach and interfaces; the algorithms follow from those.
