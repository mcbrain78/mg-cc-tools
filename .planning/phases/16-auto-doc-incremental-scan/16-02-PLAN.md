---
phase: 16-auto-doc-incremental-scan
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - auto-doc/references/schema.md
  - auto-doc/commands/auto-doc-scan.md
  - auto-doc/agents/scan-audience.md
  - auto-doc/commands/auto-doc-generate.md
  - auto-doc/commands/auto-doc.md
autonomous: true
requirements:
  - ISC-01
  - ISC-04
  - ISC-05
  - ISC-07
  - ISC-08
  - ISC-09

must_haves:
  truths:
    - "Scan command detects incremental mode when docs exist, manifests exist, and last_generated is present in docs-scan.json"
    - "In incremental mode, scan command runs diff-scan.py to produce diff-scope.json, then passes scoped data to each audience agent"
    - "Scan agents receive baseline entries for unchanged sections and copy them verbatim into output alongside new analysis"
    - "New files are auto-classified by scan agents into existing sections with source: incremental tag"
    - "After incremental scan, a diff-focused summary is shown (files changed, sections affected, new files, deleted references) instead of full project model summary"
    - "Generate command writes last_generated ISO timestamp to docs-scan.json at pipeline start"
    - "Verify step runs unchanged in both modes (no scoping)"
    - "Deleting docs dir and docs-scan.json falls back to initial mode (full scan)"
    - "Router Route D mentions that re-scan will be incremental"
  artifacts:
    - path: "auto-doc/references/schema.md"
      provides: "last_generated field documentation and diff-scope.json contract"
      contains: "last_generated"
    - path: "auto-doc/commands/auto-doc-scan.md"
      provides: "Incremental mode detection, diff-scan.py invocation, scoped agent dispatch"
      contains: "incremental"
    - path: "auto-doc/agents/scan-audience.md"
      provides: "Incremental mode handling with carry-forward and new file classification"
      contains: "incremental"
    - path: "auto-doc/commands/auto-doc-generate.md"
      provides: "last_generated timestamp write at pipeline start"
      contains: "last_generated"
    - path: "auto-doc/commands/auto-doc.md"
      provides: "Route D updated with incremental scan mention"
      contains: "incremental"
  key_links:
    - from: "auto-doc/commands/auto-doc-scan.md"
      to: "auto-doc/scripts/diff-scan.py"
      via: "python3 {SCRIPTS_DIR}/diff-scan.py invocation"
      pattern: "diff-scan\\.py"
    - from: "auto-doc/commands/auto-doc-scan.md"
      to: "auto-doc/agents/scan-audience.md"
      via: "Task tool with incremental mode context"
      pattern: "Mode: incremental"
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: ".mg/docs/docs-scan.json"
      via: "Write last_generated timestamp"
      pattern: "last_generated"
---

<objective>
Wire incremental scan mode into the auto-doc pipeline: update the schema to document the new fields, modify the scan command to detect and run incremental mode, update the scan-audience agent to handle scoped input with carry-forward, add last_generated timestamp writing to the generate command, and update the router to mention incremental capability.

Purpose: Complete the incremental scan feature by connecting diff-scan.py (built in Plan 01) into the existing pipeline so the full incremental workflow operates end-to-end.

Output: Updated schema, scan command, scan agent, generate command, and router -- all pipeline files needed for incremental scan.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auto-doc-incremental-scan/16-CONTEXT.md
@.planning/phases/16-auto-doc-incremental-scan/16-RESEARCH.md
@.planning/phases/16-auto-doc-incremental-scan/16-01-SUMMARY.md

<interfaces>
<!-- diff-scan.py CLI interface (from Plan 01) -->
```
python3 {SCRIPTS_DIR}/diff-scan.py \
    --project-root <path> \
    --manifests-dir <path to .mg/docs/reference-manifests/> \
    --docs-dir <path to docs/auto-doc/> \
    --since <last generation ISO timestamp> \
    --gsd-dir <path to .planning/phases/> \
    --output <path to .mg/docs/diff-scope.json>
```

Output: diff-scope.json at the specified --output path with fields:
  since, summary, affected_sections, new_file_candidates, deleted_files, gsd_phases_since

<!-- Current scan command step numbers for reference -->
Step 1-11: Orient (project structure, config, interfaces)
Step 12: Detect mode (initial vs update)
Step 13: Clear scan-logs
Step 14: Create workspace
Step 15: Write orientation files
Step 2 (GSD): GSD Context
Step 3 (staleness): Staleness Check (update mode only)
Step 4: Notes Classification
Step 5: Per-Audience Scan (parallel via Task)
Step 6: Merge and Present

<!-- docs-scan.json top-level fields -->
project, scan_date, root_path, mode, project_model, source_material_index,
staleness_report, note_classifications, gap_analysis, gsd_context

<!-- Reference manifest location -->
.mg/docs/reference-manifests/{audience}.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema update + generate command last_generated timestamp</name>
  <files>auto-doc/references/schema.md, auto-doc/commands/auto-doc-generate.md</files>
  <action>
1. Update auto-doc/references/schema.md:
   a. Add a new top-level field section for `last_generated` after the `mode` field documentation:
      - Type: string (ISO 8601) | null
      - Required: no (null or absent for initial scans; present after first generation)
      - Description: ISO timestamp of when the generate command last ran. Written by the generate command at pipeline start. Used by the scan command to detect incremental mode and by diff-scan.py to scope changes. Over-inclusive by design: commits during generation appear in the next diff rather than being silently missed.
      - Example: "2026-03-22T14:30:00Z"
   b. Add `last_generated` to the Structure overview JSON block at the top.
   c. Add `last_generated` to the Complete Minimal Example at the bottom (set to null for initial scan).
   d. Add a new section "Diff Scope: diff-scope.json" after the "Verify Findings" section documenting the diff-scope.json format:
      - Location: .mg/docs/diff-scope.json (NOT scan-logs/)
      - Purpose: Scoped work order produced by diff-scan.py for incremental scans
      - Full JSON structure with field descriptions matching the locked format from CONTEXT.md
      - Lifecycle: created by scan command in incremental mode, read by scan agents, deleted on next scan run
   e. Add diff-scope.json to the File Location Convention tree.

2. Update auto-doc/commands/auto-doc-generate.md:
   a. In Step 3 (Prepare Workspace), add a new substep BEFORE "Stage 1" (after manifest prep, as the last substep of Step 3):
      ```
      5. **Write last_generated timestamp.** Record the current time as the generation baseline for future incremental scans:
         ```python3
         python3 -c "
      import json, os, datetime
      scan_path = '{project_root}/.mg/docs/docs-scan.json'
      with open(scan_path) as f: data = json.load(f)
      data['last_generated'] = datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')
      tmp = scan_path + '.tmp'
      with open(tmp, 'w') as f: json.dump(data, f, indent=2, ensure_ascii=False); f.write('\n')
      os.replace(tmp, scan_path)
      "
         ```
         This timestamp is written at pipeline START so the next incremental scan's diff window is over-inclusive (commits during this generation cycle will be re-scanned next time, which is harmless).
  </action>
  <verify>
    <automated>grep -c "last_generated" auto-doc/references/schema.md && grep -c "last_generated" auto-doc/commands/auto-doc-generate.md && grep -c "diff-scope" auto-doc/references/schema.md</automated>
  </verify>
  <done>
    - schema.md documents last_generated field with type, description, and example
    - schema.md documents diff-scope.json format in a new section
    - schema.md file location convention tree includes diff-scope.json
    - auto-doc-generate.md writes last_generated to docs-scan.json at pipeline start (Step 3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Scan command incremental mode + scan-audience agent carry-forward</name>
  <files>auto-doc/commands/auto-doc-scan.md, auto-doc/agents/scan-audience.md</files>
  <action>
1. Update auto-doc/commands/auto-doc-scan.md:

   a. Modify Step 12 (Detect mode) to add incremental detection AFTER the existing initial/update detection:
      ```
      12b. **Detect incremental mode.** If mode is "update":
          a. Read `.mg/docs/docs-scan.json` and check for `last_generated` field (non-null string)
          b. Check `.mg/docs/reference-manifests/` for at least one *.json manifest file (via Bash `ls`)
          c. If BOTH exist: `mode = "incremental"` (upgrade from "update")
          d. If either missing: remain as `mode = "update"` (full scan, same as current)
          e. Print the detected mode: "Mode: incremental (scoped to changes since {last_generated})" or "Mode: update (full scan)"
      ```

   b. Add a new Step 2b (between existing Step 2 GSD and Step 3 Staleness) for incremental-only work:
      ```
      ### Step 2b: Incremental Diff Scoping (incremental mode only)

      **Only run this step if** mode is "incremental". If mode is "initial" or "update", skip entirely.

      1. **Load previous scan data.** Read `.mg/docs/docs-scan.json` and store the complete object in memory as `previous_scan`. This provides baseline entries for carry-forward. Read this BEFORE clearing scan-logs in Step 13 (Pitfall 5).

      2. **Run diff-scan.py** to produce the scoped work order:
         ```bash
         python3 {SCRIPTS_DIR}/diff-scan.py \
             --project-root <project_root> \
             --manifests-dir <project_root>/.mg/docs/reference-manifests \
             --docs-dir <docs_dir_abs> \
             --since <last_generated value from docs-scan.json> \
             --gsd-dir <project_root>/.planning/phases \
             --output <project_root>/.mg/docs/diff-scope.json
         ```
         If `--gsd-dir` path doesn't exist, still pass it -- diff-scan.py handles missing dirs gracefully.

      3. **Read diff-scope.json** from `<project_root>/.mg/docs/diff-scope.json`. Extract `affected_sections`, `new_file_candidates`, `deleted_files`, `gsd_phases_since`, and `summary`.

      4. **Prepare per-audience scoped data.** For each enabled audience, filter:
         - `audience_affected`: entries from `affected_sections` where `audience` matches
         - `audience_new_files`: entries from `new_file_candidates` (all audiences get all new files for classification)
         - `audience_baseline`: from `previous_scan.source_material_index`, extract entries whose key starts with a document name belonging to this audience. These are the unchanged entries to carry forward.

         For baseline extraction: for each key in `source_material_index` (format "DOCUMENT/section-slug"), check if DOCUMENT is in the audience's document list. If so, and if that (DOCUMENT, section-slug) pair is NOT in `audience_affected`, include it as a baseline entry.

      5. **Build GSD context string.** From `gsd_phases_since`, format a concise summary:
         ```
         GSD phases since last generation:
           Phase {phase}: {name}
             Deviations: {deviations or "none"}
             Key decisions: {key_decisions or "none"}
         ```
         If `gsd_phases_since` is empty, set to null/omit.
      ```

   c. Modify Step 3 (Staleness Check): Add note that in incremental mode, skip the staleness check step entirely. diff-scan.py replaces staleness check's scoping role. Add:
      ```
      **If mode is "incremental", skip this step.** In incremental mode, diff-scan.py replaces staleness check's scoping role. Staleness check continues as post-generate validation only.
      ```

   d. Modify Step 5 (Per-Audience Scan) to handle incremental mode. After the existing task prompt, add an incremental-mode variant:
      ```
      **If mode is "incremental"**, modify the Task prompt for each audience. Replace the standard prompt with:
      ```
      Task(
        description="Incremental scan for {audience} audience",
        prompt="You are a scan subagent for the {audience} audience.

      [paste full contents of agents/scan-audience.md here]

      Project root: {project_root}
      Read orientation: {project_root}/.mg/docs/scan-logs/scan-orientation.md
      Your audience: {audience}
      Your documents: {document_list}
      Templates directory: {TEMPLATES_DIR}
      Write output: {project_root}/.mg/docs/scan-logs/scan-{audience}.json
      Scripts directory: {SCRIPTS_DIR}

      Mode: incremental
      Changed files for your audience: {JSON array of changed_files from audience_affected entries}
      GSD context: {gsd_context_string or 'None'}

      Affected sections to re-analyze:
      {JSON of audience_affected entries with document, section, reason, changed_files, renames}

      Baseline entries for unchanged sections (copy VERBATIM into your output):
      {JSON of audience_baseline entries}

      New file candidates to classify into your document sections:
      {JSON array of audience_new_files entries}"
      )
      ```

   e. Modify Step 6 (Merge and Present) to handle the incremental summary. After the existing summary presentation, add:
      ```
      **If mode is "incremental"**, present a diff-focused summary instead of the full project model summary:
      ```
      Incremental scan complete (scoped to changes since {last_generated}).

        Files changed: {summary.files_changed}
        Files added:   {summary.files_added}
        Files deleted: {summary.files_deleted}
        Sections affected: {summary.sections_affected} across {count unique audiences in affected_sections} audiences
        New files classified: {summary.new_file_candidates}

      Run `/mg:auto-doc-generate` to update affected documentation.
      ```
      Do NOT repeat the full project model summary (tech stack, components, etc.) -- that is unchanged.
      ```

   f. Add Step 2b reading of previous_scan to happen BEFORE Step 13 (clear scan-logs). Update the ordering note: "In incremental mode, read previous docs-scan.json in Step 2b BEFORE clearing scan-logs in Step 13."

2. Update auto-doc/agents/scan-audience.md:

   a. Add a new section "## Incremental Mode" after the "## Process" section (before step 5 Write output), explaining incremental behavior:
      ```
      ## Incremental Mode

      When the orchestrator passes `Mode: incremental` in your prompt, you operate differently:

      1. **Analyze only changed sections.** The orchestrator provides a list of affected sections with their changed files. For each affected section:
         - Read the changed files listed in the affected section entry
         - Re-analyze and rebuild the source_material_index entry for that section
         - If the entry includes a `renames` dict, update any source_files paths that match old rename paths to their new paths
         - Set staleness to "unknown" (same as normal mode)

      2. **Carry forward unchanged sections.** The orchestrator provides baseline entries for sections NOT affected by changes. Copy these entries VERBATIM into your output. Do not re-analyze them. Do not modify their source_files or staleness values.

      3. **Classify new files.** The orchestrator provides new file candidates not in any manifest. For each candidate:
         - Read the file to understand its content and purpose
         - Classify it into the most appropriate existing document section based on content, file path, and audience template structure
         - Add it to the appropriate source_material_index entry's source_files array
         - Tag the entry with `"source": "incremental"` so the generate step can highlight it

      4. **Write complete output.** Your output JSON must contain ALL entries: re-analyzed affected entries + carried-forward baseline entries + new file additions. merge-scan.py expects complete per-audience output regardless of mode.

      5. **Gap analysis in incremental mode.** Only report gaps for components related to changed files. Do not re-report gaps for unchanged areas.
      ```

   b. In the existing "## Output Format" section, add the `"source": "incremental"` optional field to the source_material_index entry example:
      ```json
      "DOCUMENT/section-slug": {
        "source_files": ["relative/path/to/file.py"],
        "staleness": "unknown",
        "source": "incremental"
      }
      ```
      Add note: `The "source" field is optional. Only present on entries added during incremental scan for new file classification. Omit for carried-forward entries and normal scan entries.`

   c. In the "## Principles" section, add:
      ```
      - **In incremental mode, completeness is critical.** Your output must contain ALL section entries (changed + unchanged). Missing entries cause merge-scan.py to lose data for those sections.
      - **Carry-forward entries are verbatim.** Do not modify baseline entries. Copy them exactly as provided.
      ```

3. Update auto-doc/commands/auto-doc.md (router):

   a. In Route D (Pipeline complete), update the "Re-scan" option text to indicate incremental behavior:
      ```
      - Re-scan:   /mg:auto-doc-scan      (incremental -- scoped to changes since last generation)
      ```
  </action>
  <verify>
    <automated>grep -c "incremental" auto-doc/commands/auto-doc-scan.md && grep -c "incremental" auto-doc/agents/scan-audience.md && grep -c "incremental" auto-doc/commands/auto-doc.md</automated>
  </verify>
  <done>
    - Scan command detects incremental mode (ISC-01) based on last_generated + manifests existence
    - Scan command runs diff-scan.py and passes scoped data per-audience to agents (ISC-04)
    - Scan command skips staleness check in incremental mode
    - Scan command shows diff-focused summary after incremental scan (ISC-07)
    - Scan-audience agent handles incremental inputs: re-analyzes affected sections, carries forward unchanged sections verbatim, classifies new files with "source": "incremental" tag (ISC-05)
    - Router Route D mentions incremental re-scan capability
    - ISC-08 inherently satisfied (verify runs unchanged, no modifications to verify pipeline)
    - ISC-09 inherently satisfied (mode detection falls back to initial when last_generated missing)
  </done>
</task>

</tasks>

<verification>
1. Schema has last_generated field documented and diff-scope.json section
2. Generate command writes last_generated timestamp at Step 3
3. Scan command detects incremental mode and runs diff-scan.py
4. Scan-audience agent has incremental mode instructions
5. Router Route D mentions incremental
6. Full auto-doc test suite still passes:
   python3 -m pytest auto-doc/scripts/tests/ -x --tb=short -q --no-header
</verification>

<success_criteria>
- schema.md documents last_generated and diff-scope.json format
- auto-doc-generate.md writes last_generated to docs-scan.json at pipeline start
- auto-doc-scan.md detects incremental mode and runs diff-scan.py, passes scoped data to agents
- scan-audience.md handles incremental mode with carry-forward and new file classification
- auto-doc.md Route D mentions incremental scan
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/16-auto-doc-incremental-scan/16-02-SUMMARY.md`
</output>
