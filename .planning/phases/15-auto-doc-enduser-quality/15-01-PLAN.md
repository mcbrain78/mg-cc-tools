---
phase: 15-auto-doc-enduser-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auto-doc/references/schema.md
  - auto-doc/references/templates/end-users/USER_GUIDE.template.md
  - auto-doc/references/templates/end-users/DOMAIN_SPECIFIC.template.md
  - auto-doc/references/.docs.config.json
autonomous: true
requirements:
  - EUQ-04
  - EUQ-06
  - EUQ-07
  - EUQ-09
  - EUQ-10

must_haves:
  truths:
    - "USER_GUIDE template has exactly 7 ## headings: Overview, Key Concepts, Workflows, Getting Started, Common Tasks, Configuration, Troubleshooting"
    - "Three new sections (Overview, Key Concepts, Workflows) have SYNTHESIZED comments with field lists"
    - "Getting Started and Configuration sections have BOUNDARY comments redirecting infrastructure content"
    - "All 7 section exemplars use the road-runner portfolio analytics domain with web-UI interface style"
    - "schema.md documents user_interfaces as an optional array on project_model and synthesized_from as an optional array on source_material_index entries"
    - "DOMAIN_SPECIFIC.template.md is deleted from end-users templates directory"
  artifacts:
    - path: "auto-doc/references/templates/end-users/USER_GUIDE.template.md"
      provides: "7-section functional-first end-user template with SYNTHESIZED, BOUNDARY, PURPOSE, and EXAMPLE comments"
      contains: "SYNTHESIZED"
    - path: "auto-doc/references/schema.md"
      provides: "Updated data contract with user_interfaces and synthesized_from fields"
      contains: "user_interfaces"
  key_links:
    - from: "auto-doc/references/templates/end-users/USER_GUIDE.template.md"
      to: "auto-doc/agents/scan-audience.md"
      via: "SYNTHESIZED and BOUNDARY HTML comments parsed by scan agent"
      pattern: "SYNTHESIZED:"
    - from: "auto-doc/references/schema.md"
      to: "auto-doc/commands/auto-doc-scan.md"
      via: "user_interfaces field written to scan-project.json"
      pattern: "user_interfaces"
---

<objective>
Update the data contracts (schema and template) that the scan agent and writer agent consume. Restructure USER_GUIDE template from 4-section CLI-centric to 7-section functional-first with SYNTHESIZED, BOUNDARY comment types and interface-aware exemplars. Add user_interfaces and synthesized_from fields to schema. Delete DOMAIN_SPECIFIC template. Add user_interfaces to default config.

Purpose: Establish the contracts before scan and writer agents are updated -- both downstream plans depend on these files.
Output: Updated schema.md, rewritten USER_GUIDE.template.md, deleted DOMAIN_SPECIFIC.template.md, updated .docs.config.json
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-auto-doc-enduser-quality/15-CONTEXT.md
@.planning/phases/15-auto-doc-enduser-quality/15-RESEARCH.md

@auto-doc/references/schema.md
@auto-doc/references/templates/end-users/USER_GUIDE.template.md
@auto-doc/references/.docs.config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema.md and default config with new fields</name>
  <files>auto-doc/references/schema.md, auto-doc/references/.docs.config.json</files>
  <action>
**schema.md changes:**

1. Add `user_interfaces` as a new optional field under `project_model`:
   - After the `infrastructure` subsection, add a new `### project_model.user_interfaces` section
   - Type: `array of object` (optional -- field may be absent)
   - Description: Detected or configured user interface types for the project. When present, writer agents use this to adapt documentation style to the project's primary interface.
   - Each object fields:

     | Field | Type | Required | Description |
     |-------|------|----------|-------------|
     | `type` | `string` | yes | Interface type: `"web"`, `"cli"`, `"api"` |
     | `name` | `string` | yes | Human-readable interface name |
     | `url_pattern` | `string or null` | yes | URL pattern for web interfaces, null for CLI/API |
     | `primary` | `boolean` | yes | Whether this is the primary user interface |

   - Add example JSON showing both web (primary) and cli (secondary) entries, matching the RESEARCH.md example

2. Add `synthesized_from` as an optional field on `source_material_index` entries:
   - In the `source_material_index` section, after the existing value object table, add a note:
     "Optional field for synthesized sections (sections generated from project model fields instead of source files):"
   - Add `synthesized_from` row to the value object table:

     | Field | Type | Required | Description |
     |-------|------|----------|-------------|
     | `synthesized_from` | `array of string` | no | Dotted field paths into scan data (e.g., `"project_model.components"`). When present with empty `source_files`, signals the writer to generate from project model fields instead of source files. |

   - Update the source_material_index JSON example to include a synthesized entry (e.g., `USER_GUIDE/overview` with empty source_files and synthesized_from array)

3. Update the project_model JSON example block at the top of the `project_model` section to include a `user_interfaces` array with one web and one cli entry.

4. Update the Complete Minimal Example at the bottom of the file: add `"user_interfaces": []` inside `project_model` to show it exists but is optional/empty.

**Default config changes (.docs.config.json):**

Add `"user_interfaces": []` as a top-level field after `"gsd_integration"`. This empty array signals "not yet detected" -- the scan command populates it on first run and persists to the project's local config.
  </action>
  <verify>
    <automated>python3 -m pytest auto-doc/scripts/tests/ -x --tb=short -q --no-header</automated>
  </verify>
  <done>schema.md documents user_interfaces (with field table and example) and synthesized_from (with field table and example). Default config has user_interfaces field. All existing tests pass (no Python changes, but confirms no regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite USER_GUIDE template and delete DOMAIN_SPECIFIC template</name>
  <files>auto-doc/references/templates/end-users/USER_GUIDE.template.md, auto-doc/references/templates/end-users/DOMAIN_SPECIFIC.template.md</files>
  <action>
**Complete rewrite of USER_GUIDE.template.md** with 7 sections. The template must preserve the existing comment pattern conventions (DIATAXIS, AUDIENCE, docs-meta at top) while adding SYNTHESIZED and BOUNDARY comment types.

Structure (in exact order):

```
<!-- DIATAXIS: how-to -->
<!-- AUDIENCE: end-users -->

# User Guide

<!-- docs-meta: last-updated: {date}, sources: [{source_files}] -->
```

Then 7 sections, each with its HTML comments:

**1. ## Overview**
- `<!-- SYNTHESIZED: project_model.components, project_model.user_interfaces -->`
- `<!-- PURPOSE: Introduce what this guide covers and what the reader will gain from it. This is a guide-level introduction -- NOT a product overview (that's OVERVIEW.md) and NOT audience routing (that's also OVERVIEW.md). Orient the reader within the end-user guide itself. -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner exemplar: guide-level intro explaining the guide covers portfolio management workflows through the Road Runner Dashboard, what the reader will learn (scoring, rebalancing, exporting), and what's NOT covered (deployment, API details, agent configuration with cross-refs).

**2. ## Key Concepts**
- `<!-- SYNTHESIZED: project_model.components, project_model.entry_points -->`
- `<!-- PURPOSE: Define domain and interaction concepts the user needs before tackling procedures. Bridge domain vocabulary (portfolio, scoring model, rebalancing signal) with user actions (scoring run, dashboard view). Keep definitions concrete -- "a portfolio is a set of stock positions you track" not "a portfolio is a collection entity." -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner exemplar: 4-5 concept definitions mixing domain terms (portfolio, position, scoring model) with interaction terms (scoring run, rebalancing signal), each with a one-sentence definition and a concrete example.

**3. ## Workflows**
- `<!-- SYNTHESIZED: project_model.entry_points, project_model.user_interfaces -->`
- `<!-- PURPOSE: Map end-to-end user journeys. Each workflow is a named journey (e.g., "Quarterly Portfolio Review") with 3-5 high-level numbered steps that link to detailed procedures in Common Tasks. Workflows are a navigation layer -- not a duplication of procedures. -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner exemplar: 2 workflows ("Quarterly Portfolio Review" and "New Stock Evaluation") each with 3-5 high-level steps referencing Common Tasks sections, showing the web-UI journey.

**4. ## Getting Started**
- `<!-- BOUNDARY: Infrastructure setup and installation belong in devops/OPERATIONS.md, not here. This section covers first use of a running system. -->`
- `<!-- PURPOSE: Walk the user through their first interaction with the running system. Assume infrastructure is already deployed. Focus on the user's interface (web UI, CLI, etc.), not installation or deployment. -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner web-UI exemplar: Open the dashboard at the provided URL, navigate to Portfolios, add your first stock by clicking "Add Position" and entering a ticker, run your first scoring by clicking "Score Now", understand the score card (with a `> **Power user tip:** You can also run scoring from the CLI: `rr score --portfolio default`` callout).

**5. ## Common Tasks**
- `<!-- PURPOSE: Cover the 3-5 operations users perform most frequently. For each task: state the goal first (what the user is accomplishing and why), explain what the system will do (duration, what happens), then give steps through the primary interface, then expected results. -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner web-UI exemplar: "Updating Quarterly Data" (goal: refresh market data for accurate scores; system behavior: fetches latest prices, takes ~2 minutes; steps: click "Data" > "Refresh Quarterly", confirm the date range, click "Start Refresh"; expected result: "The Data page shows 'Last refreshed: [today's date]' and all position scores update within 5 minutes"). Second task: "Exporting a Portfolio Report" (goal -> steps -> expected result pattern, web-UI style, with CLI power-user tip).

**6. ## Configuration**
- `<!-- BOUNDARY: System-level configuration (environment variables, service settings, database connections) belongs in devops/OPERATIONS.md. Only user-facing settings that affect the user's experience belong here. -->`
- `<!-- PURPOSE: Document user-facing configuration options that affect the user's experience. Use a table for quick scanning. Show how to change settings through the user's interface. -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner web-UI exemplar: user settings table (scoring model, alert threshold, portfolio visibility) with how to change them via Settings page. "Changing Tracked Stocks" sub-task showing the add/remove flow through the UI.

**7. ## Troubleshooting**
- `<!-- OPTIONAL -- delete if not applicable -->`
- `<!-- PURPOSE: Address the most common problems users encounter. Structure each entry as: what the user sees (symptom), likely cause, and what to do (steps through the user's interface). -->`
- `<!-- EXAMPLE: ... -->` -- Road-runner web-UI exemplar: "No data found for position" (the dashboard shows 'No data found' next to a position -> likely cause: ticker not in market data feed or data not refreshed -> steps: check ticker spelling on the Positions page, click Data > Refresh, wait 2 minutes, return to Positions). Second entry: "Scores show N/A" (scores column shows N/A -> likely cause: insufficient historical data or data refresh in progress -> steps: check Data page for refresh status, if complete check position has >30 days of history).

**Explicit writer guidance (add as a comment after the last section):**
```
<!-- WRITER NOTE: Exemplars above demonstrate web-UI style as the reference case.
     If the project's primary interface is CLI or API, follow the same structure --
     functional context before procedure, expected results after steps -- but use
     commands/responses (CLI) or requests/responses (API) instead of click paths.
     Secondary interfaces appear as > **Power user tip:** callouts. -->
```

**Important:** All exemplars must use the road-runner portfolio analytics domain for consistency with existing sections. All exemplars must show web-UI style as the reference case. Each exemplar should be 8-15 lines of concrete content showing what "good" looks like.

**Delete DOMAIN_SPECIFIC.template.md:**
Delete the file `auto-doc/references/templates/end-users/DOMAIN_SPECIFIC.template.md`. Per locked decision, it is removed from v1.1 scope.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
# Verify USER_GUIDE template structure
with open('auto-doc/references/templates/end-users/USER_GUIDE.template.md') as f:
    content = f.read()
headings = re.findall(r'^## (.+)$', content, re.MULTILINE)
expected = ['Overview', 'Key Concepts', 'Workflows', 'Getting Started', 'Common Tasks', 'Configuration', 'Troubleshooting']
assert headings == expected, f'Expected {expected}, got {headings}'
assert '<!-- SYNTHESIZED:' in content, 'Missing SYNTHESIZED comments'
assert content.count('<!-- SYNTHESIZED:') == 3, f'Expected 3 SYNTHESIZED, got {content.count(\"<!-- SYNTHESIZED:\")}'
assert '<!-- BOUNDARY:' in content, 'Missing BOUNDARY comments'
assert content.count('<!-- BOUNDARY:') == 2, f'Expected 2 BOUNDARY, got {content.count(\"<!-- BOUNDARY:\")}'
assert content.count('<!-- PURPOSE:') == 7, f'Expected 7 PURPOSE, got {content.count(\"<!-- PURPOSE:\")}'
assert content.count('<!-- EXAMPLE:') == 7, f'Expected 7 EXAMPLE, got {content.count(\"<!-- EXAMPLE:\")}'
# Verify DOMAIN_SPECIFIC deleted
import os
assert not os.path.exists('auto-doc/references/templates/end-users/DOMAIN_SPECIFIC.template.md'), 'DOMAIN_SPECIFIC.template.md should be deleted'
# Verify schema has new fields
with open('auto-doc/references/schema.md') as f:
    schema = f.read()
assert 'user_interfaces' in schema, 'schema missing user_interfaces'
assert 'synthesized_from' in schema, 'schema missing synthesized_from'
print('All checks passed')
"
    </automated>
  </verify>
  <done>USER_GUIDE template has exactly 7 sections with correct comment types (3 SYNTHESIZED, 2 BOUNDARY, 7 PURPOSE, 7 EXAMPLE). All exemplars use road-runner domain with web-UI style. DOMAIN_SPECIFIC.template.md is deleted. Schema documents both new fields with examples.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest auto-doc/scripts/tests/ -x --tb=short -q --no-header` -- all existing tests pass (no Python changes)
2. Template structural validation: 7 headings, 3 SYNTHESIZED, 2 BOUNDARY, 7 PURPOSE, 7 EXAMPLE
3. Schema contains `user_interfaces` and `synthesized_from` field documentation with examples
4. DOMAIN_SPECIFIC.template.md does not exist
5. Default config has `user_interfaces` field
</verification>

<success_criteria>
- USER_GUIDE.template.md has 7 sections in correct order with all required comment types
- Schema.md documents user_interfaces (project_model) and synthesized_from (source_material_index) with field tables and JSON examples
- DOMAIN_SPECIFIC.template.md is deleted
- .docs.config.json has user_interfaces field
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-auto-doc-enduser-quality/15-01-SUMMARY.md`
</output>
