---
phase: 15-auto-doc-enduser-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - auto-doc/agents/end-user-writer.md
autonomous: true
requirements:
  - EUQ-02
  - EUQ-05
  - EUQ-08

must_haves:
  truths:
    - "Writer reads user_interfaces from scan data and adapts all procedures to the project's primary interface style"
    - "Writer generates web procedures as click paths, CLI as commands, API as requests -- with secondary interfaces as Power user tip callouts"
    - "Writer generates synthesized sections from project model fields when synthesized_from is present and source_files is empty"
    - "Writer emits TODO placeholder when insufficient project model data exists for a synthesized section"
    - "Writer respects BOUNDARY comments by cross-referencing the named alternate document instead of generating bounded content"
    - "Writer follows functional-first pattern: goal, system behavior, steps, secondary tip, expected results"
    - "Old end-user docs should be deleted and regenerated fresh with the new template"
  artifacts:
    - path: "auto-doc/agents/end-user-writer.md"
      provides: "Interface-aware, functional-first writer agent with synthesized section support and BOUNDARY handling"
      contains: "user_interfaces"
  key_links:
    - from: "auto-doc/agents/end-user-writer.md"
      to: "auto-doc/references/schema.md"
      via: "Reads project_model.user_interfaces from scan data JSON"
      pattern: "user_interfaces"
    - from: "auto-doc/agents/end-user-writer.md"
      to: "auto-doc/references/templates/end-users/USER_GUIDE.template.md"
      via: "Parses SYNTHESIZED, BOUNDARY, PURPOSE, EXAMPLE comments from template"
      pattern: "SYNTHESIZED"
---

<objective>
Rewrite the end-user writer agent to be interface-aware, support synthesized sections, respect BOUNDARY comments, and follow a functional-first pattern for all procedures. Add guidance that old end-user docs should be deleted and regenerated fresh.

Purpose: The writer is the final consumer of the scan data and template contracts. It needs to produce functional, interface-aware documentation instead of defaulting to CLI-centric output.
Output: Updated end-user-writer.md with interface-aware generation, synthesized section support, BOUNDARY handling, and functional-first conventions
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-auto-doc-enduser-quality/15-CONTEXT.md
@.planning/phases/15-auto-doc-enduser-quality/15-RESEARCH.md
@.planning/phases/15-auto-doc-enduser-quality/15-01-SUMMARY.md

@auto-doc/agents/end-user-writer.md

<interfaces>
<!-- From 15-01: Schema defines user_interfaces and synthesized_from -->
<!-- From 15-01: Template has 7 sections with SYNTHESIZED, BOUNDARY, PURPOSE, EXAMPLE comments -->

From auto-doc/references/schema.md (after Plan 01):
- project_model.user_interfaces: optional array of {type, name, url_pattern, primary}
- source_material_index entries: optional synthesized_from array of string

From auto-doc/references/templates/end-users/USER_GUIDE.template.md (after Plan 01):
- 7 sections: Overview, Key Concepts, Workflows, Getting Started, Common Tasks, Configuration, Troubleshooting
- Sections with <!-- SYNTHESIZED: field1, field2 --> generate from project model fields
- Sections with <!-- BOUNDARY: description --> exclude bounded content and cross-reference
- <!-- WRITER NOTE: ... --> at bottom guides interface style adaptation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite end-user-writer.md with interface awareness, synthesized sections, BOUNDARY handling, and functional-first pattern</name>
  <files>auto-doc/agents/end-user-writer.md</files>
  <action>
Rewrite the end-user writer agent. Preserve the overall structure (Role, Inputs, Documents, Process, End-User-Specific Conventions, Output Conventions, Principles) but add new capabilities. The agent should remain approximately 80-120 lines -- concise but complete.

**Changes to Process section:**

Insert a new step 1.5 between existing step 1 ("Read context") and step 2 ("For each assigned document"):

```markdown
2. **Determine interface style.** Read `project_model.user_interfaces` from the scan data JSON.
   - If the field is absent or an empty array: set `interface_style = "cli"` (default, backward compatible).
   - Find the object with `primary: true` -> this is the `primary_interface`.
   - All other objects -> `secondary_interfaces`.
   - Set `interface_style` based on `primary_interface.type`:
     - `"web"` -> describe click paths, form fields, screen states, expected visual results
     - `"cli"` -> describe commands, flags, expected terminal output
     - `"api"` -> describe HTTP requests, response bodies, status codes
   - Secondary interfaces appear as `> **Power user tip:** ...` callouts after primary interface steps.
```

Renumber subsequent steps (old step 2 becomes step 3).

**Changes to step 3 (formerly step 2) -- "For each assigned document":**

Update sub-step c ("For each section") to handle SYNTHESIZED and BOUNDARY comments:

```markdown
   c. For each section:
      - Read all HTML comments for the section: `<!-- PURPOSE: ... -->`, `<!-- EXAMPLE: ... -->`, `<!-- SYNTHESIZED: ... -->`, `<!-- BOUNDARY: ... -->`, `<!-- OPTIONAL ... -->`.

      - **If `<!-- SYNTHESIZED: field1, field2 -->` is present:**
        Look up the source_material_index entry for this section. If `synthesized_from` is present and `source_files` is empty:
        1. Read the named project model fields from scan data (e.g., `project_model.components`, `project_model.user_interfaces`)
        2. Generate section content purely from these structured fields -- do NOT read source files, do NOT infer beyond field contents
        3. If the project model lacks sufficient data for a meaningful section (e.g., no components, no entry points), emit:
           `<!-- TODO: needs manual input -- insufficient scan data for this section -->`
           followed by a brief placeholder paragraph explaining what content should go here
        4. Synthesized sections still get `docs-meta` comments with `sources: []`
        5. Skip manifest entry emission for synthesized sections (no code symbols or file paths to record)

      - **If `<!-- BOUNDARY: description -->` is present:**
        1. Read the boundary description to understand what content belongs elsewhere
        2. Do NOT generate content matching the boundary description (e.g., if boundary says "Infrastructure setup belongs in devops/OPERATIONS.md", do not write installation or deployment instructions)
        3. Instead, add a cross-reference callout near the top of the section:
           `> For [bounded topic], see [named alternate document].`
           Example: `> For infrastructure setup and installation, see [OPERATIONS.md](../devops/OPERATIONS.md).`
        4. Continue generating the section's non-bounded content normally (BOUNDARY restricts what goes in, it doesn't skip the section)

      - Read the `<!-- PURPOSE: ... -->` comment to understand what to generate.
      - Read the `<!-- EXAMPLE: ... -->` comment to understand what "good" looks like. **Important:** Exemplars demonstrate web-UI style as the reference case. If the project's primary interface is CLI or API, follow the same structure (functional context before procedure, expected results after steps) but use commands/responses (CLI) or requests/responses (API) instead of click paths.
      - Look up source material and generate as before (existing logic unchanged).
```

**Changes to End-User-Specific Conventions:**

Replace the current conventions with an updated set that includes the functional-first pattern:

```markdown
## End-User-Specific Conventions

These conventions override or extend the style guide for end-user documentation.

- **Functional-first pattern.** Every procedural section follows this structure:
  1. **Goal:** What is the user accomplishing? Why does it matter?
  2. **System behavior:** What will the system do? How long? What to expect?
  3. **Steps** through the primary interface (web -> click paths, CLI -> commands, API -> requests)
  4. **Secondary interface tip** (if applicable): `> **Power user tip:** ...` callout
  5. **Expected results:** What the user sees when done (web: "appears in dashboard", CLI: terminal output, API: response body)
- **Interface-aware procedures.** All procedures use the project's primary interface style. Never default to CLI unless the project's interface is actually CLI.
- **Plain language.** No jargon. If a technical term is unavoidable, define it inline on first use.
- **Task-oriented structure.** Organize by "How do I..." not by system module.
- **Scannable formatting.** Numbered steps for procedures, bullet lists for options, tables for comparisons.
- **Numbered steps.** One action per step. Maximum 7 steps per procedure.
- **Expected results.** After each procedure, state what the user should see through their interface.
- **Progressive disclosure.** Overview first, details in expandable sections or linked pages.
- **No implementation details.** Users don't need to know which library handles auth or how the database is structured.
- **Cross-audience boundaries.** Enforce these redirects:
  - Installation and infrastructure setup -> devops OPERATIONS.md
  - API details and internal architecture -> developer ARCHITECTURE.md
  - System-level configuration -> devops OPERATIONS.md
  - Only user-facing configuration stays in the USER_GUIDE
- **Error guidance.** For common mistakes, include a "Troubleshooting" callout near the relevant step.
```

**Add regeneration note to Process:**

After step 1 ("Read context"), add a note:

```markdown
   **Template change note:** The USER_GUIDE template was restructured from 4 sections to 7 sections in v1.1. If existing end-user docs use the old structure (Getting Started, Common Tasks, Configuration, Troubleshooting without Overview/Key Concepts/Workflows), they should be deleted and regenerated fresh with the new template. In initial mode, this happens naturally. In update mode, the structurally incompatible old sections won't match new template headings, so treat the entire document as needing full regeneration.
```

**Keep unchanged:** Role, Inputs, Documents, Output Conventions, Principles sections, and the manifest emission logic in step 3.c (existing logic for non-synthesized sections remains the same). The jargon check step (3.d), sections metadata step (3.e), and term proposals step (4) remain unchanged.
  </action>
  <verify>
    <automated>python3 -c "
with open('auto-doc/agents/end-user-writer.md') as f:
    content = f.read()
# Check interface awareness
assert 'user_interfaces' in content, 'Missing user_interfaces consumption'
assert 'interface_style' in content or 'primary_interface' in content, 'Missing interface style logic'
assert 'Power user tip' in content, 'Missing secondary interface callout pattern'
# Check SYNTHESIZED handling
assert 'SYNTHESIZED' in content or 'synthesized_from' in content, 'Missing SYNTHESIZED handling'
assert 'TODO' in content and 'insufficient' in content.lower(), 'Missing TODO placeholder for insufficient data'
# Check BOUNDARY handling
assert 'BOUNDARY' in content, 'Missing BOUNDARY handling'
assert 'cross-reference' in content.lower() or 'OPERATIONS.md' in content, 'Missing cross-reference pattern'
# Check functional-first pattern
assert 'Functional-first' in content or 'functional-first' in content, 'Missing functional-first pattern'
assert 'Goal' in content and 'System behavior' in content, 'Missing functional-first structure elements'
# Check cross-audience boundaries
assert 'devops' in content.lower() and 'OPERATIONS' in content, 'Missing cross-audience boundary rules'
# Check regeneration note
assert 'restructured' in content.lower() or 'regenerat' in content.lower(), 'Missing regeneration note'
print('All writer agent checks passed')
"
    </automated>
  </verify>
  <done>End-user writer agent reads user_interfaces and adapts procedures to primary interface style. Synthesized sections generate from project model fields with TODO fallback. BOUNDARY comments trigger cross-references instead of bounded content. Functional-first pattern (goal, system behavior, steps, tip, results) documented in conventions. Cross-audience boundary rules enforce content separation. Regeneration note covers old->new template transition.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest auto-doc/scripts/tests/ -x --tb=short -q --no-header` -- all existing tests pass
2. end-user-writer.md reads user_interfaces from scan data and sets interface_style
3. end-user-writer.md handles SYNTHESIZED sections (generate from fields, TODO fallback)
4. end-user-writer.md handles BOUNDARY comments (cross-reference, not inline)
5. end-user-writer.md has functional-first pattern in conventions
6. end-user-writer.md has cross-audience boundary enforcement rules
</verification>

<success_criteria>
- Writer agent consumes user_interfaces for interface-aware generation
- Writer handles SYNTHESIZED sections with project model field generation and TODO fallback
- Writer handles BOUNDARY comments with cross-reference callouts
- Functional-first pattern documented and enforced (goal -> behavior -> steps -> tip -> results)
- Cross-audience boundaries enforce content separation
- Old template transition documented
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-auto-doc-enduser-quality/15-03-SUMMARY.md`
</output>
