---
phase: 18-recursive-section-xml-core
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - auto-doc/references/schema.md
autonomous: true
requirements:
  - XML-08
must_haves:
  truths:
    - "Schema.md XML Schema section shows nested <section> elements at 2+ levels of nesting"
    - "Schema.md Section Markers subsection describes markers at every heading level"
    - "Schema.md ref type documentation reflects per-section ref scoping"
  artifacts:
    - path: "auto-doc/references/schema.md"
      provides: "Updated XML schema documentation with recursive nesting examples"
      contains: "section slug.*section slug"
  key_links:
    - from: "auto-doc/references/schema.md"
      to: "auto-doc/scripts/lib/xml_doc.py"
      via: "schema documents the format that xml_doc.py implements"
      pattern: "nested.*section"
---

<objective>
Update the XML Schema section of schema.md to document the recursive nested section model with examples at 2-3 levels of nesting, replacing the current flat single-section example.

Purpose: schema.md is the shared data contract read by all writer agents and pipeline scripts. It must accurately describe the nested `<section>` model so downstream phases (19-21) implement correctly against it.
Output: Updated schema.md with recursive XML examples and updated prose.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-recursive-section-xml-core/18-CONTEXT.md
@auto-doc/references/schema.md

<interfaces>
<!-- The XML Schema section starts at line 659 of schema.md -->
<!-- Current content shows a flat single-section example -->
<!-- Must be replaced with nested multi-level example -->

From auto-doc/references/schema.md (lines 659-764, current XML Schema section):
```
### XML Schema — single flat <section> example with one <refs> block
### Section Markers — describes <!-- section: slug --> pattern
### Nine Ref Types — table of 9 ref types and their XML nesting
### Flat JSON Wire Format — JSON exchange format for refs
```

Decisions from CONTEXT.md:
- One element type (<section>), recursively nested
- Every heading level (## through #####) produces its own <section>
- A section's <body> holds only text between its heading and the first child heading
- A section's <refs> correspond to its <body> only
- <!-- section: slug --> markers at every heading level
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update XML Schema section with nested examples</name>
  <files>auto-doc/references/schema.md</files>
  <action>
    Replace the XML Schema section (starting at line 659 "### XML Schema") with an updated version that shows the recursive nested section model. Keep everything before and after the XML Schema section unchanged.

    The new XML Schema section must include:

    1. **Updated XML example** showing 2-3 levels of nesting. Use the monitoring-alerting domain from the existing example, adding child sections:
       - `<section slug="monitoring-alerting">` (## level) with its own `<refs>` and `<body>` containing only the intro prose
         - `<section slug="etl-run-logging">` (### level) as a child with its own `<refs>` and `<body>`
           - `<section slug="artifact-format">` (#### level) as a grandchild with its own `<refs>` and `<body>`
       - Show that each section's `<body>` contains ONLY the prose between its heading and the next child heading (not child content)
       - Show that each section's `<refs>` declare only entities mentioned in that section's body

    2. **Updated prose** explaining:
       - Sections nest recursively with no depth limit
       - One uniform `<section>` element type at all depths (no `<subsection>`)
       - `<refs>` and `<body>` always come before child `<section>` elements in document order
       - `<!-- section: slug -->` markers appear at every heading level
       - Slugs are unique among siblings, not globally

    3. **Section Markers subsection** updated to note markers at every heading level (not just `##`)

    4. **Nine Ref Types table and Flat JSON Wire Format** -- keep unchanged (these are format-independent)

    5. Add a brief **Path Addressing** note explaining the slash-separated path convention (e.g. `monitoring-alerting/etl-run-logging/artifact-format`) used by `xml_doc.py` functions to address sections at any depth.

    Do NOT modify anything outside the XML Schema / Section Markers / ref types / wire format block (lines 659-763). The rest of schema.md is unchanged.
  </action>
  <verify>
    <automated>grep -c "section slug" auto-doc/references/schema.md</automated>
  </verify>
  <done>schema.md XML Schema section shows nested section example at 3 levels, documents recursive model, path addressing, and per-section ref scoping. Nine Ref Types and Flat JSON Wire Format sections preserved unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify downstream test suite is not broken</name>
  <files></files>
  <action>
    Run the full auto-doc test suite to confirm that schema.md changes (documentation only) have not broken anything, and that the Plan 01 xml_doc.py changes have not broken downstream script tests.

    Run: `python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header`

    If any tests fail in downstream scripts (test_write_section.py, test_verify_xml_refs.py, test_assemble_markdown.py, test_merge_edit_xml.py, test_extract_edit_xml.py, test_prepare_prose_verify.py, test_sync_edits.py), check whether the failure is due to:
    - The `get_section_slugs` rename to `get_section_paths` -- if so, update ONLY the import in the failing test file (the function call with flat sections still works since bare slugs are valid single-segment paths)
    - The `parse_xml_doc` return format gaining `children` key -- if so, tests accessing `doc["sections"]` still work for top-level iteration; only tests that check exact dict equality need `children: []` added

    Fix ONLY import-level breakage. Do NOT rewrite downstream scripts for nested support (that is Phase 20 scope). The goal is green tests, not deep nested support in downstream scripts.
  </action>
  <verify>
    <automated>python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header</automated>
  </verify>
  <done>Full auto-doc test suite passes. Any import-level breakage from the get_section_slugs rename is fixed. Downstream scripts work with flat (top-level only) sections.</done>
</task>

</tasks>

<verification>
python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header
</verification>

<success_criteria>
- schema.md XML Schema section shows 3-level nested section example
- schema.md documents recursive nesting, path addressing, per-section ref scoping
- schema.md Nine Ref Types and Flat JSON Wire Format preserved unchanged
- Full auto-doc test suite passes (including downstream scripts)
</success_criteria>

<output>
After completion, create `.planning/phases/18-recursive-section-xml-core/18-02-SUMMARY.md`
</output>
