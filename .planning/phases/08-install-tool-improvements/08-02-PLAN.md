---
phase: 08-install-tool-improvements
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - install/TEMPLATE-post-install.md
  - codebase-health/tool.toml
  - create-docs/tool.toml
  - data-provider/tool.toml
  - debug-triage/tool.toml
  - new-milestone-gsd/tool.toml
  - update-backlog/tool.toml
  - permission-hooks/tool.toml
  - gsd-patches/tool.toml
  - mg-gsd-wrappers/tool.toml
  - cc-regression-test/tool.toml
  - create-context/tool.toml
autonomous: true
requirements:
  - INST-18
  - INST-19
  - INST-20

must_haves:
  truths:
    - "TEMPLATE-post-install.md exists with status marker pattern mandated"
    - "6 tools have [detect] sections with correct paths in tool.toml"
    - "permission-hooks has standard = false and [detect] in tool.toml"
    - "gsd-patches has standard = false (not exclude = true) and [post_install] in tool.toml"
    - "mg-gsd-wrappers has standard = false in tool.toml"
    - "cc-regression-test has [post_install] and [detect] in tool.toml"
    - "create-context has [detect] in tool.toml"
  artifacts:
    - path: "install/TEMPLATE-post-install.md"
      provides: "Template for post-install.md files with status marker pattern"
      contains: "POST-INSTALL: SUCCESS"
    - path: "codebase-health/tool.toml"
      provides: "Detection paths for adopt"
      contains: "[detect]"
    - path: "gsd-patches/tool.toml"
      provides: "Execute-only tool config"
      contains: "standard = false"
  key_links:
    - from: "install/TEMPLATE-post-install.md"
      to: "*/post-install.md"
      via: "structural template for all post-install files"
      pattern: "POST-INSTALL: SUCCESS|POST-INSTALL: FAILED"
---

<objective>
Create the post-install template and update all 12 tool.toml files: add [detect] sections to 6 tools that need them for adopt detection, change tier settings (standard/exclude) for 4 tools per CONTEXT.md decisions, and add [post_install] sections where applicable.

Purpose: Template must exist before per-tool post-install.md files are written (Plan 03/04). Tool.toml changes are prerequisite metadata for the install.md rewrite (Plan 05).

Output: TEMPLATE-post-install.md + 12 updated tool.toml files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-install-tool-improvements/08-CONTEXT.md
@.planning/phases/08-install-tool-improvements/08-RESEARCH.md

<interfaces>
<!-- tool.toml schema from RESEARCH.md -->
```toml
# Full schema with all sections:
[tool]
description = "Tool description"
standard = true          # default true, set false for optional
exclude = false          # default false, set true for internal-only

[preflight]
required = ["python3"]
optional = ["ruff"]

[post_install]
script = "post-install.md"   # optional, filename relative to tool dir

[detect]
paths = [".claude/tool-name/scripts/"]  # optional, paths relative to target project root
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TEMPLATE-post-install.md</name>
  <files>install/TEMPLATE-post-install.md</files>
  <action>
Create `install/TEMPLATE-post-install.md` following the structure from CONTEXT.md and RESEARCH.md. This is the template that mandates the status marker pattern for all post-install.md files.

Content structure:
```markdown
# [Tool Name] -- Post-Install Configuration

<objective>
[What this post-install step does and why it cannot be done by install.sh alone]
</objective>

<context>
The target project path and source directory path are provided at the top of this prompt.
Use "the target project" and "the source directory" to reference these paths throughout.

- Target project: The project where the tool is being installed
- Source directory: The mg-cc-tools repository root
</context>

<process>
## Step 1: [First step]
[Instructions...]

## Step N: [Last step]
[Instructions...]
</process>

<completion>
## Status

After all steps complete, output exactly ONE of these markers as the final line:

If all steps succeeded:
> POST-INSTALL: SUCCESS

If any step failed:
> POST-INSTALL: FAILED: <brief reason>

You MUST output exactly one of these two markers as the final line of your response.
</completion>
```

The template is a reference document -- actual post-install.md files are created in Plans 03 and 04.
  </action>
  <verify>
    <automated>test -f install/TEMPLATE-post-install.md && grep -c "POST-INSTALL:" install/TEMPLATE-post-install.md</automated>
  </verify>
  <done>TEMPLATE-post-install.md exists with objective, context, process, and completion sections. Contains both SUCCESS and FAILED marker patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 12 tool.toml files with [detect], [post_install], and tier changes</name>
  <files>
    codebase-health/tool.toml
    create-docs/tool.toml
    data-provider/tool.toml
    debug-triage/tool.toml
    new-milestone-gsd/tool.toml
    update-backlog/tool.toml
    permission-hooks/tool.toml
    gsd-patches/tool.toml
    mg-gsd-wrappers/tool.toml
    cc-regression-test/tool.toml
    create-context/tool.toml
  </files>
  <action>
Read each tool.toml, then update per the CONTEXT.md decisions. Changes grouped by type:

**Mechanical [detect] additions (6 tools -- add section only, no other changes):**
- codebase-health/tool.toml: add `[detect]\npaths = [".claude/codebase-health/scripts/"]`
- create-docs/tool.toml: add `[detect]\npaths = [".claude/create-docs/scripts/"]`
- data-provider/tool.toml: add `[detect]\npaths = [".claude/data-provider/scripts/"]`
- debug-triage/tool.toml: add `[detect]\npaths = [".claude/commands/mg/debug-triage.md"]`
- new-milestone-gsd/tool.toml: add `[detect]\npaths = [".claude/commands/mg/new-milestone-gsd.md"]`
- update-backlog/tool.toml: add `[detect]\npaths = [".claude/commands/mg/update-backlog.md"]`

**Tier changes + [detect] and/or [post_install] (5 tools):**
- permission-hooks/tool.toml: add `standard = false`, add `[post_install]\nscript = "post-install.md"`, add `[detect]\npaths = [".claude/permission-hooks/hooks/"]`
- gsd-patches/tool.toml: change `exclude = true` to `standard = false` (remove exclude line), add `[post_install]\nscript = "post-install.md"` (NO [detect] -- execute-only, not adoptable)
- mg-gsd-wrappers/tool.toml: add `standard = false`, add `[detect]\npaths = [".claude/commands/mg/discuss-phase.md"]`
- cc-regression-test/tool.toml: keep `exclude = true`, add `[post_install]\nscript = "post-install.md"`, add `[detect]\npaths = [".claude/cc-regression-test/hooks/"]`
- create-context/tool.toml: add `[detect]\npaths = [".claude/commands/mg/create-context.md"]`

**install/tool.toml: NO changes** -- install tool is excluded and not adoptable.

After all edits, verify scan-status still discovers all 12 tools:
```bash
python3 install/scripts/mg-install-lib.py scan-status --source ./ --target /tmp/dummy-target 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'Tools found: {d[\"summary\"][\"total\"]}')"
```
  </action>
  <verify>
    <automated>python3 -c "
import tomllib, os, sys
tools = {
    'codebase-health': {'detect': True},
    'create-docs': {'detect': True},
    'data-provider': {'detect': True},
    'debug-triage': {'detect': True},
    'new-milestone-gsd': {'detect': True},
    'update-backlog': {'detect': True},
    'permission-hooks': {'detect': True, 'post_install': True, 'standard_false': True},
    'gsd-patches': {'post_install': True, 'standard_false': True, 'no_exclude': True},
    'mg-gsd-wrappers': {'standard_false': True, 'detect': True},
    'cc-regression-test': {'post_install': True, 'detect': True},
    'create-context': {'detect': True},
}
ok = True
for name, checks in tools.items():
    path = f'{name}/tool.toml'
    with open(path, 'rb') as f:
        data = tomllib.load(f)
    if checks.get('detect') and 'detect' not in data:
        print(f'FAIL: {name} missing [detect]'); ok = False
    if checks.get('post_install') and 'post_install' not in data:
        print(f'FAIL: {name} missing [post_install]'); ok = False
    if checks.get('standard_false') and data.get('tool', {}).get('standard', True) != False:
        print(f'FAIL: {name} should have standard = false'); ok = False
    if checks.get('no_exclude') and data.get('tool', {}).get('exclude', False):
        print(f'FAIL: {name} should NOT have exclude = true'); ok = False
if ok: print('ALL CHECKS PASSED')
else: sys.exit(1)
"</automated>
  </verify>
  <done>All 12 tool.toml files updated: 6 with mechanical [detect] additions, 5 with tier/section changes. scan-status discovers all 12 tools. No install.sh modifications in this task.</done>
</task>

</tasks>

<verification>
- `test -f install/TEMPLATE-post-install.md` -- template exists
- Inline Python check verifies all toml fields
- `.venv/bin/python -m pytest install/scripts/tests/test_mg_install_lib.py -x` -- regression
</verification>

<success_criteria>
- TEMPLATE-post-install.md exists with mandated status marker pattern
- All 12 tool.toml files have correct [detect], [post_install], and tier settings per CONTEXT.md
- scan-status still discovers all tools
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-tool-improvements/08-02-SUMMARY.md`
</output>
