---
phase: 08-install-tool-improvements
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - permission-hooks/install.sh
  - permission-hooks/post-install.md
  - cc-regression-test/install.sh
  - cc-regression-test/post-install.md
  - mg-gsd-wrappers/install.sh
  - create-context/install.sh
autonomous: true
requirements:
  - POSTINSTALL-PERMHOOKS
  - POSTINSTALL-CCREGTEST
  - SNAPSHOT-MGWRAPPERS
  - SNAPSHOT-CREATECTX
  - STALE-CLEANUP

must_haves:
  truths:
    - "permission-hooks install.sh no longer copies install-permission-hooks.md to commands/mg/"
    - "permission-hooks install.sh removes stale commands/mg/install-permission-hooks.md from target"
    - "permission-hooks/post-install.md is a self-contained subagent instruction document with status markers"
    - "cc-regression-test install.sh no longer contains inline Python settings.json merge"
    - "cc-regression-test/post-install.md handles settings.json merge as a self-contained subagent"
    - "mg-gsd-wrappers install.sh copies snapshot to .claude/mg-gsd-wrappers/references/ (not commands/mg/)"
    - "create-context install.sh copies snapshot to .claude/create-context/references/ (not commands/mg/)"
  artifacts:
    - path: "permission-hooks/post-install.md"
      provides: "Post-install subagent instructions for settings.json hook registration"
      contains: "POST-INSTALL: SUCCESS"
    - path: "cc-regression-test/post-install.md"
      provides: "Post-install subagent instructions for settings.json hook merge"
      contains: "POST-INSTALL: SUCCESS"
    - path: "permission-hooks/install.sh"
      provides: "Simplified install that copies hooks only, removes stale command"
      min_lines: 50
    - path: "cc-regression-test/install.sh"
      provides: "Simplified install without settings.json merge"
      min_lines: 50
  key_links:
    - from: "permission-hooks/post-install.md"
      to: "target settings.json"
      via: "subagent Python script for hook entry registration"
      pattern: "PreToolUse|permission-guard"
    - from: "cc-regression-test/post-install.md"
      to: "target settings.json"
      via: "subagent Python script for hook entry merge"
      pattern: "PreToolUse|intercept-trigger"
---

<objective>
Implement two-stage install changes for 4 tools: create post-install.md for permission-hooks and cc-regression-test, move .snapshot files for mg-gsd-wrappers and create-context, and simplify their install.sh scripts accordingly.

Purpose: These 4 tools need modifications to support the two-stage install model. permission-hooks and cc-regression-test need post-install.md subagent files for interactive settings.json configuration. mg-gsd-wrappers and create-context need snapshot files moved out of commands/mg/ to stop polluting the Claude Code skill list.

Output: 2 new post-install.md files, 4 modified install.sh files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-install-tool-improvements/08-CONTEXT.md
@.planning/phases/08-install-tool-improvements/08-RESEARCH.md
@install/TEMPLATE-post-install.md
@permission-hooks/commands/install-permission-hooks.md
@permission-hooks/install.sh
@cc-regression-test/install.sh
@mg-gsd-wrappers/install.sh
@create-context/install.sh

<interfaces>
<!-- Post-install.md execution model from CONTEXT.md -->
- Post-install.md runs as a subagent (Agent tool), not inline
- install.md reads post-install.md from source, spawns Agent with prompt prefix:
  "Target project: <TARGET_PATH>\nSource directory: <SOURCE_PATH>\n\n<contents>"
- Post-install.md is self-contained: no frontmatter, no shell variable placeholders
- References paths via natural language from the subagent prompt prefix
- Must end with status marker: POST-INSTALL: SUCCESS or POST-INSTALL: FAILED: <reason>

<!-- Stale file cleanup rules from CONTEXT.md -->
- permission-hooks: install.sh removes commands/mg/install-permission-hooks.md from target
- Snapshot files (.snapshot) in commands/mg/ appear in Claude Code skill list -- must be moved
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: permission-hooks -- create post-install.md and simplify install.sh</name>
  <files>permission-hooks/post-install.md, permission-hooks/install.sh</files>
  <action>
**Create permission-hooks/post-install.md** by rewriting the content of commands/install-permission-hooks.md as a self-contained subagent instruction document:

1. Strip the YAML frontmatter (---name/description/allowed-tools---)
2. Replace `$ARGUMENTS` target resolution with reading the target project path from the subagent prompt prefix ("The target project path is provided at the top of this prompt")
3. Replace `{SOURCE_DIR}` placeholder references with reading the source directory from the prompt prefix. For hook file location, use `<source directory>/permission-hooks/hooks/permission-guard.py`
4. Keep all 5 steps from the original command but adapt path references:
   - Step 1 (Resolve Target): Simplified -- target path comes from prompt prefix, no argument parsing
   - Step 2 (Install/Sync Hook): Use prompt prefix paths instead of {SOURCE_DIR}
   - Step 3 (Settings.json Management): Keep the Python script logic intact, update path references
   - Step 4 (Status Report): Keep as-is with updated paths
   - Step 5 (Smoke Test): Keep as-is with updated paths
5. Add completion section with POST-INSTALL: SUCCESS / POST-INSTALL: FAILED markers as final output
6. Do NOT include any {PLACEHOLDER} variables or $ARGUMENTS -- all path references use natural language from prompt prefix

**Modify permission-hooks/install.sh:**

1. Remove the `install-permission-hooks` entry from the COMMANDS array (empty it or remove the commands copy loop for this specific file)
2. Keep: hook file copy (hooks/permission-guard.py), {PROJECT_ROOT} sed resolution, directory creation, manifest update
3. Add stale file cleanup AFTER install but BEFORE manifest update:
   ```bash
   # Clean up stale command from v1.0
   STALE_CMD="${COMMANDS_DIR}/install-permission-hooks.md"
   if [[ -f "$STALE_CMD" ]]; then
     rm "$STALE_CMD"
     echo "  Removed stale: install-permission-hooks.md"
   fi
   ```
4. Remove the sed resolution for {HOOKS_DIR} and {SOURCE_DIR} in command files (no command files are being copied anymore)
5. Keep the {PROJECT_ROOT} sed resolution on the hook file itself

Do NOT delete commands/install-permission-hooks.md from source -- it stays as reference. Only install.sh stops copying it.
  </action>
  <verify>
    <automated>grep -c "POST-INSTALL:" permission-hooks/post-install.md && ! grep -q '{SOURCE_DIR}\|{HOOKS_DIR}\|\$ARGUMENTS' permission-hooks/post-install.md && echo "post-install.md OK" && grep -q "install-permission-hooks" permission-hooks/install.sh && echo "FAIL: install.sh still copies command" || echo "install.sh OK"</automated>
  </verify>
  <done>permission-hooks/post-install.md exists as self-contained subagent doc with status markers, no placeholders, no $ARGUMENTS. install.sh no longer copies install-permission-hooks.md, adds stale file cleanup, still copies hook file with PROJECT_ROOT resolution.</done>
</task>

<task type="auto">
  <name>Task 2: cc-regression-test -- create post-install.md and simplify install.sh</name>
  <files>cc-regression-test/post-install.md, cc-regression-test/install.sh</files>
  <action>
**Create cc-regression-test/post-install.md** as a self-contained subagent instruction document for the settings.json hook entry merge:

1. Follow TEMPLATE-post-install.md structure
2. Objective: Merge the PreToolUse hook entry for intercept-trigger.py into the target project's settings.json
3. Context: Target project path and source directory from prompt prefix
4. Process steps:
   - Step 1: Determine paths. Hook command = `python3 <target project>/.claude/cc-regression-test/hooks/intercept-trigger.py`. Settings file = `<target project>/.claude/settings.json`.
   - Step 2: Read existing settings.json (handle: file doesn't exist, hooks key doesn't exist, PreToolUse array doesn't exist)
   - Step 3: Check if entry already present (idempotent -- matcher "Bash" with matching hook command)
   - Step 4: If not present, append the entry. Write back with json.dump indent=2
   - Step 5: Report what was done
5. Completion section with POST-INSTALL: SUCCESS / POST-INSTALL: FAILED markers
6. No {PLACEHOLDER} variables -- path references use natural language

**Simplify cc-regression-test/install.sh:**

1. Remove the entire `python3 - "$SETTINGS_FILE" "$HOOK_CMD" <<'PYEOF'` ... `PYEOF` block (lines 158-198)
2. Remove the SETTINGS_FILE and HOOK_CMD variable assignments (lines 153-154)
3. Remove the "Merging hook config" echo line (line 156)
4. Keep everything else: commands copy, hooks/scripts copy, sed placeholder resolution, manifest update
  </action>
  <verify>
    <automated>grep -c "POST-INSTALL:" cc-regression-test/post-install.md && ! grep -q 'PYEOF' cc-regression-test/install.sh && echo "inline Python removed from install.sh" && grep -q 'update-manifest' cc-regression-test/install.sh && echo "manifest update preserved"</automated>
  </verify>
  <done>cc-regression-test/post-install.md handles settings.json merge as self-contained subagent. install.sh simplified: no inline Python merge, still handles file copies, sed resolution, and manifest update.</done>
</task>

<task type="auto">
  <name>Task 3: mg-gsd-wrappers and create-context -- move .snapshot files to references/</name>
  <files>mg-gsd-wrappers/install.sh, create-context/install.sh</files>
  <action>
**mg-gsd-wrappers/install.sh:**

1. Change snapshot copy destination from `${COMMANDS_DIR}/${SNAPSHOT_FILE}` to `${TARGET_DIR}/mg-gsd-wrappers/references/${SNAPSHOT_FILE}`
2. Create the new directory: `mkdir -p "${TARGET_DIR}/mg-gsd-wrappers/references"`
3. Update SNAPSHOT_ABSOLUTE to point to the new location
4. Update the sed resolution for {METHODOLOGY_SNAPSHOT} to use the new path
5. Add stale file cleanup: if the snapshot exists in the old location (commands/mg/), remove it:
   ```bash
   STALE_SNAPSHOT="${COMMANDS_DIR}/${SNAPSHOT_FILE}"
   if [[ -f "$STALE_SNAPSHOT" ]]; then
     rm "$STALE_SNAPSHOT"
     echo "  Removed stale: ${SNAPSHOT_FILE} from commands/mg/"
   fi
   ```

**create-context/install.sh:**

1. Change snapshot copy destination from `${COMMANDS_DIR}/${SNAPSHOT_FILE}` to `${TARGET_DIR}/create-context/references/${SNAPSHOT_FILE}`
2. Create the new directory: `mkdir -p "${TARGET_DIR}/create-context/references"`
3. Update SNAPSHOT_ABSOLUTE to point to the new location
4. Update the {TEMPLATE_SNAPSHOT} sed resolution to use the new path
5. Add stale file cleanup: if the snapshot exists in the old location (commands/mg/), remove it:
   ```bash
   STALE_SNAPSHOT="${COMMANDS_DIR}/${SNAPSHOT_FILE}"
   if [[ -f "$STALE_SNAPSHOT" ]]; then
     rm "$STALE_SNAPSHOT"
     echo "  Removed stale: ${SNAPSHOT_FILE} from commands/mg/"
   fi
   ```
  </action>
  <verify>
    <automated>grep -q 'mg-gsd-wrappers/references' mg-gsd-wrappers/install.sh && echo "mg-gsd-wrappers: references path OK" && grep -q 'create-context/references' create-context/install.sh && echo "create-context: references path OK" && ! grep -q '"${COMMANDS_DIR}/${SNAPSHOT_FILE}"' mg-gsd-wrappers/install.sh && echo "mg-gsd-wrappers: old path removed" && ! grep -q '"${COMMANDS_DIR}/${SNAPSHOT_FILE}"' create-context/install.sh && echo "create-context: old path removed"</automated>
  </verify>
  <done>Both tools copy .snapshot files to tool-specific references/ directories instead of commands/mg/. Stale file cleanup removes old copies. Sed resolution updated to new paths. Snapshots no longer appear in Claude Code skill list.</done>
</task>

</tasks>

<verification>
- `grep -c "POST-INSTALL:" permission-hooks/post-install.md cc-regression-test/post-install.md` -- both have markers
- `grep -rq '{SOURCE_DIR}\|{HOOKS_DIR}\|{PATCHES_DIR}' permission-hooks/post-install.md cc-regression-test/post-install.md` -- no placeholders (should return non-zero)
- `.venv/bin/python -m pytest install/scripts/tests/test_mg_install_lib.py -x` -- regression
</verification>

<success_criteria>
- permission-hooks/post-install.md: self-contained, status markers, no placeholders
- permission-hooks/install.sh: no command copy, stale cleanup, hook file copy preserved
- cc-regression-test/post-install.md: settings.json merge as subagent doc
- cc-regression-test/install.sh: no inline Python, file copies and manifest preserved
- mg-gsd-wrappers/install.sh: snapshot in mg-gsd-wrappers/references/, stale cleanup
- create-context/install.sh: snapshot in create-context/references/, stale cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-tool-improvements/08-03-SUMMARY.md`
</output>
