---
phase: 12-auto-doc-rename-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auto-doc/commands/auto-doc.md
  - auto-doc/commands/auto-doc-scan.md
  - auto-doc/commands/auto-doc-generate.md
  - auto-doc/commands/auto-doc-verify.md
  - auto-doc/commands/auto-doc-add.md
  - auto-doc/agents/*.md
  - auto-doc/scripts/*.py
  - auto-doc/references/schema.md
  - auto-doc/references/style-guide.md
  - auto-doc/install.sh
  - auto-doc/tool.toml
  - install/scripts/mg-install-lib.py
  - install/commands/install.md
  - .claude/commands/mg/install.md
  - permission-hooks/hooks/tests/test_permission_guard.py
  - CLAUDE.md
autonomous: true
requirements:
  - REN-01
  - REN-02
  - REN-03
  - REN-04
  - REN-05
  - REN-06
  - REN-07

must_haves:
  truths:
    - "All 5 auto-doc commands are accessible under auto-doc/ with correct filenames"
    - "No create-docs or add-docs references exist outside .planning/ and docs/work-queue/"
    - "Orphaned staleness-scanner.md is deleted"
    - "install.sh migrates old create-docs installations before deploying auto-doc"
    - "mg-install-lib.py workspace mapping uses auto-doc key"
    - "CLAUDE.md references auto-doc (not create-docs) as the documentation tool"
  artifacts:
    - path: "auto-doc/commands/auto-doc.md"
      provides: "Renamed router command"
      contains: "name: mg:auto-doc"
    - path: "auto-doc/commands/auto-doc-add.md"
      provides: "Renamed add command (non-trivial rename)"
      contains: "name: mg:auto-doc-add"
    - path: "auto-doc/install.sh"
      provides: "Install script with migration path"
      contains: "auto-doc"
    - path: "auto-doc/tool.toml"
      provides: "Tool metadata with updated paths"
      contains: "auto-doc"
  key_links:
    - from: "auto-doc/install.sh"
      to: "mg-install-lib.py"
      via: "SUPPORT_DIR and COMMANDS array reference auto-doc"
      pattern: "auto-doc"
    - from: "install/scripts/mg-install-lib.py"
      to: "auto-doc workspace"
      via: "WORKSPACE_DIRS mapping"
      pattern: "auto-doc.*\\.mg/docs"
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: "/mg:auto-doc-add"
      via: "HTML comment template strings"
      pattern: "auto-doc-add"
---

<objective>
Rename the entire create-docs tool family to auto-doc and clean up the orphaned staleness-scanner agent.

Purpose: This clears the namespace for all subsequent v1.1 work (phases 13-16) and establishes the auto-doc brand. The rename is a clean break -- users reinstall via /mg:install.

Output: auto-doc/ directory with all commands, agents, scripts, references, install.sh, and tool.toml correctly named and internally consistent. Cross-tool references updated. Migration path in install.sh. CLAUDE.md updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-auto-doc-rename-and-cleanup/12-CONTEXT.md
@.planning/phases/12-auto-doc-rename-and-cleanup/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Directory move, file renames, delete orphan, and bulk substitution</name>
  <files>
    auto-doc/ (entire directory, renamed from create-docs/)
  </files>
  <action>
Execute the rename in this exact order:

1. **Directory rename:** `git mv create-docs auto-doc`

2. **Command file renames** (inside auto-doc/commands/):
   - `git mv auto-doc/commands/create-docs.md auto-doc/commands/auto-doc.md`
   - `git mv auto-doc/commands/create-docs-scan.md auto-doc/commands/auto-doc-scan.md`
   - `git mv auto-doc/commands/create-docs-generate.md auto-doc/commands/auto-doc-generate.md`
   - `git mv auto-doc/commands/create-docs-verify.md auto-doc/commands/auto-doc-verify.md`
   - `git mv auto-doc/commands/add-docs.md auto-doc/commands/auto-doc-add.md`

3. **Delete orphaned agent:** `git rm auto-doc/agents/staleness-scanner.md`
   - Never wired into any command; scan pipeline calls staleness-check.py directly
   - Agent references `--doc-file` parameters that do not exist (scripts use `--docs-dir`)

4. **Delete __pycache__/** directories under auto-doc/ if any exist (stale bytecode after rename)

5. **Explicit add-docs substitution FIRST** on files under auto-doc/ that reference add-docs:
   Run `sed -i 's/add-docs/auto-doc-add/g'` on these specific files:
   - `auto-doc/commands/auto-doc-generate.md` (HTML comment templates at ~lines 378, 467, 473 and any other add-docs references)
   - `auto-doc/commands/auto-doc.md` (router references to add-docs command)
   - `auto-doc/commands/auto-doc-add.md` (self-references including YAML frontmatter `name: mg:add-docs` -> `name: mg:auto-doc-add`)
   Note: `add-docs` is NOT a substring of `create-docs`, so these two sed passes are independent and order does not matter. But doing add-docs first is cleaner.

6. **Bulk substitution** on ALL files under auto-doc/ (commands, agents, scripts, references, install.sh, tool.toml):
   Run `sed -i 's/create-docs/auto-doc/g'` on every file under auto-doc/
   This handles:
   - Command name references (`/mg:create-docs` -> `/mg:auto-doc`)
   - Directory path references (`.claude/create-docs/` -> `.claude/auto-doc/`)
   - YAML frontmatter name fields
   - Docstrings and comments
   - install.sh SUPPORT_DIR, COMMANDS array, echo statements

7. **Migration path in install.sh:** Add a migration section to `auto-doc/install.sh` BEFORE the normal install flow (after variable declarations, before the mkdir/copy block). The migration should:
   - Check if old installation exists: `[[ -f "${COMMANDS_DIR}/create-docs.md" ]] || [[ -d "${TARGET_DIR}/create-docs" ]]`
   - If found: remove old command files (`create-docs*.md` and `add-docs.md` from COMMANDS_DIR), remove old support directory (`rm -rf "${TARGET_DIR}/create-docs"`)
   - Clean the manifest: use inline `python3 -c` to read the manifest JSON at the target, pop the `create-docs` key from `tools`, write back. The manifest path is `${TARGET_DIR}/mg-cc-tools.manifest.json` (TARGET_DIR is the `.claude` directory). Wrap in `2>/dev/null || echo "Warning: could not clean manifest (non-fatal)"` per CONTEXT.md discretion guidance

8. **Verify install.sh** is internally consistent: SUPPORT_DIR should be `auto-doc`, COMMANDS array should list auto-doc command names, TARGET_DIR should resolve to `.claude/auto-doc`.
  </action>
  <verify>
    <automated>python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header && find auto-doc/ -name '__pycache__' -type d | head -1 && echo "FAIL: __pycache__ still exists" || echo "OK: no __pycache__" ; test -f auto-doc/commands/auto-doc.md && test -f auto-doc/commands/auto-doc-add.md && test ! -f auto-doc/agents/staleness-scanner.md && echo "PASS: files correctly renamed and orphan deleted" || echo "FAIL: file rename issue"</automated>
  </verify>
  <done>
    - auto-doc/ directory exists with all 5 command files renamed
    - staleness-scanner.md is deleted
    - All files under auto-doc/ reference auto-doc (not create-docs) and auto-doc-add (not add-docs)
    - install.sh has migration path for old create-docs installations
    - No __pycache__ directories under auto-doc/
    - All 105 existing tests pass after rename (sed did not break Python imports)
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-tool reference updates, CLAUDE.md update, and verification grep</name>
  <files>
    install/scripts/mg-install-lib.py
    install/commands/install.md
    .claude/commands/mg/install.md
    permission-hooks/hooks/tests/test_permission_guard.py
    CLAUDE.md
  </files>
  <action>
1. **mg-install-lib.py** (line 62): Change workspace mapping key from `"create-docs"` to `"auto-doc"`:
   `"auto-doc": ".mg/docs"` (the value stays the same -- .mg/docs is the project data directory, not the tool namespace)

2. **install/commands/install.md** (~line 108): Update tool listing from `create-docs` to `auto-doc`. Use sed: `sed -i 's/create-docs/auto-doc/g' install/commands/install.md`

3. **.claude/commands/mg/install.md** (~line 108): Same update as above -- this is the installed copy in this repo. `sed -i 's/create-docs/auto-doc/g' .claude/commands/mg/install.md`

4. **permission-hooks/hooks/tests/test_permission_guard.py** (lines ~999, 1001, 1033): Update `/mg:add-docs` references in heredoc test data to `/mg:auto-doc-add`. Use sed: `sed -i 's/add-docs/auto-doc-add/g' permission-hooks/hooks/tests/test_permission_guard.py`. Also check for any `create-docs` references and update those too: `sed -i 's/create-docs/auto-doc/g' permission-hooks/hooks/tests/test_permission_guard.py`

5. **CLAUDE.md** (per locked decision: "CLAUDE.md in this repo must be updated"): Update the Architecture section to mention `auto-doc` alongside `codebase-health` as the complex tools. Currently CLAUDE.md only references `codebase-health` as the complex tool example. Add `auto-doc` to the "Complex tools" parenthetical so it reads: "**Complex tools** (codebase-health, auto-doc) have agents, Python scripts, and a shared schema." Also add a brief "Auto-doc pipeline" subsection after the "Codebase health pipeline" subsection describing the auto-doc 3-step pipeline (scan, generate, verify) for documentation lifecycle management.

6. **Verification grep** -- search the ENTIRE repo for remaining references, excluding historical directories:
   ```
   grep -r "create-docs\|add-docs" --exclude-dir=.planning --exclude-dir=docs --exclude-dir=.git --exclude-dir=__pycache__ --exclude-dir=node_modules .
   ```
   - If any hits remain, fix them
   - `.planning/` and `docs/work-queue/` are excluded (historical records per CONTEXT.md)

7. **Run the full test suite** to confirm the rename did not break anything:
   `python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header`
   All 105 tests should still pass (the rename is mechanical and does not change logic).
  </action>
  <verify>
    <automated>grep -r "create-docs" --exclude-dir=.planning --exclude-dir=docs --exclude-dir=.git --exclude-dir=__pycache__ . ; EXIT=$? ; if [ $EXIT -eq 0 ]; then echo "FAIL: create-docs references remain"; else echo "PASS: no create-docs references outside exclusions"; fi ; grep -r "add-docs" --exclude-dir=.planning --exclude-dir=docs --exclude-dir=.git --exclude-dir=__pycache__ . ; EXIT=$? ; if [ $EXIT -eq 0 ]; then echo "FAIL: add-docs references remain"; else echo "PASS: no add-docs references outside exclusions"; fi</automated>
  </verify>
  <done>
    - mg-install-lib.py workspace mapping uses "auto-doc" key
    - Both install.md copies reference auto-doc (not create-docs)
    - permission-hooks test data references /mg:auto-doc-add (not /mg:add-docs)
    - CLAUDE.md mentions auto-doc as a complex tool alongside codebase-health
    - Verification grep returns zero hits outside .planning/ and docs/ exclusions
    - All 105 existing tests pass after rename
  </done>
</task>

</tasks>

<verification>
1. `test -d auto-doc && test ! -d create-docs` -- directory rename complete
2. `test -f auto-doc/commands/auto-doc-add.md` -- non-trivial rename handled
3. `test ! -f auto-doc/agents/staleness-scanner.md` -- orphan deleted
4. `grep -c "create-docs" auto-doc/install.sh` returns 0 (only in migration path comments is acceptable if any)
5. `grep "auto-doc" install/scripts/mg-install-lib.py` confirms workspace mapping
6. `grep "auto-doc" CLAUDE.md` confirms CLAUDE.md updated
7. `python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header` -- 105 tests pass
8. Full verification grep clean (no create-docs or add-docs outside exclusions)
</verification>

<success_criteria>
- auto-doc/ directory exists with correct structure (5 commands, 8 agents, scripts, references)
- create-docs/ directory no longer exists
- staleness-scanner.md deleted
- All internal references updated to auto-doc namespace
- install.sh contains migration path for old create-docs installations
- Cross-tool references updated in mg-install-lib.py, install.md (both copies), permission-hooks tests
- CLAUDE.md updated to reference auto-doc as a complex tool
- Verification grep clean outside .planning/ and docs/ exclusions
- All 105 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-doc-rename-and-cleanup/12-01-SUMMARY.md`
</output>
