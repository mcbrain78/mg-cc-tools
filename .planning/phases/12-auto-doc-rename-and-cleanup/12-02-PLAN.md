---
phase: 12-auto-doc-rename-and-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - auto-doc/scripts/staleness-check.py
  - auto-doc/scripts/tests/test_staleness_check.py
  - auto-doc/scripts/add-note.py
  - auto-doc/scripts/tests/test_add_note.py
  - auto-doc/scripts/classify-note.py
  - auto-doc/scripts/tests/test_classify_note.py
  - auto-doc/commands/auto-doc-scan.md
autonomous: true
requirements:
  - FIX-C1
  - FIX-C2
  - FIX-C3
  - FIX-C4
  - FIX-C5

must_haves:
  truths:
    - "staleness-check.py CLI output is a dict with staleness_report key consumable by merge-scan.py"
    - "add-note.py and classify-note.py use note_id field consistently matching the schema"
    - "add-note.py prints JSON with note_id to stdout for command file consumption"
    - "Scan pipeline no longer calls check-references.py (verify-only concern)"
    - "All tests pass with updated assertions reflecting the fixed behavior"
  artifacts:
    - path: "auto-doc/scripts/staleness-check.py"
      provides: "Fixed CLI output wrapper for merge-scan.py compatibility"
      contains: "staleness_report"
    - path: "auto-doc/scripts/add-note.py"
      provides: "Fixed note_id field and JSON stdout output"
      contains: "note_id"
    - path: "auto-doc/scripts/classify-note.py"
      provides: "Fixed note_id field reading"
      contains: "note_id"
    - path: "auto-doc/commands/auto-doc-scan.md"
      provides: "Scan command without dead check-references.py call"
  key_links:
    - from: "auto-doc/scripts/staleness-check.py"
      to: "auto-doc/scripts/merge-scan.py"
      via: "CLI output dict with staleness_report key"
      pattern: "staleness_report"
    - from: "auto-doc/scripts/add-note.py"
      to: "auto-doc/commands/auto-doc-add.md"
      via: "JSON stdout with note_id"
      pattern: "json\\.dumps.*note_id"
    - from: "auto-doc/scripts/add-note.py"
      to: "auto-doc/references/schema.md"
      via: "note_id field matching schema contract"
      pattern: "note_id"
---

<objective>
Fix latent bugs in the already-renamed auto-doc scripts and commands.

Purpose: These bugs were discovered during v1.0 analysis. The staleness-check output format prevents merge-scan.py from consuming staleness data. The note_id vs id mismatch breaks the add-note-to-generate round-trip. The dead check-references.py call in the scan pipeline produces output that merge-scan.py silently discards. Fixing these now -- after the rename -- prevents bulk sed from mangling the fix text.

Output: Four bug fixes with updated tests, all 105+ tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-auto-doc-rename-and-cleanup/12-CONTEXT.md
@.planning/phases/12-auto-doc-rename-and-cleanup/12-RESEARCH.md
@.planning/phases/12-auto-doc-rename-and-cleanup/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix staleness-check.py output format and remove dead scan call</name>
  <files>
    auto-doc/scripts/staleness-check.py
    auto-doc/scripts/tests/test_staleness_check.py
    auto-doc/commands/auto-doc-scan.md
  </files>
  <action>
**Run existing tests first as baseline:**
`python3 -m pytest auto-doc/scripts/tests/test_staleness_check.py --tb=short -q --no-header`

**Fix 1: staleness-check.py CLI output wrapper**

In `auto-doc/scripts/staleness-check.py`, find the CLI output section (around line 275 after rename -- verify exact location). Currently:
```python
save_json(args.output, entries)
```

Change to:
```python
save_json(args.output, {"staleness_report": entries})
```

Also find the stdout output path (around line 277) where it does `json.dump(entries, sys.stdout, ...)`. Wrap this consistently:
```python
json.dump({"staleness_report": entries}, sys.stdout, indent=2)
```

The internal `check_staleness()` function MUST continue returning a bare list -- only the CLI output paths change. This is the dual-output pattern: function returns raw data, CLI wraps for consumers.

**Fix 2: Update staleness-check tests**

In `auto-doc/scripts/tests/test_staleness_check.py`:

- `TestCLI::test_cli_json_output` (around line 334): Currently asserts `isinstance(data, list)`. Change to:
  ```python
  data = json.loads(result.stdout)
  assert isinstance(data, dict)
  assert "staleness_report" in data
  assert isinstance(data["staleness_report"], list)
  ```

- `TestCLI::test_cli_output_file` (around line 353): Currently asserts `isinstance(data, list)`. Change to:
  ```python
  data = json.loads(output_file.read_text())
  assert isinstance(data, dict)
  assert "staleness_report" in data
  assert isinstance(data["staleness_report"], list)
  ```

- Function-level tests (`TestStalenessDetection`, `TestSeverity`, `TestOutputFormat`) remain UNCHANGED -- they test the internal API which still returns bare lists.

**Fix 3: Remove dead check-references.py call from scan pipeline**

In `auto-doc/commands/auto-doc-scan.md`, find Step 3 (Staleness Check section). Remove the `check-references.py --docs-dir` invocation (was around lines 173-179 in pre-rename file -- verify exact location after rename). Also remove the mention of check-references.py around line 290 (the pipeline summary or instructions section).

Why: check-references.py output is a flat JSON array that fails merge-scan.py's `isinstance(data, dict)` check. merge-scan.py has no consumer key for reference data. Reference checking is a verify concern (auto-doc-verify.md still uses it), not a scan concern.

Do NOT modify check-references.py itself -- it is kept alive for the verify pipeline.

**Run tests after fixes:**
`python3 -m pytest auto-doc/scripts/tests/test_staleness_check.py --tb=short -q --no-header`
  </action>
  <verify>
    <automated>python3 -m pytest auto-doc/scripts/tests/test_staleness_check.py --tb=short -q --no-header</automated>
  </verify>
  <done>
    - staleness-check.py CLI output (both --output file and stdout) wraps entries in {"staleness_report": [...]}
    - staleness-check.py internal check_staleness() function still returns bare list
    - test_cli_json_output asserts dict with staleness_report key
    - test_cli_output_file asserts dict with staleness_report key
    - Function-level tests unchanged and passing
    - auto-doc-scan.md no longer invokes check-references.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix note_id field mismatch and add-note.py stdout JSON</name>
  <files>
    auto-doc/scripts/add-note.py
    auto-doc/scripts/tests/test_add_note.py
    auto-doc/scripts/classify-note.py
    auto-doc/scripts/tests/test_classify_note.py
  </files>
  <action>
**Run existing tests first as baseline:**
`python3 -m pytest auto-doc/scripts/tests/test_add_note.py auto-doc/scripts/tests/test_classify_note.py --tb=short -q --no-header`

**Fix 1: add-note.py note_id field**

In `auto-doc/scripts/add-note.py`:
- Line 37 (or wherever `note.get("id", "")` appears): Change `"id"` to `"note_id"`
- Line 78 (or wherever the note dict is constructed with `"id": note_id`): Change key from `"id"` to `"note_id"`

**Fix 2: add-note.py JSON stdout output**

In `auto-doc/scripts/add-note.py`:
- Add `import json` to the imports at the top of the file (if not already present)
- Before the stderr confirmation line (around line 96), add:
  ```python
  print(json.dumps({"note_id": note_id}))
  ```
- The existing stderr line (`print(f"Added note {note_id}: ...", file=sys.stderr)`) stays as-is

This fulfills the command file's expectation (auto-doc-add.md line 66) that stdout contains JSON with note_id.

**Fix 3: classify-note.py note_id field**

In `auto-doc/scripts/classify-note.py`:
- Line 153 (or wherever `note["id"]` is read): Change to `note["note_id"]`
- Search for any other occurrences of `["id"]` or `"id"` that refer to note IDs and update them

**Fix 4: Update test assertions**

In `auto-doc/scripts/tests/test_add_note.py`:
- Find all assertions that reference the `"id"` key in note dicts and change to `"note_id"`
- Add assertion for stdout JSON output: in the test that creates a note (e.g., `test_append_to_empty_inbox` or equivalent), capture stdout and assert it contains valid JSON with a `"note_id"` key matching the expected note ID

In `auto-doc/scripts/tests/test_classify_note.py`:
- Find all test data setup that uses `"id"` key for notes and change to `"note_id"`
- Find all assertions referencing `"id"` and change to `"note_id"`

**Run all tests after fixes:**
`python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header`

This should pass all tests (105 original + any new assertions). The note_id change is a breaking change for existing notes-inbox.json with "id" fields, but this is accepted per CONTEXT.md because the feature has never completed a full add-to-generate round-trip.
  </action>
  <verify>
    <automated>python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header</automated>
  </verify>
  <done>
    - add-note.py uses "note_id" (not "id") when constructing and reading notes
    - add-note.py prints JSON {"note_id": "NOTE-XXX"} to stdout
    - classify-note.py reads "note_id" (not "id") from inbox notes
    - All test assertions updated to use "note_id"
    - At least one test asserts stdout JSON from add-note.py
    - Full test suite passes (105+ tests)
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header` -- all tests pass
2. `python3 -m pytest --tb=short -q --no-header` -- full project suite passes
3. `grep -n '"id"' auto-doc/scripts/add-note.py` -- no note "id" fields remain (only "note_id")
4. `grep -n '"id"' auto-doc/scripts/classify-note.py` -- no note "id" fields remain
5. `grep -n "staleness_report" auto-doc/scripts/staleness-check.py` -- confirms dict wrapper present
6. `grep -n "check-references" auto-doc/commands/auto-doc-scan.md` -- no invocations remain
7. `grep -n "check-references" auto-doc/commands/auto-doc-verify.md` -- verify pipeline still uses it (should find matches)
</verification>

<success_criteria>
- staleness-check.py CLI output produces {"staleness_report": [...]} consumable by merge-scan.py
- add-note.py and classify-note.py use "note_id" field matching the schema contract
- add-note.py outputs JSON to stdout for command file consumption
- Scan pipeline no longer calls check-references.py (kept for verify pipeline)
- All existing tests pass with updated assertions
- Full project test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-doc-rename-and-cleanup/12-02-SUMMARY.md`
</output>
