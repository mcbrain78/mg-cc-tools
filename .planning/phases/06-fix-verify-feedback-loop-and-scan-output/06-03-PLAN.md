---
phase: 06-fix-verify-feedback-loop-and-scan-output
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - create-docs/commands/create-docs-verify.md
  - create-docs/commands/create-docs.md
  - create-docs/commands/create-docs-scan.md
autonomous: true
requirements: [FIX-A4, FIX-A5, FIX-B3]

must_haves:
  truths:
    - "create-docs-verify.md has NO Task prompt OVERRIDE or ADDITIONAL blocks — all behavior baked into verifier.md"
    - "create-docs-verify.md clears docs-verify-findings.json before spawning verifier agent"
    - "create-docs-verify.md passes findings_file and scripts_dir paths to verifier agent"
    - "create-docs-verify.md Step 4 guidance references re-running generate (not re-scanning)"
    - "create-docs.md router detects docs-verify-findings.json and routes to re-run generate when findings are non-empty"
    - "create-docs-scan.md passes {SCRIPTS_DIR} to scan-audience agents so they can call write-scan-output.py"
  artifacts:
    - path: "create-docs/commands/create-docs-verify.md"
      provides: "Simplified verify command with no Task prompt overrides"
      contains: "docs-verify-findings.json"
    - path: "create-docs/commands/create-docs.md"
      provides: "Router with findings-aware state detection"
      contains: "docs-verify-findings.json"
    - path: "create-docs/commands/create-docs-scan.md"
      provides: "Scan command passing script path to audience agents"
      contains: "write-scan-output.py"
  key_links:
    - from: "create-docs/commands/create-docs-verify.md"
      to: "create-docs/agents/verifier.md"
      via: "Task prompt pastes agent contents with parameters"
      pattern: "agents/verifier\\.md"
    - from: "create-docs/commands/create-docs-verify.md"
      to: "create-docs/scripts/add-verify-finding.py"
      via: "verifier agent calls script (path passed as parameter)"
      pattern: "findings_file"
    - from: "create-docs/commands/create-docs.md"
      to: ".mg/docs/docs-verify-findings.json"
      via: "router reads findings file directly"
      pattern: "docs-verify-findings\\.json"
    - from: "create-docs/commands/create-docs-scan.md"
      to: "create-docs/agents/scan-audience.md"
      via: "Task prompt includes scripts_dir parameter"
      pattern: "SCRIPTS_DIR"
---

<objective>
Simplify create-docs-verify.md (remove all Task prompt overrides, add findings lifecycle), add findings-aware state to create-docs.md router, and update create-docs-scan.md to pass script paths to scan agents.

Purpose: Complete the wiring between the new scripts/agents (Plans 01-02) and the command layer. Verify command becomes a thin parameter-passer, router closes the feedback loop by detecting findings, scan command enables validated output.

Output: 3 updated command files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-verify-feedback-loop-and-scan-output/06-CONTEXT.md
@.planning/phases/06-fix-verify-feedback-loop-and-scan-output/06-RESEARCH.md

@.planning/phases/06-fix-verify-feedback-loop-and-scan-output/06-01-SUMMARY.md
@.planning/phases/06-fix-verify-feedback-loop-and-scan-output/06-02-SUMMARY.md

@create-docs/commands/create-docs-verify.md
@create-docs/commands/create-docs.md
@create-docs/commands/create-docs-scan.md

<interfaces>
<!-- From Plan 02: verifier.md now accepts these input parameters -->
Verifier agent inputs (from rewritten verifier.md):
- project_root, docs_dir, scan_data_path, glossary_path, style_guide_path
- output_report_path
- verify_refs_path (pre-extracted references)
- findings_file (path to docs-verify-findings.json)
- scripts_dir is received via {SCRIPTS_DIR} placeholder in agent definition (sed-resolved at install time)

<!-- From Plan 02: scan-audience.md now uses write-scan-output.py -->
Scan-audience agent now needs {SCRIPTS_DIR} path to call write-scan-output.py.
The agent definition uses {SCRIPTS_DIR} placeholder (sed-resolved), but the Task prompt
should also include scripts_dir parameter for consistency.

<!-- From Plan 01: Script interfaces -->
add-verify-finding.py: --input <temp_file> --findings-file <path>
list-verify-findings.py: --findings-file <path> --summary --output <path>
                          --findings-file <path> --document X --audience Y --output <path>
write-scan-output.py: --input <temp_file> --output <scan-logs/path> --audience <name>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify create-docs-verify.md and add findings lifecycle</name>
  <files>create-docs/commands/create-docs-verify.md</files>
  <action>
    Modify create-docs-verify.md with these specific changes:

    **Step 1 (Load Context) — add findings path:**
    - Add to "Build runtime paths" (step 1.3):
      `findings_file` = `{project_root}/.mg/docs/docs-verify-findings.json`

    **Add new Step 1.5: Clear prior findings:**
    After ensuring scan-logs directory exists (step 1.4), add:
    ```
    Clear findings from prior verify runs to start fresh:
    ```bash
    rm -f {project_root}/.mg/docs/docs-verify-findings.json
    ```
    This ensures each verify run produces findings reflecting the current documentation state. Generate reads findings but never clears them — only verify clears (per CONTEXT.md "Finding lifecycle" decision).

    **Step 3 (Spawn Verifier Agent) — remove ALL override blocks, add findings_file:**
    Replace the current Task prompt with a simplified version that has NO OVERRIDE or ADDITIONAL blocks. The rewritten verifier.md (Plan 02) has LSP and glossary reconciliation baked in.

    New Task prompt:
    ```
    Task(
      description="Verify documentation quality (6 checks)",
      prompt="You are the documentation verifier agent.

    [paste full contents of agents/verifier.md here]

    Parameters:
    - project_root: {project_root}
    - docs_dir: {docs_dir_abs}
    - scan_data_path: {project_root}/.mg/docs/docs-scan.json
    - glossary_path: {docs_dir_abs}/GLOSSARY.md
    - style_guide_path: references/style-guide.md
    - output_report_path: {project_root}/.mg/docs/docs-verify-report.md
    - verify_refs_path: {project_root}/.mg/docs/scan-logs/verify-refs.json
    - findings_file: {project_root}/.mg/docs/docs-verify-findings.json"
    )
    ```

    That's it — no OVERRIDE block for Check 1, no ADDITIONAL block for Check 2. The agent definition handles everything.

    **Step 4 (Present Results) — update guidance:**
    Replace the current conditional guidance:
    - OLD: "To fix documentation issues, re-run /mg:create-docs-generate after a fresh scan."
    - NEW: "Run /mg:create-docs-generate to address verify findings. The generator will present findings as an approval tier alongside staleness and notes."

    This directs users to generate (which now has the 3rd tier) instead of suggesting re-scanning.

    **Important Principles section — remove outdated principles:**
    - REMOVE: "Do not modify the verifier agent file (agents/verifier.md). Override behavior in the Task prompt only."
    - REMOVE: "Do not modify install.sh. All commands and sed placeholders are already handled."
    - KEEP all other principles (read-only, --docs-dir not --doc-file, agent instructions pasted, LSP for symbols, 5-tier severity, false negatives, {SCRIPTS_DIR} placeholder, {GLOBAL_CONFIG} placeholder)
    - ADD: "Verify clears docs-verify-findings.json before each run. Generate reads findings but never clears them. This ensures each verify run reflects the current documentation state."
  </action>
  <verify>
    <automated>! grep -q "OVERRIDE" create-docs/commands/create-docs-verify.md && ! grep -q "ADDITIONAL" create-docs/commands/create-docs-verify.md && grep -q "findings_file" create-docs/commands/create-docs-verify.md && grep -q "docs-verify-findings.json" create-docs/commands/create-docs-verify.md && echo "Verify command simplified correctly"</automated>
  </verify>
  <done>
    - No OVERRIDE or ADDITIONAL blocks in Task prompt
    - findings_file path built and passed to agent
    - Prior findings cleared before agent spawn
    - Step 4 guidance references re-running generate (not re-scanning)
    - Outdated principles removed, findings lifecycle principle added
  </done>
</task>

<task type="auto">
  <name>Task 2: Add findings-aware state to router and update scan command</name>
  <files>create-docs/commands/create-docs.md, create-docs/commands/create-docs-scan.md</files>
  <action>
    **create-docs.md — Add findings-aware state (Route D sub-check):**

    In the State Detection section, modify check 6 ("All exist -> Route D"):

    Replace step 6 with:
    ```
    6. **Does `.mg/docs/docs-verify-report.md` exist?**
       - NO -> Route C (generation done, verification needed)

    7. **Are there unresolved verify findings?**
       Check if `.mg/docs/docs-verify-findings.json` exists. If it does, read it and check if the array is non-empty.
       - YES (findings exist and array is non-empty) -> Route E (findings need resolution)
       - NO (file missing, empty, or empty array) -> Route D (pipeline complete)
    ```

    Add a new Route E after Route D:
    ```markdown
    ### Route E: Verify found issues — needs re-generation

    Read `docs-verify-findings.json` and count findings by severity:

    ```
    Verify found quality issues in the documentation.

      {N} critical, {N} high, {N} medium, {N} low, {N} info

    Run /mg:create-docs-generate to address verify findings.
    The generator will present findings as an approval tier alongside staleness and notes.

    Or re-verify first:  /mg:create-docs-verify  (re-check after manual fixes)
    ```
    ```

    Update Route D to reflect that pipeline is truly complete (no findings):
    - Keep the current Route D text but ensure it clearly indicates no outstanding findings

    **Simple file check:** Router reads docs-verify-findings.json directly and checks if array is non-empty. No script dependency (list-verify-findings.py not needed here, per CONTEXT.md decision).

    **create-docs-scan.md — Pass scripts_dir to scan-audience agents:**

    In Step 5 (Per-Audience Scan), update the Task prompt template to include the scripts directory path. Add a line to the agent spawn:

    Current prompt ends with:
    ```
    Write output: {project_root}/.mg/docs/scan-logs/scan-{audience}.json"
    ```

    Add after it:
    ```
    Scripts directory: {SCRIPTS_DIR}"
    ```

    This allows the scan-audience agent to call `write-scan-output.py` via the scripts_dir path. The agent definition (updated in Plan 02) references `{SCRIPTS_DIR}` which gets sed-resolved, but passing it as a runtime parameter ensures consistency.

    Also add a note in the Step 5 section:
    "The scan-audience agent writes its output to a temp file first, then validates it via `write-scan-output.py` before writing to the output path. If validation fails, the agent retries once. If it fails again, the merge handles missing audience data gracefully."
  </action>
  <verify>
    <automated>grep -q "Route E" create-docs/commands/create-docs.md && grep -q "docs-verify-findings.json" create-docs/commands/create-docs.md && grep -q "Scripts directory" create-docs/commands/create-docs-scan.md && echo "Router and scan command updated"</automated>
  </verify>
  <done>
    - Router detects docs-verify-findings.json and routes to "re-run generate" (Route E)
    - Router reads findings file directly (no script dependency)
    - Route D only reached when findings are empty or absent
    - Scan command passes scripts_dir to scan-audience agents
    - Scan command notes the temp-file-then-validate workflow
  </done>
</task>

</tasks>

<verification>
Verify command has no override blocks:
```bash
grep -c "OVERRIDE\|ADDITIONAL" create-docs/commands/create-docs-verify.md
# Should output 0
```

Router has findings-aware state:
```bash
grep "Route E" create-docs/commands/create-docs.md
grep "docs-verify-findings.json" create-docs/commands/create-docs.md
```

Scan command passes script path:
```bash
grep "Scripts directory\|SCRIPTS_DIR" create-docs/commands/create-docs-scan.md
```

No regressions:
```bash
python3 -m pytest -x
```
</verification>

<success_criteria>
- create-docs-verify.md simplified: no OVERRIDE/ADDITIONAL blocks, findings lifecycle managed
- create-docs.md router has Route E for findings-aware state
- create-docs-scan.md passes scripts_dir to scan-audience agents
- All changes consistent with CONTEXT.md locked decisions
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-verify-feedback-loop-and-scan-output/06-03-SUMMARY.md`
</output>
