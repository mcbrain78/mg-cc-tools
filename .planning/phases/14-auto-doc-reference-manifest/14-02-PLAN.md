---
phase: 14-auto-doc-reference-manifest
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - auto-doc/agents/end-user-writer.md
  - auto-doc/agents/developer-writer.md
  - auto-doc/agents/agent-writer.md
  - auto-doc/agents/devops-writer.md
  - auto-doc/commands/auto-doc-generate.md
autonomous: true
requirements: [MAN-02, MAN-04, MAN-05, MAN-06, MAN-12]

must_haves:
  truths:
    - "All 4 writer agents emit manifest entries after writing each section"
    - "All 4 writer agents emit a _written_sections metadata entry per document"
    - "Generate orchestrator creates reference-manifests directory before spawning writers"
    - "Generate orchestrator clears manifests in initial mode but preserves them in update mode"
    - "Generate orchestrator merges temp manifests into persisted manifests after writers complete"
    - "Stale section entries are pruned during merge using _written_sections metadata"
    - "Only audience-specific writers emit manifests -- glossary writer does not"
  artifacts:
    - path: "auto-doc/agents/end-user-writer.md"
      provides: "Writer agent with manifest emission step"
      contains: "add-manifest-entry"
    - path: "auto-doc/agents/developer-writer.md"
      provides: "Writer agent with manifest emission step"
      contains: "add-manifest-entry"
    - path: "auto-doc/agents/agent-writer.md"
      provides: "Writer agent with manifest emission step"
      contains: "add-manifest-entry"
    - path: "auto-doc/agents/devops-writer.md"
      provides: "Writer agent with manifest emission step"
      contains: "add-manifest-entry"
    - path: "auto-doc/commands/auto-doc-generate.md"
      provides: "Generate orchestrator with manifest merge logic"
      contains: "reference-manifests"
  key_links:
    - from: "auto-doc/agents/developer-writer.md"
      to: "auto-doc/scripts/add-manifest-entry.py"
      via: "python3 {SCRIPTS_DIR}/add-manifest-entry.py call in per-section loop"
      pattern: "add-manifest-entry"
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: ".mg/docs/reference-manifests/"
      via: "mkdir -p and manifest merge logic"
      pattern: "reference-manifests"
---

<objective>
Wire manifest emission into the generate pipeline: update all 4 writer agents to emit manifest entries after each section, and update the generate orchestrator to handle manifest directory creation, clearing (initial mode), merging, and stale section cleanup.

Purpose: This is the "write side" of the manifest system. After this plan, every generate run produces per-audience manifest files capturing all code symbols and file paths referenced in generated documentation.
Output: Modified writer agents and generate command with manifest support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auto-doc-reference-manifest/14-CONTEXT.md
@.planning/phases/14-auto-doc-reference-manifest/14-RESEARCH.md
@.planning/phases/14-auto-doc-reference-manifest/14-01-SUMMARY.md

@auto-doc/agents/developer-writer.md
@auto-doc/agents/end-user-writer.md
@auto-doc/agents/agent-writer.md
@auto-doc/agents/devops-writer.md
@auto-doc/commands/auto-doc-generate.md
</context>

<interfaces>
<!-- From Plan 01: add-manifest-entry.py CLI interface -->
<!-- Writers call this after each section to record references -->

```
python3 {SCRIPTS_DIR}/add-manifest-entry.py \
  --input /tmp/manifest-entry-{audience}-NNN.json \
  --manifest /tmp/manifest-{audience}.json
```

Input JSON format:
```json
{"document": "ARCHITECTURE", "section": "system-overview",
 "symbols": ["RoadRunnerBase", "fetch_quarterly"],
 "file_paths": ["src/app.ts", "src/routes/"]}
```

Metadata entry for stale section cleanup:
```json
{"document": "ARCHITECTURE", "section": "_written_sections",
 "symbols": [], "file_paths": [],
 "sections_written": ["system-overview", "data-model", "component-map"]}
```

Manifest file structure:
```json
{"audience": "developers", "generated": "2026-03-22T14:30:00Z",
 "documents": {"ARCHITECTURE": {"system-overview": {"symbols": [...], "file_paths": [...]}}}}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest emission to all 4 writer agents</name>
  <files>auto-doc/agents/end-user-writer.md, auto-doc/agents/developer-writer.md, auto-doc/agents/agent-writer.md, auto-doc/agents/devops-writer.md</files>
  <action>
Add a manifest emission step to each of the 4 audience-specific writer agents. The change goes in the Process section, inside the per-section loop (step 2c), AFTER the docs-meta comment emission and BEFORE writing the document file. Also add a per-document metadata emission step after all sections for that document are written.

For each writer agent, add these two additions:

**Addition 1: Per-section manifest emission (inside the per-section loop, after generating section content and docs-meta comment):**

Add to step 2 (the per-section loop), after the docs-meta comment line, a new sub-step:

```
   - **Emit manifest entry.** After writing each section, record every code symbol and file path you referenced:
     1. List all code symbols referenced in this section as unqualified identifiers (e.g., `RoadRunnerBase`, `fetch_quarterly` -- NOT `FMPClient.fetch_quarterly`)
     2. List all file paths referenced in this section, relative to project root
     3. Write a temp JSON file to `/tmp/manifest-entry-{audience}-NNN.json` (increment NNN per section, starting from 001):
        ```json
        {"document": "DOCUMENT_NAME", "section": "section-slug",
         "symbols": ["symbol1", "symbol2"],
         "file_paths": ["src/file.ts", "src/dir/"]}
        ```
     4. Call:
        ```bash
        python3 {SCRIPTS_DIR}/add-manifest-entry.py \
          --input /tmp/manifest-entry-{audience}-NNN.json \
          --manifest /tmp/manifest-{audience}.json
        ```
     If a section references no code symbols or file paths (e.g., a pure conceptual section), skip the manifest entry for that section.
```

**Addition 2: Per-document _written_sections metadata entry (after all sections for a document are written, before moving to the next document):**

After the per-section loop completes for each document, add:

```
   - **Emit sections metadata.** After all sections for this document are written, emit a metadata entry listing all sections you wrote:
     Write to `/tmp/manifest-entry-{audience}-metadata-{DOCUMENT}.json`:
     ```json
     {"document": "DOCUMENT_NAME", "section": "_written_sections",
      "symbols": [], "file_paths": [],
      "sections_written": ["section-slug-1", "section-slug-2"]}
     ```
     Call:
     ```bash
     python3 {SCRIPTS_DIR}/add-manifest-entry.py \
       --input /tmp/manifest-entry-{audience}-metadata-{DOCUMENT}.json \
       --manifest /tmp/manifest-{audience}.json
     ```
```

**Audience-specific {audience} values:**
- end-user-writer.md: `end-users`
- developer-writer.md: `developers`
- agent-writer.md: `agents`
- devops-writer.md: `devops`

Do NOT modify the glossary-writer.md -- shared docs have no manifest (MAN-12).

Keep changes minimal. Do not rewrite entire agent files. Insert the new steps at the correct locations in each agent's Process section.
  </action>
  <verify>
    <automated>for f in auto-doc/agents/end-user-writer.md auto-doc/agents/developer-writer.md auto-doc/agents/agent-writer.md auto-doc/agents/devops-writer.md; do echo "=== $f ===" && grep -c "add-manifest-entry" "$f" && grep -c "_written_sections" "$f"; done</automated>
  </verify>
  <done>All 4 writer agents contain manifest emission instructions (add-manifest-entry calls) and _written_sections metadata emission. Glossary writer is unchanged. Each agent uses its own audience-namespaced temp files.</done>
</task>

<task type="auto">
  <name>Task 2: Add manifest merge logic to generate orchestrator</name>
  <files>auto-doc/commands/auto-doc-generate.md</files>
  <action>
Modify auto-doc-generate.md with three changes: workspace prep additions in Step 3, a new manifest merge step after Stage 2, and temp file cleanup for manifests.

**Change 1: Step 3 (Prepare Workspace) -- add manifest directory creation and initial mode clearing.**

After the existing "Clean glossary reconciliation log" step (the last item in Step 3), add:

```
4. **Prepare manifest workspace.**
   ```bash
   mkdir -p {project_root}/.mg/docs/reference-manifests
   ```

   In initial mode only, clear existing manifests for a clean slate:
   ```bash
   rm -f {project_root}/.mg/docs/reference-manifests/*.json
   ```

   Clean temp manifest files from prior runs:
   ```bash
   rm -f /tmp/manifest-*.json /tmp/manifest-entry-*.json
   ```
```

**Change 2: New inline step between Stage 2 and Stage 3 -- manifest merge.**

After Stage 2 ("Write Audience Documents") completes and before Stage 3 ("Reconcile Glossary"), add a new section:

```
### Manifest Merge

After all writer agents complete, merge their temp manifests into persisted location.

For each audience that had a writer agent run (i.e., each audience that was spawned in Stage 2):

1. Check if `/tmp/manifest-{audience}.json` exists. If not, skip this audience (agent produced no manifest entries).

2. Read the temp manifest: `/tmp/manifest-{audience}.json`

3. Read persisted manifest (if it exists): `{project_root}/.mg/docs/reference-manifests/{audience}.json`
   If it does not exist, start with an empty manifest: `{"audience": "{audience}", "generated": "", "documents": {}}`

4. **Process _written_sections metadata for stale cleanup.** For each document in the temp manifest:
   - Look for an entry with section key `_written_sections`
   - If found, read its `sections_written` list
   - In the persisted manifest for that document, remove any section entries whose slug is NOT in `sections_written` (these are stale sections that were dropped during regeneration)
   - Delete the `_written_sections` entry from the temp manifest (do not persist metadata)

5. **Overlay temp entries onto persisted manifest.** For each document -> section in the temp manifest, replace the matching (document, section) entry in the persisted manifest. Preserve entries in the persisted manifest that are not in the temp manifest (sections that were not regenerated).

6. **Update timestamp.** Set `generated` to the current ISO timestamp.

7. **Write merged manifest** to `{project_root}/.mg/docs/reference-manifests/{audience}.json`.

This merge logic ensures:
- Initial mode: persisted manifests were cleared in Step 3, so merge equals copy
- Update mode: only regenerated sections are upserted, all other sections preserved
- Stale sections from dropped headings are cleaned up via _written_sections metadata
```

**Change 3: Update Stage 2 progress message** to mention manifest emission:

Change `"Stage 2/4: Writing audience documents (4 agents in parallel)..."` to `"Stage 2/4: Writing audience documents with manifest emission (4 agents in parallel)..."`

Do NOT modify any other stages or steps. The manifest merge is inline logic performed by the orchestrator (not an agent invocation), placed between Stage 2 completion and Stage 3 start.
  </action>
  <verify>
    <automated>grep -c "reference-manifests" auto-doc/commands/auto-doc-generate.md && grep -c "_written_sections" auto-doc/commands/auto-doc-generate.md && grep -c "Manifest Merge" auto-doc/commands/auto-doc-generate.md</automated>
  </verify>
  <done>Generate command creates reference-manifests directory in Step 3, clears manifests in initial mode, merges temp manifests after Stage 2 with stale section cleanup via _written_sections metadata. Update mode preserves non-regenerated sections.</done>
</task>

</tasks>

<verification>
```bash
# All 4 writer agents reference add-manifest-entry.py
for f in auto-doc/agents/end-user-writer.md auto-doc/agents/developer-writer.md auto-doc/agents/agent-writer.md auto-doc/agents/devops-writer.md; do
  grep -l "add-manifest-entry" "$f"
done

# Glossary writer does NOT reference manifest (MAN-12)
! grep -q "add-manifest-entry" auto-doc/agents/glossary-writer.md

# Generate command has manifest merge logic
grep "Manifest Merge" auto-doc/commands/auto-doc-generate.md
grep "reference-manifests" auto-doc/commands/auto-doc-generate.md

# Full test suite still passes (prompt changes don't break Python tests)
python3 -m pytest auto-doc/scripts/tests/ -x --tb=short -q --no-header
```
</verification>

<success_criteria>
- All 4 audience-specific writer agents emit manifest entries per section and _written_sections metadata per document
- Glossary writer is unchanged (MAN-12)
- Generate orchestrator creates manifest directory, clears in initial mode, merges temp manifests with stale cleanup
- Update mode preserves non-regenerated sections and audiences
- Existing test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-auto-doc-reference-manifest/14-02-SUMMARY.md`
</output>
