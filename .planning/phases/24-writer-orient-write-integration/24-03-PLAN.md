---
phase: 24-writer-orient-write-integration
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - auto-doc/commands/auto-doc-generate.md
autonomous: true
requirements: [OWI-06, OWI-07]

must_haves:
  truths:
    - "Generate command routes devops writers through orient-write prompt when refined template exists"
    - "Generate command falls back to standard prompt for devops when no refined template exists"
    - "Non-devops audiences always use standard prompt (unchanged behavior)"
    - "Generate command prints stale warning when refined templates are stale"
    - "State file path is scoped per audience+document to prevent collision (Pitfall 3)"
    - "Heading state temp files are cleaned at workspace setup time"
    - "Projects without refined templates continue working exactly as before"
  artifacts:
    - path: "auto-doc/commands/auto-doc-generate.md"
      provides: "Refined template routing in Stage 2, stale warning, per-document state file paths"
      contains: "refined_templates"
  key_links:
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: "auto-doc/scripts/generate-setup.py"
      via: "JSON output includes refined_templates dict consumed for routing decisions"
      pattern: "refined_templates"
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: "auto-doc/agents/devops-writer.md"
      via: "Agent prompt passes refined_template_path and state_file_path"
      pattern: "refined_template_path|state_file_path"
    - from: "auto-doc/commands/auto-doc-generate.md"
      to: "auto-doc/scripts/next-heading.py"
      via: "State file path for heading-state-{audience}-{DOCUMENT}.json created in tmp"
      pattern: "heading-state"
---

<objective>
Modify auto-doc-generate.md to route devops writers through orient-write loop when refined templates exist

Purpose: The generate command must detect refined templates (from generate-setup.py output), construct different writer prompts for refined vs generic paths, print stale warnings, and ensure backward compatibility for projects without refined templates.
Output: Modified auto-doc/commands/auto-doc-generate.md with conditional routing in Stage 2
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-writer-orient-write-integration/24-CONTEXT.md
@.planning/phases/24-writer-orient-write-integration/24-RESEARCH.md
@.planning/phases/24-writer-orient-write-integration/24-01-SUMMARY.md
@.planning/phases/24-writer-orient-write-integration/24-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 and 02. -->

From generate-setup.py JSON output (extended by Plan 01):
```json
{
  "project_root": "/abs/path",
  "docs_dir_abs": "/abs/path/docs/auto-doc",
  "tmp_dir": "/abs/path/.mg/docs/tmp",
  "scan_data_path": "/abs/path/.mg/docs/docs-scan.json",
  "project_model_path": "/abs/path/.mg/docs/tmp/project-model.json",
  "mode": "initial",
  "audiences": {"devops": {"documents": ["OPERATIONS", "TROUBLESHOOTING"]}, ...},
  "scan_views": {"devops": "/abs/path/.mg/docs/tmp/scan-view-devops.json", ...},
  "notes_by_audience": {"devops": [], ...},
  "refined_templates": {
    "devops": {
      "OPERATIONS": {"path": "/abs/path/.mg/docs/templates/devops/OPERATIONS.template.md", "stale": false},
      "TROUBLESHOOTING": {"path": "/abs/path/.mg/docs/templates/devops/TROUBLESHOOTING.template.md", "stale": true}
    },
    "end-users": {
      "USER_GUIDE": null
    }
  },
  "stale_templates": ["devops/TROUBLESHOOTING"]
}
```

From devops-writer.md (rewritten in Plan 02, new inputs):
```
Inputs:
  refined_template_path: Path to .mg/docs/templates/devops/{DOCUMENT}.template.md
  state_file_path: Path to heading-state-devops-{DOCUMENT}.json
  scripts_dir: Absolute path to auto-doc/scripts/
```

From auto-doc/commands/auto-doc-generate.md Stage 2 (current pattern, lines 118-160):
```markdown
### Stage 2: Write Audience Documents (parallel)
# Spawns one Agent per audience, each reads its own writer.md instructions
Agent(
  description="Generate {audience} documentation ({mode} mode)",
  prompt="You are a {audience} writer agent.
  Read and follow the instructions in: {AGENTS_DIR}/{audience}-writer.md
  Project root: {project_root}
  ...
  Templates dir: {TEMPLATES_DIR}/{audience}/
  ..."
)
```

State file path convention (Pitfall 3 prevention):
```
{TMP_DIR}/heading-state-{audience}-{DOCUMENT}.json
```
e.g., heading-state-devops-OPERATIONS.json, heading-state-devops-TROUBLESHOOTING.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add refined template routing and stale warning to generate command</name>
  <files>auto-doc/commands/auto-doc-generate.md</files>
  <action>
    Read the current auto-doc-generate.md fully to understand Stage 2 writer spawning.

    **Modification 1: Parse refined_templates from setup output (after Step 1)**

    After parsing the generate-setup.py JSON output, add parsing of two new fields:
    - `refined_templates`: dict of audience -> document -> {path, stale} or null
    - `stale_templates`: list of "audience/document" strings

    **Modification 2: Stale template warning (after Step 1, before Stage 1)**

    If `stale_templates` is non-empty, print a warning:
    ```
    Warning: Refined templates may be stale (scan is newer than template):
      - devops/TROUBLESHOOTING
    Run /mg:auto-doc-prepare-templates to refresh.
    ```
    This is a warning only -- do NOT abort. Continue with stale templates.

    **Modification 3: Add heading-state-*.json to temp cleanup (in prepare_workspace or note in command)**

    In the command's Step 1, after calling generate-setup.py (which already cleans temp files), note that heading-state-*.json files should also be cleaned. Since generate-setup.py handles temp cleanup, extend the cleanup pattern list in generate-setup.py's `prepare_workspace` to include `"heading-state-*.json"`. Actually -- the cleanup patterns in generate-setup.py already include write-state, section, refs, header, manifest patterns. Add `"heading-state-*.json"` to the temp cleanup patterns in generate-setup.py.

    IMPORTANT: This requires a ONE-LINE addition to generate-setup.py's `prepare_workspace()` function (line ~178-184 area) -- add `"heading-state-*.json"` to the cleanup patterns list. This is a minor cross-file touch that is acceptable.

    **Modification 4: Conditional writer prompt in Stage 2 (main change)**

    Replace the current single Agent template in Stage 2 with conditional logic:

    ```markdown
    2. **Spawn one Agent call per audience** in `audiences` (from setup output) in a SINGLE message (parallel foreground). Each subagent reads its own instructions.

    **For devops audience:** Check `refined_templates["devops"]` for each document. If the audience has ANY non-null refined template entries, construct a specialized orient-write prompt:

      For each document in devops.documents where refined_templates["devops"][document] is not null:
        ```
        Agent(
          description="Generate devops {DOCUMENT} documentation ({mode} mode, orient-write)",
          prompt="You are a devops writer agent.

        Read and follow the instructions in: {AGENTS_DIR}/devops-writer.md

        Project root: {project_root}
        Docs dir: {docs_dir_abs}
        Scan data path: {scan_views["devops"]}
        Project model path: {project_model_path}
        Style guide path: references/style-guide.md
        Glossary path: {docs_dir_abs}/GLOSSARY.md
        Documents: {DOCUMENT}
        Mode: {mode}
        Refined template path: {refined_templates["devops"][DOCUMENT]["path"]}
        State file path: {tmp_dir}/heading-state-devops-{DOCUMENT}.json
        Scripts dir: {SCRIPTS_DIR}
        Tmp dir: {tmp_dir}

        Standing notes (incorporate into relevant sections):
        {notes for this document, or 'None'}"
        )
        ```

      For each document in devops.documents where refined_templates["devops"][document] IS null:
        Use the standard prompt (same as current behavior with Templates dir).

    **For all other audiences (end-users, developers, agents):** Always use the standard prompt (current behavior, unchanged):
      ```
      Agent(
        description="Generate {audience} documentation ({mode} mode)",
        prompt="You are a {audience} writer agent.

      Read and follow the instructions in: {AGENTS_DIR}/{audience}-writer.md

      Project root: {project_root}
      Docs dir: {docs_dir_abs}
      Scan data path: {scan_views[audience]}
      Project model path: {project_model_path}
      Templates dir: {TEMPLATES_DIR}/{audience}/
      Style guide path: references/style-guide.md
      Glossary path: {docs_dir_abs}/GLOSSARY.md
      Documents: {audiences[audience].documents joined by comma}
      Mode: {mode}

      Standing notes (incorporate into relevant sections):
      {notes_by_audience[audience] formatted, or 'None' if empty}"
      )
      ```
    ```

    **Key differences in orient-write prompt vs standard prompt:**
    - `Refined template path` replaces `Templates dir` -- writer sees ONLY the refined template
    - `State file path` is new -- scoped per document (`heading-state-devops-{DOCUMENT}.json`)
    - `Scripts dir` is new -- writer needs it for next-heading.py calls
    - `Tmp dir` is new -- writer needs it for temp files
    - `Documents` is singular (one document per agent call when using orient-write, since state files are per-document)

    **Important:** When using orient-write, each devops document gets its OWN Agent call (not a single agent for all devops documents) because each document needs its own state file. For standard prompts, the existing pattern of one agent per audience handling all documents continues.

    **Modification 5: Update cleanup patterns in generate-setup.py**

    Add `"heading-state-*.json"` to the temp cleanup for loop in generate-setup.py's `prepare_workspace()` function (around line 178-184).
  </action>
  <verify>
    <automated>grep -c "refined_templates" auto-doc/commands/auto-doc-generate.md && grep -c "orient-write" auto-doc/commands/auto-doc-generate.md && grep -c "heading-state" auto-doc/commands/auto-doc-generate.md && grep "heading-state" auto-doc/scripts/generate-setup.py && echo "PASS"</automated>
  </verify>
  <done>
    Generate command conditionally routes devops writers through orient-write prompt when refined_templates entry is non-null. Standard prompt used for all other audiences and for devops documents without refined templates. Stale warning printed when stale_templates is non-empty. State files scoped per audience+document. heading-state-*.json cleaned at workspace setup. Backward compatibility maintained -- projects without refined templates use existing behavior unchanged.
  </done>
</task>

</tasks>

<verification>
```bash
grep -c "refined_templates" auto-doc/commands/auto-doc-generate.md && grep -c "orient-write" auto-doc/commands/auto-doc-generate.md && grep -c "stale" auto-doc/commands/auto-doc-generate.md && grep "heading-state" auto-doc/scripts/generate-setup.py && uv run pytest auto-doc/scripts/tests/test_generate_setup.py -x --tb=short -q && echo "ALL PASS"
```
</verification>

<success_criteria>
- Generate command routes devops writers through orient-write prompt when refined template exists for that document
- Generate command uses standard prompt when refined template is null or audience is not devops
- Stale warning printed before Stage 1 when stale_templates is non-empty
- Each devops document gets its own Agent call with per-document state file path
- Non-devops audiences unchanged (same Agent prompt as current)
- heading-state-*.json cleaned in prepare_workspace
- generate-setup.py existing tests still pass after adding cleanup pattern
- Full auto-doc test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/24-writer-orient-write-integration/24-03-SUMMARY.md`
</output>
