---
phase: 23-template-refiner-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - auto-doc/agents/template-refiner.md
autonomous: true
requirements: [TRF-02, TRF-03, TRF-04, TRF-05, TRF-06, TRF-07]

must_haves:
  truths:
    - "Refiner reads generic template and preserves all ## heading text and order verbatim"
    - "Refiner performs shallow source exploration: get_symbols_overview for Python, full Read for non-code files"
    - "PURPOSE comments contain project-specific structural facts grounded in source evidence"
    - "EXAMPLE blocks contain only generic format demonstrations with placeholder data"
    - "OPTIONAL sections are resolved based on source evidence: keep if any evidence, drop if none"
    - "Refined template includes REFINED metadata comment with current date and scan date"
    - "Same scan data produces structurally equivalent heading trees on re-run"
  artifacts:
    - path: "auto-doc/agents/template-refiner.md"
      provides: "Refiner agent definition for per-document template refinement"
      min_lines: 100
  key_links:
    - from: "auto-doc/agents/template-refiner.md"
      to: "auto-doc/scripts/get-section-sources.py"
      via: "Bash invocation for source file lookup"
      pattern: "get-section-sources\\.py"
    - from: "auto-doc/agents/template-refiner.md"
      to: ".mg/docs/docs-scan.json"
      via: "Read scan data for source_material_index"
      pattern: "source_material_index"
    - from: "auto-doc/agents/template-refiner.md"
      to: "next-heading.py parse_template()"
      via: "Output format compatibility"
      pattern: "PURPOSE|EXAMPLE"
---

<objective>
Create the template-refiner agent definition that produces project-specific refined templates.

Purpose: The refiner agent is the core intelligence of the pipeline -- spawned once per document, it reads the generic template, performs shallow source exploration, decides what `###`/`####` headings to add under each `##` section, writes project-specific PURPOSE comments with structural facts, and produces generic EXAMPLE blocks. The refined template must be parseable by next-heading.py's `parse_template()` function (Phase 22).

Output: `auto-doc/agents/template-refiner.md` agent definition file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-template-refiner-pipeline/23-CONTEXT.md
@.planning/phases/23-template-refiner-pipeline/23-RESEARCH.md
@.planning/phases/22-heading-iterator-script/22-01-SUMMARY.md

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->

From auto-doc/agents/TEMPLATE.md (agent structure pattern):
```markdown
# Writer Agent Template
## Role
You are a specialized writer agent for the **[AUDIENCE_NAME]** audience...
## Inputs
- **project_root**: Absolute path to the project.
- **docs_dir**: ...
- **scan_data_path**: ...
## Process
1. **Read context** -- ...
2. **For each assigned document:** ...
## Output Conventions
- Write audience-specific docs to `{docs_dir}/{audience}/`...
```

From auto-doc/agents/devops-writer.md (agent input pattern):
```markdown
## Inputs
- **project_root**: Absolute path to the project.
- **docs_dir**: Absolute path to the output docs directory.
- **scan_data_path**: Path to per-audience view file.
- **templates_dir**: Path to `{TEMPLATES_DIR}/devops/`.
- **style_guide_path**: Path to `references/style-guide.md`.
```

From auto-doc/references/templates/devops/OPERATIONS.template.md (generic template format):
```markdown
<!-- DIATAXIS: how-to + reference -->
<!-- AUDIENCE: devops -->

# Operations Guide

## Infrastructure Overview
<!-- PURPOSE: Operators need a single-page mental model... -->
<!-- EXAMPLE:
### Deployment Topology
| Component | Host | Port | Health Check |
|-----------|------|------|-------------|
...
-->

## Deployment
<!-- PURPOSE: Step-by-step deployment procedure... -->
<!-- OPTIONAL -- delete if not applicable -->
```

From auto-doc/scripts/next-heading.py parse_template() (output format requirements):
- Expects ##-#### headings as markdown heading lines
- Expects <!-- PURPOSE: content --> multi-line HTML comments after headings
- Expects <!-- EXAMPLE: content --> multi-line HTML comments after headings
- Strips HTML comment ranges before heading detection (handles headings inside EXAMPLE blocks)

From auto-doc/scripts/get-section-sources.py (source file lookup):
```bash
python3 {SCRIPTS_DIR}/get-section-sources.py \
  --scan-file .mg/docs/docs-scan.json \
  --key "DOCUMENT/section-slug"
```
Returns JSON: { "source_files": [...] }

From auto-doc/scripts/list-optional-sections.py (OPTIONAL section identification):
```bash
python3 {SCRIPTS_DIR}/list-optional-sections.py \
  --template-file {path_to_generic_template}
```
Returns JSON array of section slugs marked OPTIONAL.

Refined template output format (from RESEARCH.md Pattern 4):
```markdown
<!-- DIATAXIS: how-to + reference -->
<!-- AUDIENCE: devops -->
<!-- REFINED: 2026-04-02, scan: 2026-04-01 -->

# Operations Guide

## Infrastructure Overview
<!-- PURPOSE: Operators need a single-page mental model of the system's
     deployment topology. 3 systemd services on a single host, PostgreSQL
     on a separate host, Prefect orchestration layer. -->

### Deployment Topology
<!-- PURPOSE: Component-to-host mapping. 3 systemd services (prefect-server,
     finance-data-worker, stock-ranker-worker) plus PostgreSQL on mcbrain-server2. -->
<!-- EXAMPLE:
| Component | Service Unit | Host | Port | Health Check |
|-----------|-------------|------|------|-------------|
| ... | ... | ... | ... | ... |
-->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template-refiner agent definition</name>
  <files>auto-doc/agents/template-refiner.md</files>
  <action>
Create `auto-doc/agents/template-refiner.md` following the TEMPLATE.md and devops-writer.md agent structure. The file has NO frontmatter (agent files are not slash commands -- they are spawned via Agent tool from the command file).

**Structure:**

```
# Template Refiner Agent

{Brief description: spawned per document, produces project-specific refined template}

## Role

You are a template refiner agent. You produce a project-specific refined template by reading the generic template, performing shallow source exploration, and deciding what ### and #### headings each ## section needs. You write structural facts into PURPOSE comments and generic format demonstrations into EXAMPLE blocks. **You never modify project source code.**

## Inputs

- **project_root**: Absolute path to the project
- **generic_template_path**: Path to the generic template for this document
- **scan_data_path**: Path to docs-scan.json
- **output_path**: Exact path where the refined template should be written
- **audience**: Audience name (e.g., devops, developers)
- **document**: Document name (e.g., OPERATIONS, ARCHITECTURE)
- **scan_date**: From scan data (for REFINED metadata comment)
- **scripts_dir**: Path to scripts directory for helper script invocation

## Process

1. **Read context**
   - Read the generic template from `generic_template_path`
   - Read scan data from `scan_data_path` -- extract `source_material_index` and `project_model`
   - Note the `<!-- DIATAXIS: ... -->` and `<!-- AUDIENCE: ... -->` comments from the generic template -- these are preserved verbatim in the refined template

2. **Extract ## sections from generic template**
   - Parse all `## ` heading lines from the generic template
   - Record each heading's exact text (you will preserve this verbatim)
   - Record each heading's PURPOSE comment content
   - Record each heading's EXAMPLE comment content (if any)
   - Identify which sections are marked `<!-- OPTIONAL -- delete if not applicable -->`

3. **For each ## section, perform shallow source exploration**
   a. Look up source files for this section:
      ```bash
      python3 {SCRIPTS_DIR}/get-section-sources.py --scan-file {scan_data_path} --key "{document}/{section-slug}"
      ```
      Parse the JSON output to get the `source_files` array.

   b. For each source file, perform SHALLOW exploration only:
      - **Python files (.py):** Use `get_symbols_overview` (Serena) to get class names, function names, public API surface. Do NOT read function bodies. If Serena is not available, Read the file but focus only on class/function definitions, imports, and module-level docstrings.
      - **Non-code files** (.service, .yaml, .yml, .toml, .env, .env.example, .cfg, .ini, .conf, .sql, .json, .md): Read the full file content. These are typically small configuration or infrastructure files.
      - **Other code files** (.js, .ts, .go, etc.): Use `get_symbols_overview` if available, otherwise Read and focus on exports/class/function declarations.

   c. Also check the `project_model` for relevant information about this section's domain (components, infrastructure, tech_stack entries).

   d. **Decide what ### and #### headings this section needs** based on source findings:
      - Group related findings into ### headings (e.g., 3 systemd services -> "Service Units" heading)
      - Add #### headings when a ### has multiple distinct sub-topics (e.g., per-service details if > 3 services)
      - Each heading must be justified by source evidence -- do not invent headings for topics not found in source files or project_model

   e. **Resolve OPTIONAL sections:**
      - If ANY evidence exists for an OPTIONAL section (source files in index, relevant entries in project_model, related components), KEEP the section and add child headings
      - If NO evidence exists at all, DROP the section entirely from the refined template
      - Conservative approach: when in doubt, keep

4. **Compose the refined template**

   The refined template is a COMPLETE document. Write it with this structure:

   ```
   <!-- DIATAXIS: {preserved from generic} -->
   <!-- AUDIENCE: {preserved from generic} -->
   <!-- REFINED: {today's date}, scan: {scan_date} -->

   # {Title preserved from generic}

   ## {Section 1 -- verbatim from generic}
   <!-- PURPOSE: {project-specific rewrite grounded in source evidence} -->

   ### {New heading based on source findings}
   <!-- PURPOSE: {project-specific structural facts} -->
   <!-- EXAMPLE:
   {Generic format demonstration with ... placeholders}
   -->

   ### {Another heading}
   <!-- PURPOSE: {project-specific structural facts} -->
   <!-- EXAMPLE:
   {Generic format demonstration}
   -->

   ## {Section 2 -- verbatim from generic}
   ...
   ```

5. **Write the refined template** to `output_path` using the Write tool.

## Critical Rules

### MUST rules:
- **MUST** preserve ## heading text EXACTLY as written in the generic template. Do NOT rename, reword, reorder, or reorganize ## sections. Slug identity must match source_material_index keys.
- **MUST** preserve <!-- DIATAXIS: ... --> and <!-- AUDIENCE: ... --> comments verbatim from the generic template.
- **MUST** include <!-- REFINED: {date}, scan: {scan_date} --> as the third comment line.
- **MUST** ground every PURPOSE comment in observable source evidence (class counts, service names, config entries, API names). Cite what you found.
- **MUST** use ... placeholders and generic column headers in EXAMPLE blocks. They demonstrate format only.
- **MUST** make heading decisions deterministically based on source evidence. The same source files should produce the same heading structure.

### MUST NOT rules:
- **MUST NOT** change ## heading text in any way -- not even capitalization or punctuation.
- **MUST NOT** put project-specific values (real class names, file paths, service names, counts) in EXAMPLE blocks. All project-specific content goes ONLY in PURPOSE comments.
- **MUST NOT** read function bodies or implementation logic. `get_symbols_overview` returns class/function names and signatures -- that is the ceiling for code files.
- **MUST NOT** process shared documents (OVERVIEW, GLOSSARY) -- the command ensures only audience-specific documents are passed to this agent.

## Output Format

The refined template is a standalone markdown file that fully replaces the generic template for the writer. It contains:
- All metadata comments (DIATAXIS, AUDIENCE, REFINED)
- All ## sections from the generic template (in original order, with original text)
- Project-specific PURPOSE comments at every heading level (##, ###, ####)
- Generic structural EXAMPLE blocks at ### and #### levels
- No OPTIONAL markers -- they are resolved (kept as concrete headings or dropped)
  </action>
  <verify>
    <automated>test -f auto-doc/agents/template-refiner.md && grep -q 'MUST NOT.*change.*heading' auto-doc/agents/template-refiner.md && grep -q 'get-section-sources' auto-doc/agents/template-refiner.md && grep -q 'REFINED:' auto-doc/agents/template-refiner.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  - template-refiner.md exists with Role, Inputs, Process, Critical Rules, and Output Format sections
  - Agent reads generic template and preserves ## heading text verbatim (explicit MUST NOT rule)
  - Agent performs shallow source exploration: get_symbols_overview for Python, full Read for non-code
  - PURPOSE comments are project-specific with structural facts from source evidence
  - EXAMPLE blocks are generic format demonstrations with ... placeholders
  - OPTIONAL sections are resolved conservatively (keep if any evidence, drop if none)
  - REFINED metadata comment includes current date and scan_date
  - Heading decisions are evidence-based for idempotency
  - Output is parseable by next-heading.py parse_template() (##-#### headings with PURPOSE/EXAMPLE HTML comments)
  </done>
</task>

</tasks>

<verification>
1. `wc -l auto-doc/agents/template-refiner.md` shows >= 100 lines
2. `grep -c 'MUST NOT' auto-doc/agents/template-refiner.md` shows >= 3 critical constraints
3. `grep 'get-section-sources' auto-doc/agents/template-refiner.md` confirms script usage
4. `grep 'get_symbols_overview' auto-doc/agents/template-refiner.md` confirms shallow exploration pattern
5. Full test suite passes: `uv run python -m pytest --tb=short -q --no-header`
</verification>

<success_criteria>
- Agent file follows TEMPLATE.md structure (Role, Inputs, Process, Output)
- Shallow exploration pattern is precisely defined (symbol overviews for code, full reads for non-code)
- PURPOSE vs EXAMPLE content boundary is explicitly enforced via MUST/MUST NOT rules
- ## slug preservation is enforced via explicit MUST NOT rule
- OPTIONAL resolution uses conservative heuristic (keep if any evidence)
- Output format matches next-heading.py parse_template() expectations
- Idempotency guidance: evidence-based heading decisions for deterministic output
</success_criteria>

<output>
After completion, create `.planning/phases/23-template-refiner-pipeline/23-02-SUMMARY.md`
</output>
