---
phase: 03-scan-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - create-docs/install.sh
autonomous: false
requirements:
  - CMD-02

must_haves:
  truths:
    - "install.sh validates and copies scan-audience.md alongside other agent files"
    - "Existing Python test suite still passes after all changes"
    - "User confirms scan command produces valid docs-scan.json on a real project"
  artifacts:
    - path: "create-docs/install.sh"
      provides: "Updated install script handling new scan-audience agent"
      contains: "scan-audience"
  key_links:
    - from: "create-docs/install.sh"
      to: "create-docs/agents/scan-audience.md"
      via: "wildcard copy of agents/*.md"
      pattern: "agents/\\*\\.md"
---

<objective>
Validate the Phase 3 deliverables work end-to-end: ensure install.sh handles the new scan-audience agent, existing tests still pass, and the user can verify the scan command on a real project.

Purpose: Phase 3's command and agent are LLM prompts that cannot be unit-tested. The only meaningful verification is running install.sh to confirm integration and having the user smoke-test on a real project (road-runner, per ROADMAP success criteria).

Output: Updated install.sh (if needed) and user confirmation of scan pipeline functionality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-pipeline/03-01-SUMMARY.md

<interfaces>
From create-docs/install.sh (existing agent copy logic):
```bash
# Copy agents (if any exist -- Phase 2 adds them)
if ls "${SCRIPT_DIR}"/agents/*.md &>/dev/null 2>&1; then
  cp "${SCRIPT_DIR}"/agents/*.md "${SUPPORT_DIR}/agents/"
fi
```
Note: install.sh already copies ALL agents/*.md files via wildcard. Adding scan-audience.md
should be picked up automatically. But verify no agent-specific validation or sed issues.

From create-docs/install.sh (sed resolution for agent files):
```bash
# Resolve placeholders in agent files (Phase 2 adds agents)
for agent_file in "${SUPPORT_DIR}/agents/"*.md; do
  # resolves: references/schema.md, references/style-guide.md,
  #   {GLOBAL_CONFIG}, {SCRIPTS_DIR}, {TEMPLATES_DIR}
done
```

Existing test commands:
- `python3 -m pytest create-docs/scripts/tests/ -x -q` (create-docs scripts)
- `python3 -m pytest -x -q` (full suite)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate install.sh integration and run test suite</name>
  <files>create-docs/install.sh</files>
  <action>
1. **Verify install.sh handles new agent:**
   - Read `create-docs/install.sh` and confirm the existing `agents/*.md` wildcard copy will pick up `scan-audience.md`
   - Confirm the sed resolution loop for agent files will resolve any placeholders in `scan-audience.md` (specifically `{SCRIPTS_DIR}`, `{TEMPLATES_DIR}`, and `references/style-guide.md` if used)
   - If scan-audience.md uses any NEW placeholder not in the existing sed list, add the corresponding sed pattern to install.sh

2. **Verify source file validation:**
   - Check if install.sh has any hardcoded agent filename checks that would miss scan-audience.md
   - The current validation only checks directories exist (not individual agent files), so this should be fine
   - If the install.sh summary agent count includes the new file automatically (it uses `find` with wildcard), confirm this

3. **Run existing test suite:**
   ```bash
   python3 -m pytest create-docs/scripts/tests/ -x -q
   ```
   All Phase 1 tests must still pass. The scan command and agent are markdown prompt files -- they don't affect Python script behavior.

4. **Run full project test suite:**
   ```bash
   python3 -m pytest -x -q
   ```

5. **Dry-run install.sh** to verify no errors:
   ```bash
   mkdir -p /tmp/test-install-03/.claude
   ./create-docs/install.sh --target /tmp/test-install-03/.claude
   ```
   Verify output shows the correct agent count (should be 8 now: 4 writers + glossary + staleness-scanner + verifier + scan-audience).
   Verify scan-audience.md exists in the installed agents directory with resolved paths.
   Clean up: `rm -rf /tmp/test-install-03`

6. **If install.sh needs updates** (new sed patterns or validation), make the minimal targeted changes. If no changes needed, do not modify the file -- just confirm in the summary.
  </action>
  <verify>
    <automated>python3 -m pytest -x -q && mkdir -p /tmp/test-install-03/.claude && ./create-docs/install.sh --target /tmp/test-install-03/.claude 2>&1 | tail -10 && ls /tmp/test-install-03/.claude/create-docs/agents/scan-audience.md && rm -rf /tmp/test-install-03</automated>
  </verify>
  <done>All Python tests pass. install.sh completes without error. scan-audience.md is present in installed agents/ with placeholders resolved to absolute paths. Agent count in install summary is 8.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of scan pipeline</name>
  <files>create-docs/commands/create-docs-scan.md, create-docs/agents/scan-audience.md</files>
  <action>
Present the completed scan pipeline deliverables for user review. The command and agent are LLM prompts that cannot be unit-tested -- human verification is the appropriate quality gate.

**What was built:** Complete scan pipeline consisting of create-docs-scan.md command prompt and scan-audience.md subagent template. The command orchestrates a 6-step pipeline (orient, GSD context, staleness, notes, per-audience scan, merge) to produce docs-scan.json.

**How to verify:**

1. Review the scan command:
   ```
   cat create-docs/commands/create-docs-scan.md
   ```
   Verify it covers all 6 steps, uses correct script CLI flags, and addresses the pitfalls from research.

2. Review the scan-audience agent:
   ```
   cat create-docs/agents/scan-audience.md
   ```
   Verify it follows the Phase 2 agent pattern and produces output matching merge-scan.py expectations.

3. (Optional but recommended) Test on road-runner:
   - Install the tool: `./create-docs/install.sh --project ../road-runner`
   - Run the scan: In a Claude Code session on road-runner, invoke `/mg:create-docs-scan`
   - Verify `../road-runner/.mg/docs/docs-scan.json` is produced with valid structure

**Resume signal:** Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>test -f create-docs/commands/create-docs-scan.md && test -f create-docs/agents/scan-audience.md && echo "Both files exist"</automated>
  </verify>
  <done>User has reviewed the scan command and agent template and confirmed they are correct. Optionally, user has tested on road-runner and confirmed valid docs-scan.json output.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest -x -q` passes (full suite green)
- install.sh dry-run succeeds with correct agent count
- scan-audience.md installed with resolved paths
- User confirms command structure is correct
</verification>

<success_criteria>
- All existing tests still pass (no regressions)
- install.sh handles the new scan-audience agent without modification (or with minimal targeted updates)
- User has reviewed the scan command and agent template
- (Stretch) User has run `/mg:create-docs-scan` on road-runner and confirmed valid output
</success_criteria>

<output>
After completion, create `.planning/phases/03-scan-pipeline/03-02-SUMMARY.md`
</output>
