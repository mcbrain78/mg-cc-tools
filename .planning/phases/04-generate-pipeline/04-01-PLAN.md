---
phase: 04-generate-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - create-docs/commands/create-docs-generate.md
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - GEN-06
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08
  - DOC-09
  - DOC-10
  - DOC-11
  - DOC-12
  - DOC-13
  - CMD-03

must_haves:
  truths:
    - "Running /mg:create-docs-generate in initial mode orchestrates glossary-first, then 4 writers in parallel, then glossary reconciliation, then OVERVIEW.md last"
    - "Running /mg:create-docs-generate in update mode presents interactive staleness approval before regenerating only approved sections"
    - "Writer agents receive file paths (not content) and generate section-by-section following three-layer templates"
    - "Each generated section includes a docs-meta HTML comment with last-updated and sources"
    - "Each generated file starts with the auto-generation ownership header"
    - "Classified inbox notes appear in update approval flow, are expanded into prose at classified locations, and marked as integrated"
    - "Post-generation summary shows per-file section count, word count, and status"
  artifacts:
    - path: "create-docs/commands/create-docs-generate.md"
      provides: "Full generate pipeline orchestration command"
      min_lines: 350
      contains: "mg:create-docs-generate"
  key_links:
    - from: "create-docs/commands/create-docs-generate.md"
      to: "agents/glossary-writer.md"
      via: "Task tool spawn with pass=initial and pass=reconciliation"
      pattern: "glossary-writer"
    - from: "create-docs/commands/create-docs-generate.md"
      to: "agents/{audience}-writer.md"
      via: "Task tool spawn with documents, mode, update_sections parameters"
      pattern: "writer\\.md"
    - from: "create-docs/commands/create-docs-generate.md"
      to: ".mg/docs/docs-scan.json"
      via: "Read for source material index, staleness report, note classifications"
      pattern: "docs-scan\\.json"
    - from: "create-docs/commands/create-docs-generate.md"
      to: "docs/auto-doc/"
      via: "Writer agents write generated documents to this directory tree"
      pattern: "docs/auto-doc"
---

<objective>
Write the full `create-docs-generate.md` command -- the orchestration prompt that replaces the current stub with a complete generate pipeline covering initial mode, update mode with interactive approval, 4-stage generation pipeline, notes integration, and generation summary.

Purpose: This is the only pipeline step that writes documentation files. It wires together all Phase 1 scripts, Phase 2 templates/agents, and Phase 3 scan output into a working generation pipeline.

Output: A single command file (~400-500 lines) at `create-docs/commands/create-docs-generate.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-generate-pipeline/04-CONTEXT.md
@.planning/phases/04-generate-pipeline/04-RESEARCH.md

@create-docs/commands/create-docs-scan.md
@create-docs/agents/glossary-writer.md
@create-docs/agents/developer-writer.md
@create-docs/agents/end-user-writer.md
@create-docs/agents/agent-writer.md
@create-docs/agents/devops-writer.md
@create-docs/references/schema.md
@create-docs/references/.docs.config.json
@create-docs/install.sh

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From create-docs/commands/create-docs-generate.md (CURRENT STUB):
```markdown
---
name: mg:create-docs-generate
description: Generate or update audience-segmented documentation section-by-section
allowed-tools: Bash, Read, Write, Glob, Grep, Task
---

<!-- Content added in Phase 4 -->
```
NOTE: The stub's allowed-tools is missing AskUserQuestion. The command needs it for update mode approval flow. Add it to the frontmatter.

From all 4 writer agents (common Inputs interface):
```
- project_root: Absolute path to the project
- docs_dir: Absolute path to the output docs directory
- scan_data_path: Path to .mg/docs/docs-scan.json
- templates_dir: Path to {TEMPLATES_DIR}/{audience}/
- style_guide_path: Path to references/style-guide.md
- glossary_path: Path to the current GLOSSARY.md
- documents: List of document names from config
- mode: "initial" or "update"
- update_sections: (Update mode only) List of sections approved for regeneration
```

From glossary-writer.md Inputs:
```
- project_root, docs_dir, scan_data_path, glossary_template_path, style_guide_path
- mode: "initial" or "update"
- pass: "initial" or "reconciliation"
- term_proposals_dir: Path to .mg/docs/scan-logs/ where terms-{audience}.json files are
```

From docs-scan.json schema (top-level fields read by generate):
```json
{
  "project": "string",
  "scan_date": "string (ISO 8601)",
  "root_path": "string (absolute path)",
  "mode": "initial | update",
  "source_material_index": { "DOCUMENT/section-slug": { "source_files": [...], "relevance": "..." } },
  "staleness_report": [ { "document": "...", "section": "...", "severity": "high|medium|low", "reason": "...", "changed_files": [...] } ],
  "note_classifications": [ { "note_id": "...", "audience": "...", "document": "...", "section": "...", "confidence": 0.0, "expansion_outline": [...] } ],
  "gap_analysis": { "audience": { "undocumented_components": [...], "missing_topics": [...] } }
}
```

From .docs.config.json (default config structure):
```json
{
  "docs_dir": "docs/auto-doc",
  "audiences": {
    "end-users": { "enabled": true, "documents": ["USER_GUIDE"] },
    "developers": { "enabled": true, "documents": ["ARCHITECTURE", "DEVELOPER_GUIDE", "QUICK_REFERENCE"] },
    "agents": { "enabled": true, "documents": ["SYSTEM_MAP", "CONVENTIONS", "GOTCHAS", "TESTING"] },
    "devops": { "enabled": true, "documents": ["OPERATIONS", "TROUBLESHOOTING"] }
  },
  "shared_documents": ["OVERVIEW", "GLOSSARY"],
  "gsd_integration": true
}
```

From create-docs-scan.md (pattern reference for command structure):
- YAML frontmatter with name, description, allowed-tools
- Role statement ("You are the Scanner -- step N of...")
- "Before You Start" section reading schema
- Step-by-step Process with clear stage labels
- Key Formats Reference section
- Important Principles section at the end
- 349 lines total

From codebase-health scan command (subagent delegation pattern):
- Read agent file, paste contents into Task prompt
- Pass runtime parameters as named fields after agent instructions
- Spawn multiple Task calls in parallel for independent audiences

Output directory structure:
```
docs/auto-doc/
  OVERVIEW.md          # DOC-01 (shared, generated last)
  GLOSSARY.md          # DOC-02 (shared, generated first + reconciled)
  end-users/
    USER_GUIDE.md      # DOC-03
  developers/
    ARCHITECTURE.md    # DOC-05
    DEVELOPER_GUIDE.md # DOC-06
    QUICK_REFERENCE.md # DOC-07
  agents/
    SYSTEM_MAP.md      # DOC-08
    CONVENTIONS.md     # DOC-09
    GOTCHAS.md         # DOC-10
    TESTING.md         # DOC-11
  devops/
    OPERATIONS.md      # DOC-12
    TROUBLESHOOTING.md # DOC-13
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write initial mode flow (Steps 1-3, Stages 1-4)</name>
  <files>create-docs/commands/create-docs-generate.md</files>
  <action>
Replace the stub content with the full generate command. This task builds the initial mode flow -- the core generation pipeline that creates all docs from scratch.

**Frontmatter:** Keep existing name and description. Update allowed-tools to add AskUserQuestion (needed for update mode approval in Task 2): `allowed-tools: Bash, Read, Write, Glob, Grep, Task, AskUserQuestion`

**Structure the command following the create-docs-scan.md pattern:**

1. **Role statement** -- "You are the Generator -- step 2 of a 3-step documentation pipeline (scan, generate, verify). Your job is to create or update audience-segmented documentation using templates and scan results. You are the ONLY pipeline step that writes documentation files."

2. **Before You Start** -- Read the shared schema at `references/schema.md`.

3. **Step 1: Load Context and Detect Mode**
   - Read `.mg/docs/.docs.config.json` (fall back to `{GLOBAL_CONFIG}`)
   - Extract docs_dir, audiences, shared_documents
   - Read `.mg/docs/docs-scan.json` -- abort if missing ("Run /mg:create-docs-scan first")
   - Extract project_root from scan data's root_path
   - Detect mode: if `{docs_dir}` contains `.md` files -> "update", else -> "initial"
   - Cross-check with scan data mode field

4. **Step 2: Update Approval Flow (update mode only)**
   - If mode is "initial", skip entirely to Step 3
   - [Placeholder section -- Task 2 fills this in]

5. **Step 3: Prepare Workspace**
   - Create output directories: `mkdir -p {docs_dir}/end-users {docs_dir}/developers {docs_dir}/agents {docs_dir}/devops` (Pitfall 2 from research)
   - Clean stale term proposals: `rm -f {project_root}/.mg/docs/scan-logs/terms-*.json` (Pitfall 7)
   - Clean glossary reconciliation log: `rm -f {project_root}/.mg/docs/scan-logs/glossary-reconciliation.log`

6. **Stage 1: Build Glossary (initial pass)**
   - Print progress: "Building glossary (initial pass)..."
   - Read `agents/glossary-writer.md` (full contents)
   - Spawn single Task call with glossary-writer agent instructions pasted, runtime parameters: project_root, docs_dir, scan_data_path (.mg/docs/docs-scan.json), glossary_template_path ({TEMPLATES_DIR}/GLOSSARY.template.md), style_guide_path (references/style-guide.md), mode, pass="initial", term_proposals_dir (.mg/docs/scan-logs/)
   - Wait for completion. Verify {docs_dir}/GLOSSARY.md was created.

7. **Stage 2: Write Audience Documents (parallel)**
   - Print progress: "Writing audience documents (4 agents in parallel)..."
   - For each enabled audience in config, read the corresponding `agents/{audience}-writer.md` file
   - In update mode: only spawn agents that have approved sections (skip audiences with no approved sections to save subagent cost -- open question 3 from research)
   - Spawn one Task call per enabled audience in a SINGLE message (parallel execution):
     - Paste full agent file contents
     - Pass: project_root, docs_dir, scan_data_path, templates_dir ({TEMPLATES_DIR}/{audience}/), style_guide_path, glossary_path ({docs_dir}/GLOSSARY.md), documents (from config), mode, update_sections (empty list for initial mode)
   - After all complete, verify output files exist in each audience subdirectory

8. **Stage 3: Reconcile Glossary**
   - Print progress: "Reconciling glossary terms..."
   - Read `agents/glossary-writer.md` again
   - Spawn Task with pass="reconciliation", same parameters as Stage 1 but pass changed
   - Wait for completion

9. **Stage 4: Generate OVERVIEW.md**
   - Print progress: "Generating OVERVIEW.md..."
   - Generate OVERVIEW.md INLINE (not via subagent -- per research open question 1 recommendation): the orchestrator already has context of what was generated
   - Read the OVERVIEW.template.md from {TEMPLATES_DIR}/OVERVIEW.template.md
   - For each audience directory, list the generated documents and their first H1 heading
   - Generate OVERVIEW.md following the template structure: project intro, audience guide with routing table, quick links
   - Add file ownership header and docs-meta comment
   - Write to {docs_dir}/OVERVIEW.md

**Key patterns to follow throughout:**
- File ownership header at top of every generated file: `<!-- This file is auto-generated by /mg:create-docs. To add content, use /mg:add-docs. Manual edits may be overwritten. -->` (GEN-04, before any other content)
- docs-meta HTML comment after each section heading: `<!-- docs-meta: last-updated: YYYY-MM-DD, sources: [file1.py, file2.py] -->` (GEN-04)
- Agent prompts include file PATHS, not content (locked decision) -- but agent instructions themselves ARE pasted per codebase-health pattern
- Agents read files themselves via Read tool

**Important: do NOT include the update mode approval flow yet** (Step 2 content). Leave a clear placeholder comment: `<!-- UPDATE APPROVAL FLOW: Added by Task 2 -->`. Task 2 fills this.

Write approximately 250-300 lines covering the initial mode flow, all 4 stages, and the command structure.
  </action>
  <verify>
    <automated>test -f create-docs/commands/create-docs-generate.md && wc -l create-docs/commands/create-docs-generate.md | awk '{if ($1 >= 200) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>create-docs-generate.md contains full initial mode pipeline: load context, prepare workspace, 4-stage generation (glossary initial -> parallel writers -> glossary reconciliation -> OVERVIEW.md), with correct frontmatter including AskUserQuestion in allowed-tools</done>
</task>

<task type="auto">
  <name>Task 2: Add update mode approval flow, notes integration, and generation summary</name>
  <files>create-docs/commands/create-docs-generate.md</files>
  <action>
Fill in the remaining sections of create-docs-generate.md: update mode interactive approval (Step 2), notes integration (Step 4), and generation summary (Step 5). Also add Key Formats Reference and Important Principles sections at the end.

**Step 2: Update Approval Flow (update mode only)**

Replace the placeholder comment with the full interactive approval flow using AskUserQuestion tool:

1. Parse staleness_report from docs-scan.json. Group by document name, count sections per severity.
2. Parse note_classifications from docs-scan.json. Filter for pending notes (status != "integrated").
3. Present Level 1 overview via AskUserQuestion:
   ```
   Staleness Report:
     ARCHITECTURE.md:    3 stale sections (2 high, 1 medium)
     DEVELOPER_GUIDE.md: 1 stale section (1 low)
   Inbox notes: 2 pending
   ```
   Options: "Approve all" | "Select by document" | "Select by severity" | "Cancel"
4. If "Cancel": exit with message "No sections approved. Run again when ready."
5. If "Approve all": approve all stale sections + all pending notes
6. If "Select by document": for each document with stale sections, use AskUserQuestion to show section details (section name, reason, changed files, severity) and ask which to approve
7. If "Select by severity": let user pick a minimum severity level (e.g., "high only", "high + medium", "all"). Approve all sections at or above that level.
8. After staleness approval, present pending inbox notes as a separate group (same approve-all / per-note pattern)
9. Build the approved_sections dict: { audience: [section_slugs] } for passing to writer agents
10. Normalize section identifiers to slug format (Pitfall 5 from research: template headings are Title Case, source_material_index uses lowercased-hyphenated slugs)

**Step 4: Notes Integration**

After all generation stages complete (after Stage 4):
1. If notes were approved and integrated by writer agents during generation:
   - Read notes-inbox.json
   - For each integrated note, update status to "integrated" and add "integrated_date" field
   - Write the full updated inbox back using the Write tool (single atomic write per Pitfall 4)

**Step 5: Summary and Next Steps**

1. Collect stats for each generated/updated file: count sections (## headings), count words, status (Generated/Updated/Unchanged)
2. Present as a table:
   ```
   | File | Sections | Words | Status |
   |------|----------|-------|--------|
   | GLOSSARY.md | 5 | 820 | Generated |
   ...
   ```
3. Show totals: total files, total sections, total words
4. If notes were integrated: show count and note IDs
5. If glossary reconciliation added terms: show count
6. Suggest next step: "Run `/mg:create-docs-verify` to check reference integrity and consistency."

**Key Formats Reference section** (after Step 5):
- File ownership header format (exact string)
- docs-meta HTML comment format (exact string)
- Output directory layout (docs/auto-doc/ tree)
- Source material index key format (DOCUMENT/section-slug)

**Important Principles section** (at the end, matching scan command's pattern):
- Agents receive file paths only; they read files themselves (locked decision)
- Agent instructions ARE pasted into Task prompts (codebase-health pattern)
- Create all output directories before spawning writers (Pitfall 2)
- Clean terms-*.json before each generation run (Pitfall 7)
- OVERVIEW.md is always generated last (Pitfall 6)
- Glossary runs first even on update mode -- catches new terms from updated scan data
- Only spawn writer agents for audiences with approved sections in update mode
- File ownership header goes at the very TOP of every generated file, before DIATAXIS/AUDIENCE comments (Pitfall 8)
- No memory of rejections between runs -- rejected sections reappear on next update run

The final command file should be 400-500 lines total (both tasks combined).
  </action>
  <verify>
    <automated>wc -l create-docs/commands/create-docs-generate.md | awk '{if ($1 >= 350) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}' && grep -c 'AskUserQuestion' create-docs/commands/create-docs-generate.md | awk '{if ($1 >= 2) print "PASS: AskUserQuestion found " $1 " times"; else print "FAIL: AskUserQuestion not found enough"}'</automated>
  </verify>
  <done>create-docs-generate.md is complete with both initial and update mode flows, interactive approval via AskUserQuestion, notes integration with status tracking, generation summary table, key formats reference, and important principles. File is 350-500 lines total.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `create-docs-generate.md` exists and is 350-500 lines
2. Frontmatter includes `allowed-tools: Bash, Read, Write, Glob, Grep, Task, AskUserQuestion`
3. Command covers all 4 stages: glossary initial, parallel writers, glossary reconciliation, OVERVIEW.md
4. Update mode has interactive approval flow with AskUserQuestion
5. Notes integration marks notes as "integrated" in inbox
6. Post-generation summary with per-file stats
7. File ownership header and docs-meta format documented
8. Important Principles section present
9. Existing test suite still passes: `python3 -m pytest -x -q`
</verification>

<success_criteria>
- create-docs-generate.md replaces stub with full orchestration command (350+ lines)
- All 20 requirement IDs are addressed: GEN-01 through GEN-06, DOC-01 through DOC-13, CMD-03
- Command follows create-docs-scan.md structural pattern (frontmatter, role, before you start, process steps, reference, principles)
- All locked decisions from CONTEXT.md are implemented (section-by-section generation, file paths not content, 4-stage order, interactive approval, docs-meta format, ownership header)
- No deferred features included (no DOC-04 custom documents)
</success_criteria>

<output>
After completion, create `.planning/phases/04-generate-pipeline/04-01-SUMMARY.md`
</output>
