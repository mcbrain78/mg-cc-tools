---
phase: 04-generate-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - GEN-01
  - GEN-02
  - GEN-04
  - GEN-05
  - GEN-06
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08
  - DOC-09
  - DOC-10
  - DOC-11
  - DOC-12
  - DOC-13
  - CMD-03

must_haves:
  truths:
    - "Existing test suite passes with no regressions after command file change"
    - "install.sh copies the updated create-docs-generate.md and resolves all path placeholders correctly"
    - "Running /mg:create-docs-generate on road-runner produces a valid docs/auto-doc/ directory with 12 document files"
    - "Generated documents contain docs-meta HTML comments and file ownership headers"
    - "GLOSSARY.md exists at docs/auto-doc/ root, audience docs are in their subdirectories"
    - "OVERVIEW.md contains accurate routing to all audience-specific documents"
  artifacts: []
  key_links:
    - from: "create-docs/commands/create-docs-generate.md"
      to: "create-docs/install.sh"
      via: "install.sh COMMANDS array already includes create-docs-generate"
      pattern: "create-docs-generate"
---

<objective>
Validate the generate pipeline end-to-end: regression test existing scripts, verify install.sh integration (no changes needed), and smoke test on road-runner to confirm the command produces valid audience-segmented documentation.

Purpose: This mirrors the Phase 3 validation pattern (03-02). The generate command is an LLM prompt file that cannot be unit-tested -- the quality gate is real-project execution.

Output: Validation results confirming the generate pipeline works on road-runner.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-generate-pipeline/04-CONTEXT.md
@.planning/phases/03-scan-pipeline/03-02-SUMMARY.md

@create-docs/commands/create-docs-generate.md
@create-docs/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate install.sh integration and run test suite</name>
  <files></files>
  <action>
Verify the generate command integrates with the existing installation pipeline. No file changes expected.

1. **Confirm install.sh COMMANDS array includes create-docs-generate.** Read install.sh and verify `create-docs-generate` is in the COMMANDS list. It should already be there from Phase 1.

2. **Verify sed resolution covers generate command placeholders.** Check that every placeholder used in create-docs-generate.md (`references/schema.md`, `references/style-guide.md`, `{GLOBAL_CONFIG}`, `{SCRIPTS_DIR}`, `{TEMPLATES_DIR}`, `agents/`) has a corresponding sed replacement in install.sh. These should all be covered by the existing resolution loop.

3. **Dry-run install to /tmp:**
   ```bash
   bash create-docs/install.sh --target /tmp/test-create-docs-install
   ```
   Verify:
   - create-docs-generate.md was copied to `/tmp/test-create-docs-install/commands/mg/`
   - All path placeholders are resolved (no remaining `{SCRIPTS_DIR}`, `{TEMPLATES_DIR}`, `agents/` literals)
   - The file contains absolute paths where placeholders were

4. **Run full test suite:**
   ```bash
   python3 -m pytest -x -q
   ```
   All tests must pass. This is a regression guard -- Phase 4 created no new Python scripts, but the existing suite must remain green.

5. **Clean up:**
   ```bash
   rm -rf /tmp/test-create-docs-install
   ```
  </action>
  <verify>
    <automated>python3 -m pytest -x -q</automated>
  </verify>
  <done>install.sh copies and resolves create-docs-generate.md correctly. Full test suite passes (59+ tests). No regressions from Phase 4 changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of generate pipeline on road-runner</name>
  <files>create-docs/commands/create-docs-generate.md</files>
  <action>
Present the completed generate pipeline for user review and road-runner validation. The command is an LLM prompt file that cannot be unit-tested -- human verification on a real project is the required quality gate per the cross-cutting decision that road-runner validation is baked into phase success criteria.

**What was built:** Complete generate pipeline command (create-docs-generate.md) that orchestrates glossary writer + 4 audience writers + glossary reconciliation + OVERVIEW.md generation using scan data from Phase 3.

**Prerequisites:** road-runner must have been scanned already (docs-scan.json should exist from Phase 3 testing). If not, run `/mg:create-docs-scan` on road-runner first.

**How to verify -- Test initial mode:**

1. Install the updated tool into road-runner:
   ```bash
   cd /path/to/mg-cc-tools
   bash create-docs/install.sh --project ../road-runner
   ```

2. In a Claude Code session on road-runner, run:
   ```
   /mg:create-docs-generate
   ```

3. Verify the command:
   - Detects "initial" mode (no existing docs)
   - Creates `docs/auto-doc/` directory tree with subdirectories: `end-users/`, `developers/`, `agents/`, `devops/`
   - Runs glossary agent first, then 4 writers in parallel, then glossary reconciliation, then OVERVIEW.md
   - Shows progress updates for each stage
   - Shows generation summary table with file names, section counts, and word counts

4. Check generated files:
   - `docs/auto-doc/GLOSSARY.md` exists with domain terms
   - `docs/auto-doc/OVERVIEW.md` exists with audience routing table
   - `docs/auto-doc/end-users/USER_GUIDE.md` exists
   - `docs/auto-doc/developers/ARCHITECTURE.md` exists
   - `docs/auto-doc/developers/DEVELOPER_GUIDE.md` exists
   - `docs/auto-doc/developers/QUICK_REFERENCE.md` exists
   - `docs/auto-doc/agents/SYSTEM_MAP.md` exists
   - `docs/auto-doc/agents/CONVENTIONS.md` exists
   - `docs/auto-doc/agents/GOTCHAS.md` exists
   - `docs/auto-doc/agents/TESTING.md` exists
   - `docs/auto-doc/devops/OPERATIONS.md` exists
   - `docs/auto-doc/devops/TROUBLESHOOTING.md` exists

5. Spot-check quality:
   - Open any 2 generated files and verify they have the ownership header at the top: `<!-- This file is auto-generated by /mg:create-docs ...`
   - Verify docs-meta comments appear after section headings: `<!-- docs-meta: last-updated: ..., sources: [...] -->`
   - Verify content references actual road-runner source files and components (not generic examples)
   - Verify OVERVIEW.md links to actual generated audience documents

6. After completion: suggest `/mg:create-docs-verify` as next step should appear in output.

**Resume signal:** Type "approved" if generate pipeline produced valid documentation, or describe issues found.
  </action>
  <verify>
    <automated>test -f create-docs/commands/create-docs-generate.md && echo "Command file exists"</automated>
  </verify>
  <done>User has run /mg:create-docs-generate on road-runner and confirmed: 12 document files generated across 4 audience directories + 2 shared files, with ownership headers, docs-meta comments, and real source material references. OVERVIEW.md accurately routes to all audience documents.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Test suite: all existing tests pass (regression guard)
2. install.sh: dry-run confirms path resolution works for generate command
3. Road-runner: 12 document files generated across 4 audience directories + 2 shared files
4. Quality: ownership headers, docs-meta comments, real source material references
</verification>

<success_criteria>
- Test suite: 59+ tests passing, zero failures
- install.sh: create-docs-generate.md copied and all placeholders resolved
- Road-runner: 12 documents generated with correct directory structure
- Each generated file has ownership header and docs-meta section comments
- OVERVIEW.md accurately routes to all generated audience documents
- Generation summary table displayed after completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-generate-pipeline/04-02-SUMMARY.md`
</output>
