---
phase: 07-install-command
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified: []
autonomous: false

requirements: [INST-06, INST-09, INST-10, INST-11]

must_haves:
  truths:
    - "Bootstrap install.sh copies install.md into mg-cc-tools .claude/commands/mg/"
    - "Modified install.sh scripts successfully install tools AND update the manifest"
    - "mg-install-lib.py scan-status correctly reports tool states on a real project"
    - "Full pytest suite passes with no regressions"
  artifacts: []
  key_links:
    - from: "install/install.sh"
      to: ".claude/commands/mg/install.md"
      via: "bootstrap copy"
      pattern: "install\\.md"
    - from: "*/install.sh"
      to: ".claude/mg-cc-tools.manifest.json"
      via: "manifest update on real install"
      pattern: "mg-cc-tools\\.manifest\\.json"
---

<objective>
Validate the complete install system end-to-end: bootstrap the installer, run a modified install.sh on a test target, verify manifest creation, and confirm full test suite passes.

Purpose: This is the integration checkpoint that confirms all the pieces (mg-install-lib.py, tool.toml files, install.md, modified install.sh scripts) work together correctly on a real project.

Output: Validated install system ready for production use.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-install-command/07-CONTEXT.md

@.planning/phases/07-install-command/07-01-SUMMARY.md
@.planning/phases/07-install-command/07-02-SUMMARY.md
@.planning/phases/07-install-command/07-03-SUMMARY.md
@.planning/phases/07-install-command/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated integration validation</name>
  <files></files>
  <action>
    Run a comprehensive automated validation sequence:

    1. **Full test suite**: `python3 -m pytest -x -v` -- all existing tests plus new install tests must pass
    2. **Bootstrap test**: Run `bash install/install.sh --project` from mg-cc-tools to self-install, verify `.claude/commands/mg/install.md` is created
    3. **Install.sh integration test**: Create a temp directory, run `bash debug-triage/install.sh --target <tmp>/.claude`, verify:
       - Command file exists at `<tmp>/.claude/commands/mg/debug-triage.md`
       - Manifest exists at `<tmp>/.claude/mg-cc-tools.manifest.json`
       - Manifest contains debug-triage tool entry with version, installed_at, commands, source_checksums
    4. **Scan-status test on temp target**: Run `mg-install-lib.py scan-status` on the temp target from step 3, verify debug-triage shows as "current"
    5. **Lint check**: `ruff check install/scripts/mg-install-lib.py` passes
    6. **All tool.toml parseable**: Quick validation that all 12 tool.toml files parse via tomllib

    Clean up temp directories after validation.

    If any validation fails, diagnose and fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>python3 -m pytest -x -v && echo "SUITE PASSED"</automated>
  </verify>
  <done>
    - Full pytest suite passes (all existing + new install tests)
    - Bootstrap installs install.md into mg-cc-tools .claude/
    - Modified install.sh creates manifest entries
    - scan-status reports correct states after real install
    - No regressions in existing tools
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Road-runner end-to-end validation</name>
  <files></files>
  <action>
    Present the human-verify checkpoint for manual validation of /mg:install on a real project.
  </action>
  <verify>User approves the interactive flow on road-runner</verify>
  <done>User confirms /mg:install works end-to-end on road-runner</done>
  <what-built>
    Complete /mg:install system: mg-install-lib.py (5 subcommands), tool.toml metadata (12 files), install.md slash command, bootstrap install.sh, and 11 modified install.sh scripts with manifest update calls.
  </what-built>
  <how-to-verify>
    1. Open Claude Code in mg-cc-tools directory
    2. Run `/mg:install` (should bootstrap first if not already done: `./install/install.sh --project`)
    3. When prompted for target, select road-runner project
    4. Observe: status table should show tools (some as "Available" since road-runner has pre-manifest installs, migration/adopt should be offered)
    5. If migration offered, accept it -- should detect existing installed tools
    6. Select one tool to install or update
    7. Observe: preflight checks run, capability probe runs, install executes, validation runs, summary shown
    8. Verify manifest created at road-runner's `.claude/mg-cc-tools.manifest.json`
    9. Re-run `/mg:install` on road-runner -- previously installed tools should show as "Current"

    Key things to verify:
    - Status table renders correctly with all columns
    - Numbered prompt works (not AskUserQuestion) for action selection
    - LSP probe runs from road-runner directory (not mg-cc-tools)
    - Manifest contains correct tool entries with checksums
    - No unresolved {PLACEHOLDER} patterns in installed files
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest -x -v` -- full suite passes
- Bootstrap `install/install.sh --project` creates `.claude/commands/mg/install.md`
- Running a modified install.sh creates manifest entry in target
- Manual road-runner validation confirms interactive flow works end-to-end
</verification>

<success_criteria>
All automated tests pass. Bootstrap works. Modified install.sh scripts create manifests. User confirms /mg:install interactive flow works correctly on road-runner with status table, action selection, preflight, probe, install, validation, and summary.
</success_criteria>

<output>
After completion, create `.planning/phases/07-install-command/07-05-SUMMARY.md`
</output>
