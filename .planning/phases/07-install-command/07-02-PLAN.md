---
phase: 07-install-command
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - codebase-health/tool.toml
  - create-docs/tool.toml
  - create-context/tool.toml
  - data-provider/tool.toml
  - debug-triage/tool.toml
  - gsd-patches/tool.toml
  - mg-gsd-wrappers/tool.toml
  - new-milestone-gsd/tool.toml
  - update-backlog/tool.toml
  - permission-hooks/tool.toml
  - cc-regression-test/tool.toml
  - install/tool.toml
autonomous: true
requirements: [INST-02]

must_haves:
  truths:
    - "Every tool directory has a tool.toml with required tool.description field"
    - "Excluded tools (install, cc-regression-test) have exclude = true"
    - "Tools with Python scripts declare python3 in preflight.required"
    - "GSD extension tools declare gsd in preflight.required"
    - "codebase-health declares ruff, vulture, pyright, jscpd as preflight.optional"
    - "create-docs declares lsp as preflight.optional"
  artifacts:
    - path: "codebase-health/tool.toml"
      provides: "Complex tool metadata with optional preflight checks"
      contains: "preflight"
    - path: "debug-triage/tool.toml"
      provides: "Simple GSD tool metadata"
      contains: "[tool]"
    - path: "install/tool.toml"
      provides: "Excluded meta-tool metadata"
      contains: "exclude = true"
  key_links:
    - from: "*/tool.toml"
      to: "install/scripts/mg-install-lib.py"
      via: "tomllib parsing during tool discovery"
      pattern: "tomllib\\.loads?"
---

<objective>
Create tool.toml metadata files for all 12 tool directories (11 existing + new install directory).

Purpose: tool.toml files are the metadata contract that mg-install-lib.py reads for tool discovery, description display, and preflight check aggregation. Without these, no tool can be discovered by the installer.

Output: 12 tool.toml files, one per tool directory.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-install-command/07-CONTEXT.md
@.planning/phases/07-install-command/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tool.toml for all 12 tool directories</name>
  <files>
    codebase-health/tool.toml,
    create-docs/tool.toml,
    create-context/tool.toml,
    data-provider/tool.toml,
    debug-triage/tool.toml,
    gsd-patches/tool.toml,
    mg-gsd-wrappers/tool.toml,
    new-milestone-gsd/tool.toml,
    update-backlog/tool.toml,
    permission-hooks/tool.toml,
    cc-regression-test/tool.toml,
    install/tool.toml
  </files>
  <action>
    Create the `install/` directory if it does not exist (mkdir -p install/commands install/scripts).

    Create tool.toml for each tool with the exact schema per the RESEARCH.md specification. Use these values (from CONTEXT.md/RESEARCH.md):

    | Tool | description | exclude | preflight.required | preflight.optional |
    |------|-------------|---------|-------------------|--------------------|
    | codebase-health | "Scan, verify, and fix code health issues" | false | ["python3"] | ["ruff", "vulture", "pyright", "jscpd"] |
    | create-docs | "Documentation pipeline (scan, generate, verify)" | false | ["python3"] | ["lsp"] |
    | create-context | "Create context snapshots for LLM prompts" | false | ["gsd"] | [] |
    | data-provider | "Research and map external data field sources" | false | ["python3"] | [] |
    | debug-triage | "GSD debug workflow with structured triage" | false | ["gsd"] | [] |
    | gsd-patches | "Apply GSD methodology patches" | false | ["gsd"] | [] |
    | mg-gsd-wrappers | "GSD workflow wrappers (plan, execute, discuss)" | false | ["gsd"] | [] |
    | new-milestone-gsd | "Create new GSD milestone with backlog review" | false | ["gsd"] | [] |
    | update-backlog | "Update GSD backlog after milestone completion" | false | ["gsd"] | [] |
    | permission-hooks | "Claude Code permission guard hooks" | false | ["python3"] | [] |
    | cc-regression-test | "Claude Code regression test harness (internal)" | true | ["python3"] | [] |
    | install | "mg-cc-tools installer (internal)" | true | ["python3"] | [] |

    TOML format per CONTEXT.md specification:
    ```toml
    [tool]
    description = "..."
    # only include exclude if true (omit for false/default)

    [preflight]
    required = [...]
    # only include optional if non-empty
    ```

    Omit `exclude = false` (it's the default). Omit `[preflight]` section entirely if both arrays would be empty. Omit `optional` key if array would be empty.

    For create-context, gsd needs to be checked. create-context currently has no python3 check in its install.sh and no Python scripts of its own, but it does require GSD to be installed in the target project for its context-template.snapshot to work properly.

    Also update pyproject.toml: change `requires-python = ">=3.8"` to `requires-python = ">=3.11"` per Pitfall 4 (tomllib needs 3.11+).
  </action>
  <verify>
    <automated>python3 -c "
import tomllib, os, sys
tools = ['codebase-health','create-docs','create-context','data-provider','debug-triage',
         'gsd-patches','mg-gsd-wrappers','new-milestone-gsd','update-backlog',
         'permission-hooks','cc-regression-test','install']
errors = []
for t in tools:
    path = f'{t}/tool.toml'
    if not os.path.isfile(path):
        errors.append(f'MISSING: {path}')
        continue
    with open(path, 'rb') as f:
        data = tomllib.load(f)
    if 'tool' not in data or 'description' not in data['tool']:
        errors.append(f'BAD SCHEMA: {path} missing tool.description')
if errors:
    print('\n'.join(errors)); sys.exit(1)
print(f'All {len(tools)} tool.toml files valid')
"
    </automated>
  </verify>
  <done>
    - All 12 tool directories contain a valid tool.toml
    - Each tool.toml has [tool] section with description string
    - Excluded tools (install, cc-regression-test) have exclude = true
    - Preflight requirements match the inventory from RESEARCH.md
    - pyproject.toml requires-python updated to >=3.11
  </done>
</task>

</tasks>

<verification>
- All 12 tool.toml files exist and parse without error via tomllib
- `python3 -c "import tomllib; [tomllib.load(open(f'{t}/tool.toml','rb')) for t in ['codebase-health','create-docs','install','cc-regression-test']]"` succeeds
- Excluded tools have exclude = true; regular tools do not have exclude field
- pyproject.toml shows requires-python = ">=3.11"
</verification>

<success_criteria>
All 12 tool.toml files created with correct metadata. pyproject.toml updated. tomllib can parse every file without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-install-command/07-02-SUMMARY.md`
</output>
