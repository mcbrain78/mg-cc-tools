---
phase: 20-recursive-pipeline-script-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auto-doc/scripts/verify-xml-refs.py
  - auto-doc/scripts/prepare-prose-verify.py
  - auto-doc/scripts/load-audit-findings.py
  - auto-doc/scripts/tests/test_verify_xml_refs.py
  - auto-doc/scripts/tests/test_prepare_prose_verify.py
  - auto-doc/scripts/tests/test_load_audit_findings.py
autonomous: true
requirements: [RPS-01, RPS-02, RPS-03]

must_haves:
  truths:
    - "verify-xml-refs.py recursively visits nested sections and produces findings with slash-separated section paths"
    - "prepare-prose-verify.py produces output for every section at any depth, in nested directories mirroring the tree"
    - "load-audit-findings.py deduplicates findings that use slash-separated paths without any structural changes"
  artifacts:
    - path: "auto-doc/scripts/verify-xml-refs.py"
      provides: "Recursive ref verification with path-based findings"
      contains: "walk_sections"
    - path: "auto-doc/scripts/prepare-prose-verify.py"
      provides: "Nested directory output for prose verification"
      contains: "walk_sections"
    - path: "auto-doc/scripts/load-audit-findings.py"
      provides: "Path-agnostic finding deduplication"
  key_links:
    - from: "auto-doc/scripts/verify-xml-refs.py"
      to: "auto-doc/scripts/lib/xml_doc.py"
      via: "walk_sections import"
      pattern: "from lib\\.xml_doc import.*walk_sections"
    - from: "auto-doc/scripts/prepare-prose-verify.py"
      to: "auto-doc/scripts/lib/xml_doc.py"
      via: "walk_sections import"
      pattern: "from lib\\.xml_doc import.*walk_sections"
---

<objective>
Migrate verify-xml-refs.py, prepare-prose-verify.py, and load-audit-findings.py from flat section iteration to recursive tree traversal using walk_sections().

Purpose: These three scripts iterate all sections for read-only analysis. Converting them to walk_sections() makes them depth-aware so findings and prose verification cover nested sections.

Output: Three updated scripts with tests, all using slash-separated paths for section identification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-recursive-pipeline-script-updates/20-CONTEXT.md
@.planning/phases/20-recursive-pipeline-script-updates/20-RESEARCH.md
@.planning/phases/18-recursive-section-xml-core/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from xml_doc.py (Phase 18). -->

From auto-doc/scripts/lib/xml_doc.py:
```python
def walk_sections(sections, prefix=""):
    """Yield (path, section_dict) for all sections in depth-first order.
    Top-level sections have bare slugs as paths.
    Nested sections have slash-separated paths (e.g., "parent/child").
    """

def _find_section_by_path(root, path):
    """Resolve a slash-separated section path to an XML element.
    Returns None if not found."""

def parse_xml_doc(path):
    """Parse XML document file into structured dict with nested 'sections' list.
    Each section has: slug, body, refs (list of flat dicts), children (list of sections)."""
```

From verify-xml-refs.py (current flat pattern to replace):
```python
# CURRENT (flat):
for section in doc["sections"]:
    slug = section["slug"]
    # ... check refs using slug

# AFTER (recursive):
from lib.xml_doc import walk_sections
for path, section in walk_sections(doc["sections"]):
    # path = "monitoring-alerting/etl-run-logging" for nested
    # ... check refs using path
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate verify-xml-refs.py and prepare-prose-verify.py to walk_sections</name>
  <files>
    auto-doc/scripts/verify-xml-refs.py,
    auto-doc/scripts/prepare-prose-verify.py,
    auto-doc/scripts/tests/test_verify_xml_refs.py,
    auto-doc/scripts/tests/test_prepare_prose_verify.py
  </files>
  <behavior>
    verify-xml-refs.py:
    - Test: verify_xml_file on nested XML produces findings with slash-separated "section" field (e.g., "monitoring-alerting/etl-run-logging")
    - Test: _make_finding group_id uses path (e.g., "OPERATIONS/monitoring-alerting/etl-run-logging")
    - Test: parent section with ref that only exists in child body correctly fails parent's audit
    - Test: main() doc_refs count includes refs from nested sections (not just top-level)

    prepare-prose-verify.py:
    - Test: prepare() on nested XML creates nested directories (e.g., output_dir/monitoring-alerting/etl-run-logging.json)
    - Test: section JSON includes "path" field alongside "slug"
    - Test: manifest "sections" lists slash-separated paths (not bare slugs)
    - Test: top-level sections still produce flat files (backward compatible for 1-level docs)
  </behavior>
  <action>
    **verify-xml-refs.py changes:**
    1. Add `walk_sections` to the import from `lib.xml_doc`
    2. In `verify_xml_file()`: replace `for section in doc["sections"]` with `for path, section in walk_sections(doc["sections"])`. Use `path` instead of `slug` in the `_make_finding()` call for the `section` parameter.
    3. In `main()`: replace `doc_refs = sum(len(s["refs"]) for s in doc["sections"])` with `doc_refs = sum(len(s["refs"]) for _, s in walk_sections(doc["sections"]))` so nested refs are counted.
    4. The `_make_finding` function's `group_id` field already uses `f"{document}/{section}"` -- since `section` is now a path, group_id naturally becomes `f"{document}/{path}"` (e.g., "OPS/monitoring-alerting/etl-run-logging"). Accept this format change per Pitfall 1.

    **prepare-prose-verify.py changes:**
    1. Add `walk_sections` to the import from `lib.xml_doc`
    2. In `prepare()`: replace `for section in doc["sections"]` with `for path, section in walk_sections(doc["sections"])`.
    3. Change output file writing to use nested directories: compute `parent_dir = os.path.join(output_dir, os.path.dirname(path))`, call `os.makedirs(parent_dir, exist_ok=True)`, write to `os.path.join(parent_dir, f"{slug}.json")`.
    4. Add `"path"` field to `section_data` dict alongside existing `"slug"`.
    5. Change `slugs` list to `paths` list, append `path` instead of `slug`.
    6. Update manifest `"sections"` to use `paths` list.
    7. Update return value and docstring to reflect paths instead of slugs.

    **Test updates:**
    For both test files, add nested XML fixtures with 2-3 level sections (parent with children) and write tests exercising the behaviors listed above. Use the same fixture patterns as test_xml_doc.py (build XML with children key in section dicts).
  </action>
  <verify>
    <automated>cd /home/mcbrain/mg_projects/mg-cc-tools && uv run python -m pytest auto-doc/scripts/tests/test_verify_xml_refs.py auto-doc/scripts/tests/test_prepare_prose_verify.py -x --tb=short -q --no-header</automated>
  </verify>
  <done>
    verify-xml-refs.py uses walk_sections for recursive iteration, findings have slash-separated paths in "section" field, nested ref counts are correct.
    prepare-prose-verify.py writes section JSON to nested directories, includes "path" field, manifest lists paths.
    All new and existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify load-audit-findings.py path compatibility and run full suite</name>
  <files>
    auto-doc/scripts/load-audit-findings.py,
    auto-doc/scripts/tests/test_load_audit_findings.py
  </files>
  <action>
    **load-audit-findings.py:** This script is already path-format-agnostic -- its deduplication key uses `f.get("section", "")` as a raw string, which works identically for bare slugs and slash-separated paths. Per research analysis, no structural changes are needed.

    Review the script to confirm no code references `"slug"` or assumes flat section names. The only change needed (if any) is adding a test that explicitly exercises deduplication with slash-separated paths to prove backward compatibility.

    **Test updates:**
    1. Add a test case to test_load_audit_findings.py that feeds findings with slash-separated section paths (e.g., `"section": "monitoring-alerting/etl-run-logging"`) and verifies deduplication works correctly.
    2. Add a test that mixes old-format (bare slug) and new-format (path) findings to confirm they are treated as distinct entries when their section values differ.

    **Full suite verification:** Run the complete auto-doc test suite to confirm no regressions from Task 1 changes.
  </action>
  <verify>
    <automated>cd /home/mcbrain/mg_projects/mg-cc-tools && uv run python -m pytest auto-doc/scripts/tests/test_load_audit_findings.py auto-doc/scripts/tests/ -x --tb=short -q --no-header</automated>
  </verify>
  <done>
    load-audit-findings.py confirmed path-agnostic with explicit test coverage for slash-separated paths.
    Full auto-doc test suite passes with no regressions.
  </done>
</task>

</tasks>

<verification>
Run the full auto-doc test suite:
```bash
cd /home/mcbrain/mg_projects/mg-cc-tools && uv run python -m pytest auto-doc/scripts/tests/ --tb=short -q --no-header
```
All tests pass including new nested section tests.
</verification>

<success_criteria>
- verify-xml-refs.py findings use slash-separated paths in "section" field at any depth
- verify-xml-refs.py recursively audits refs at every nesting level
- prepare-prose-verify.py creates nested output directories mirroring section tree
- prepare-prose-verify.py manifest lists paths, section JSON includes "path" field
- load-audit-findings.py handles slash-separated paths in deduplication (confirmed by test)
- All existing tests still pass (backward compatibility with flat sections)
</success_criteria>

<output>
After completion, create `.planning/phases/20-recursive-pipeline-script-updates/20-01-SUMMARY.md`
</output>
