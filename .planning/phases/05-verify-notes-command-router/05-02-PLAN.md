---
phase: 05-verify-notes-command-router
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - create-docs/commands/create-docs.md
autonomous: false
requirements:
  - CMD-01

must_haves:
  truths:
    - "Running /mg:create-docs detects pipeline state and routes to the correct next step"
    - "Router detects: no workspace, no scan, no docs, no verify report, pipeline complete"
    - "Router never runs pipeline steps itself -- it tells the user what command to run"
    - "Full pipeline (scan, generate, verify) can be run end-to-end on road-runner"
  artifacts:
    - path: "create-docs/commands/create-docs.md"
      provides: "Router command prompt with state detection and routing"
      min_lines: 100
  key_links:
    - from: "create-docs/commands/create-docs.md"
      to: ".mg/docs/docs-scan.json"
      via: "Filesystem existence check for state detection"
      pattern: "docs-scan\\.json"
    - from: "create-docs/commands/create-docs.md"
      to: "create-docs/commands/create-docs-verify.md"
      via: "Routes user to /mg:create-docs-verify"
      pattern: "create-docs-verify"
    - from: "create-docs/commands/create-docs.md"
      to: "create-docs/commands/add-docs.md"
      via: "Mentions /mg:add-docs in pipeline complete route"
      pattern: "add-docs"
---

<objective>
Write the create-docs router command and validate the complete pipeline end-to-end on road-runner.

Purpose: The router is the main entry point (/mg:create-docs) that detects pipeline state and guides users to the correct next step. The validation confirms all three commands (scan, generate, verify) work together as a complete pipeline. This plan depends on Plan 01 because the router references the verify and add-docs commands.

Output: Fully-written router command .md file, plus end-to-end validation of the complete pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verify-notes-command-router/05-CONTEXT.md
@.planning/phases/05-verify-notes-command-router/05-RESEARCH.md
@.planning/phases/05-verify-notes-command-router/05-01-SUMMARY.md

Read these files for pattern reference:
@codebase-health/commands/codebase-health.md (router pattern exemplar -- follow this closely)
@create-docs/commands/create-docs-scan.md (scan command -- router points to this)
@create-docs/commands/create-docs-generate.md (generate command -- router points to this)
@create-docs/commands/create-docs-verify.md (verify command from Plan 01 -- router points to this)

<interfaces>
<!-- Key state files the router checks for state detection -->

State detection checks (in order):
1. .mg/docs/ directory exists? NO -> Route A (fresh start)
2. .mg/docs/docs-scan.json exists? NO -> Route A (scan needed)
3. {docs_dir}/ contains .md files? NO -> Route B (generate needed)
4. .mg/docs/docs-verify-report.md exists? NO -> Route C (verify needed)
5. All exist -> Route D (pipeline complete)

Config file: .mg/docs/.docs.config.json (for docs_dir, default: docs/auto-doc)
Fallback config: {GLOBAL_CONFIG}

From create-docs/commands/create-docs.md (current stub):
```yaml
name: mg:create-docs
description: Documentation lifecycle router -- detects pipeline state and routes to correct step
allowed-tools: Bash, Read, Write, Glob, Grep
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write create-docs.md router command</name>
  <files>create-docs/commands/create-docs.md</files>
  <action>
Replace the stub content in create-docs.md with the full router command prompt. Keep the existing YAML frontmatter unchanged.

Follow the codebase-health/commands/codebase-health.md pattern precisely. The router detects state and routes -- it never runs pipeline steps itself.

**Role section:** You are the entry point for the 3-step documentation pipeline (scan, generate, verify). Your job is to assess the current state and guide the user to the right next step.

**How the Pipeline Works section:**
```
  SCAN ---------> GENERATE ---------> VERIFY
  (read-only)     (writes docs)       (read-only)
  analyzes code   creates/updates     checks quality
```
Three commands, always run in order:
1. /mg:create-docs-scan -- Scans the codebase and builds source material index. Read-only.
2. /mg:create-docs-generate -- Creates or updates audience-segmented documents section-by-section. The only step that writes documentation files.
3. /mg:create-docs-verify -- Checks reference integrity, cross-doc consistency, Diataxis compliance, completeness. Read-only.

Plus a companion command:
- /mg:add-docs -- Capture documentation notes to the inbox (standalone, runs independently of the pipeline).

**Your Task: Detect State and Route section:**

Check the project state and determine pipeline position. Load config first.

0. **Load configuration.** Read .mg/docs/.docs.config.json from the project root. If not found, read defaults from {GLOBAL_CONFIG}. Extract docs_dir (default: docs/auto-doc).

Run these checks IN ORDER:

1. **Does .mg/docs/ directory exist?**
   - NO -> Route A (fresh start)

2. **Does .mg/docs/docs-scan.json exist?**
   - NO -> Check if docs exist in {docs_dir}/. If docs exist but no scan data, this is "update mode needing a scan" -- suggest /mg:create-docs-scan (which will detect existing docs as update mode). If no docs either, Route A.

3. **Does {docs_dir}/ contain any .md files?**
   (Use Glob to check for .md files in the docs directory)
   - NO -> Route B (scan done, generation needed)

4. **Does .mg/docs/docs-verify-report.md exist?**
   - NO -> Route C (generation done, verification needed)

5. **All exist** -> Route D (pipeline complete)

**Additional state: partial scan detection.**
If docs-scan.json exists, read it and check for required top-level fields: project_model, source_material_index, gap_analysis. If any are missing, treat as incomplete scan and suggest re-running /mg:create-docs-scan.

**Route A: No scan yet (or fresh start)**
Present the pipeline overview:
```
This project hasn't been scanned for documentation yet.

The documentation pipeline creates audience-segmented docs in 3 steps:
  1. Scan    -- analyze code structure, tech stack, components
  2. Generate -- create docs for end-users, developers, agents, devops
  3. Verify   -- check references, consistency, completeness

All steps are guided. You review results between each step.

Ready to scan? Run:  /mg:create-docs-scan
```

**Route B: Scan complete, needs generation**
Read docs-scan.json and show a brief summary:
```
Scan complete -- ready to generate documentation.

  Tech stack:  {tech_stack items}
  Components:  {count}
  Entry points: {count}
  Source material entries: {count from source_material_index}

Review the scan data: .mg/docs/docs-scan.json

When ready, generate documentation:
  /mg:create-docs-generate
```

**Route C: Generation complete, needs verification**
Show docs summary:
```
Documentation generated -- ready for verification.

  Documents: {count .md files in docs_dir}
  Audiences: {list enabled audiences}

When ready, verify documentation quality:
  /mg:create-docs-verify
```

**Route D: Pipeline complete**
Read docs-verify-report.md and show a brief summary:
```
Pipeline complete -- last verification results:

  {severity summary from report}

Review the verification report: .mg/docs/docs-verify-report.md

Options:
  - Re-scan:   /mg:create-docs-scan      (re-analyze codebase for changes)
  - Re-verify: /mg:create-docs-verify     (re-check documentation quality)
  - Add notes: /mg:add-docs "your note"   (capture documentation notes)
```

**Important section:**
- Never run a pipeline step yourself. Detect state, show summary, tell user what command to run next.
- Always read the reports when they exist. Pull out key numbers so the user gets a useful snapshot.
- Be concise. This is a routing command, not an analysis.
- When docs exist but no scan data, suggest scan (not generate) -- the scan step handles update mode detection.

CRITICAL CONSTRAINTS:
- DO NOT modify any other files. Only create-docs/commands/create-docs.md.
- DO NOT modify install.sh. The router is already in the COMMANDS array.
- Use {GLOBAL_CONFIG} placeholder for default config path (resolved by install.sh).
  </action>
  <verify>
    <automated>python3 -m pytest -x -q</automated>
    Verify the file has: YAML frontmatter preserved, pipeline overview, 5 state detection checks, 4 routes (A/B/C/D), references to all 3 pipeline commands plus add-docs. Grep for: "create-docs-scan", "create-docs-generate", "create-docs-verify", "add-docs", "docs-scan.json", "docs-verify-report.md", "Route A", "Route B", "Route C", "Route D".
  </verify>
  <done>create-docs.md is a complete router command (100+ lines) that detects 5 pipeline states, routes to 4 destinations, reads reports for summaries, and never runs steps itself</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end pipeline validation on road-runner</name>
  <files>create-docs/commands/create-docs.md, create-docs/commands/create-docs-verify.md, create-docs/commands/add-docs.md</files>
  <action>
Present the completed Phase 5 deliverables for user review and road-runner validation. The commands are LLM prompts that cannot be unit-tested -- human verification on a real project is the required quality gate per the cross-cutting decision that road-runner validation is baked into phase success criteria.

**What was built:** Three command .md files completing the documentation pipeline:
- create-docs-verify.md: orchestrates verifier agent for 6 quality checks, uses LSP for symbol verification
- add-docs.md: standalone note capture with auto-classification and user correction
- create-docs.md: router that detects pipeline state and guides to next step

**How to verify:**

1. Install the tool to road-runner:
   ```bash
   cd /home/mcbrain/mg_projects/mg-cc-tools
   ./create-docs/install.sh --project /home/mcbrain/mg_projects/road-runner
   ```
   Verify: all 5 commands installed, no sed resolution errors

2. Test the router (fresh state):
   In a Claude Code session on road-runner, invoke `/mg:create-docs`
   Verify: shows Route A (no scan yet), lists pipeline overview, suggests /mg:create-docs-scan

3. Run the full pipeline:
   ```
   /mg:create-docs-scan
   /mg:create-docs-generate
   /mg:create-docs-verify
   ```
   Verify:
   - Scan produces docs-scan.json with project_model, source_material_index, gap_analysis
   - Generate creates docs in docs/auto-doc/ across audience subdirectories
   - Verify produces .mg/docs/docs-verify-report.md with severity-categorized issues

4. Test the router (pipeline complete state):
   Invoke `/mg:create-docs` again
   Verify: shows Route D (pipeline complete), severity summary from report, re-run options

5. Test add-docs:
   Invoke `/mg:add-docs "Document the authentication flow"`
   Verify: note added to inbox, classification displayed, correction option offered

**Resume signal:** Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>test -f create-docs/commands/create-docs.md && test -f create-docs/commands/create-docs-verify.md && test -f create-docs/commands/add-docs.md && echo "All 3 command files exist"</automated>
  </verify>
  <done>User has reviewed all 3 command files and confirmed correctness. User has run the full pipeline (scan, generate, verify) on road-runner and confirmed valid output. User has tested /mg:create-docs router in fresh and complete states. User has tested /mg:add-docs with note capture and classification.</done>
</task>

</tasks>

<verification>
1. All 3 command stub files are now complete (not just frontmatter + comment)
2. create-docs.md follows codebase-health.md router pattern (state detection + routing)
3. Full pipeline runs end-to-end on road-runner producing docs and verify report
4. install.sh deploys all 5 commands without errors
5. Test suite still green: `python3 -m pytest -x`
</verification>

<success_criteria>
- create-docs.md is a complete router command that detects 5 states and routes correctly
- Full pipeline (scan, generate, verify) produces usable documentation on road-runner
- /mg:add-docs captures notes with classification and correction UX
- All existing tests pass (59+ tests)
- No files outside create-docs/commands/ were modified
</success_criteria>

<output>
After completion, create `.planning/phases/05-verify-notes-command-router/05-02-SUMMARY.md`
</output>
