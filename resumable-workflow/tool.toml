[tool]
description = "Resumable dynamic investigation loop — decompose, research, verify, summarize; survives a session death"

[preflight]
required = ["python3"]

[detect]
paths = [".claude/resumable-workflow/scripts/"]
