[tool]
description = "Claude Code regression test harness (internal)"
exclude = true

[preflight]
required = ["python3"]

[post_install]
script = "post-install.md"

[detect]
paths = [".claude/cc-regression-test/hooks/"]
