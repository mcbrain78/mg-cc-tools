[tool]
description = "Documentation pipeline (scan, generate, verify)"

[preflight]
required = ["python3"]
optional = ["lsp"]

[post_install]
script = "post-install.md"

[detect]
paths = [".claude/auto-doc/scripts/"]
